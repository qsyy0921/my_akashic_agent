# TDD - Dashboard Job Events Outbox Events Table

## Tests

- 在 `tests/test_runtime_overview_dashboard_plugin.py` 断言 panel JS 资产包含：
  - `Job Events`
  - `No job event totals sampled`
  - `No recent job events sampled`
  - `Event ID`
  - `Outbox Events`
  - `No outbox event totals sampled`
  - `No recent outbox events sampled`
  - `Delivery / Event`
- 继续运行 SDD governance/spec index 测试，确保新 spec 已被索引。

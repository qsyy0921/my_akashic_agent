# Akashic Go 迁移 Open Issues

最后更新：2026-06-04

本文件是 `PROBLEM_REGISTRY.md` 下当前 open issue 的详细表，记录所有尚未解决的问题、风险、待决策项和需要后续跟踪的缺口。和其它 SDD 文档分工如下：

- `TODO.md`：只放本轮必须全部完成的短任务，结束前必须清空或写明阻塞。
- `DONE.md`：只放已经完成并验证的事实。
- `BACKLOG.md`：放未来候选任务、长期规划和暂不承诺的改造方向。
- `LIVE_CHECKS.md`：放需要真实运行环境或人工观察的验证项。
- `PROBLEM_REGISTRY.md`：所有问题文档的总入口、分类规则和 open issue 索引。
- `OPEN_ISSUES.md`：放尚未解决的问题、风险、待决策项和需要后续跟踪的缺口。

进入某轮迭代前，如果某个 open issue 要在本轮解决，先把它拆成可完成任务写入 `TODO.md`；完成后从本文件移除或改为已解决记录并同步 `DONE.md`。

## 当前未解决问题

| ID | 领域 | 问题 | 影响 | 下一步 | 状态 |
| --- | --- | --- | --- | --- | --- |
| OI-001 | QQ / Telegram / Observe | QQ/NapCat Go adapter 已完成双账号私聊文本、单群文本、`go_local_outbox_worker` 文本 owner smoke、account-kind file gate，以及 account-conversation-kind file gate。群图片/文件已明确走到 OneBot WebSocket `upload_file_stream` staging；第一账号 `1049511700` 的原生 group 对照在 `27234224`、`3219982`、`164369633` 三个群上都复现了 image/file 最终 `rich media transfer failed`，且简单 `docker restart napcat` 后仍失败。本轮又通过 `scripts/verify-go-migration-goal.ps1 -IncludeNativeRichMediaProbe -RichMediaProbeGroupId 3219982` 复核到同样的 `image_send.retcode=1200` / `file_send.retcode=1200`。同时，本轮先修复了 Go OneBot WebSocket 对结构化 `status` 的解析，再修复了 repo-local launcher 缺失 `AKASHIC_DELIVERY_CHANNEL_BY_ACCOUNT` 的问题；在两处修复后，`scripts/verify-go-outbox-scope-live.ps1` 与统一 goal 脚本的 `-IncludeOutboxScopeSmoke` 已再次稳定证明：第一账号 `private file` 与第二账号 `group file` 仍真实自动成功，而第一账号 `group file` 与第一账号 `private image` 仍被 gate 保持 `queued/attempts=0`。随后又把 QQ 群发送升级成“可手动开关”的全局策略：当前 Go runtime `AKASHIC_QQ_GROUP_SEND_ENABLED=false`、Python `group_send_enabled=false`，因此两个 QQ 账号的任何 group route 都会被 `qq group sends are disabled` 稳定阻断，private route 不受影响。与此同时，observe-only 群静默仍保留为更细粒度的 runtime invariant。第二账号 `2365524513` 的原生与 Akashic 对照进一步表明：group file 可以成功，但 image 仍原生失败且 Akashic 同步失败。private 对照又确认：两账号 private image 仍原生失败，而 private file 两账号都成功；第一账号 Akashic private file 也成功。当前 Go worker 已可在全局 `text` 基础上额外放开第二账号 `file` 与第一账号 `private file`，但由于全局 group send toggle 当前关闭，这些群路由不会实际发送。本轮还新增 `scripts/verify-qq-cutover-route-matrix.ps1`，把当前 route 明确拆成 `go_execution_owner_scope/currently_sendable_routes/policy_blocked_routes/platform_blocker_routes`。 | 文本路径已经可以由 Go 作为真实 execution owner；当前 rich-media blocker 已被拆成三层：`image` 在两个账号、group/private 两种路由上都原生失败，因此不是 Akashic adapter 问题；`file` 在 private 两账号都成功、在 second-account group 也成功；`file` 只在 first-account group 路由失败。与此同时，QQ 群静默现在既有 observe-only route invariant，也有可手动开关的全局 `qq/group` policy，因此即使 future outbox/config 漂移，当前本机也不会向任何 QQ 群发消息。新的 route matrix verifier 已能在当前 turn 直接区分“Go 已接管”“群发策略拦截”“平台 blocker gated”，但仍不能声称全量 QQ cutover 完成，因为第一账号 group-file 和所有 image 路由还没恢复。 | 保留 `scripts/run-qq-group-live-smoke.ps1`、`scripts/run-napcat-native-rich-media-smoke.ps1`、`scripts/verify-go-outbox-scope-live.ps1`、`scripts/verify-qq-cutover-route-matrix.ps1` 和 `scripts/verify-go-migration-goal.ps1` 作为重复验证入口；继续人工重登/替换第一账号 group rich-media 会话，并在 image 原生问题解决前维持 account-conversation-kind gate，不做全局 `image/file` 扩容；需要重新允许群发时，显式打开 `config.toml` 中 QQ `group_send_enabled` 与 `scripts/start-agent-runtime.ps1 -QQGroupSendEnabled true`。 | Open |
| OI-002 | Queue / MQ | NATS JetStream 仍是当前唯一已实现、且被推荐的第一外部 MQ；Redis Streams / RabbitMQ adapter 仍未实现。本轮已通过 `scripts/verify-mq-adapter-boundary.ps1` 与 dashboard `/api/dashboard/runtime-overview` 证明：`queue_backend` capability matrix 已完整透传，selected provider、recommended external MQ 和 planned-only adapter 边界都能在 live runtime 和 dashboard 里直接看到。 | 当前剩余 gap 不再是 visibility/read-model，而是未来如果部署环境真的指定 Redis Streams 或 RabbitMQ，就还需要新增 infrastructure adapter 和对应 cutover smoke。 | 保留 `scripts/verify-mq-adapter-boundary.ps1` 与 unified goal verifier 作为重复验证入口；只有出现明确 MQ 部署需求时，再补 Redis/Rabbit adapter，而不是为了形式完整提前扩实现面。 | Open |
| OI-003 | AgentJob / Worker | `agent_job` external lease result-ack 仍处于 readiness/plan/cutover 边界。本轮除 `scripts/verify-agent-job-external-lease-nats-smoke.ps1` 外，又新增了真正的 approval-bound API `GET /v1/agent-job-external-lease/preflight`，并增强 `scripts/verify-agent-job-external-lease-cutover-preflight.ps1` 在 repo-owned temp runtime 中固定三段语义：1) 无 external-lease flags 时 preflight 先停在 `agent_job_external_lease_plan_not_ready`；2) 满足 temp NATS、explicit result-ack flags、active `knowledge` worker coverage 后，不带 approval 的 preflight 稳定返回 `missing_approval_id`；3) 注入 active approval 后，同一路径返回 `agent_job_external_lease_preflight_ready`。本轮又新增 `scripts/start-agent-runtime.ps1` 的 external-lease launcher 参数面，以及 `scripts/verify-agent-job-external-lease-launcher-preflight.ps1`，在 repo-owned launcher temp runtime 中再次证明：外部 queue / strict-token / result-ack flags 能被 launcher 正确注入 Go runtime，`runtime-config` / `queue-backend` / `queue-topology` / `runtime-overview` 会一起翻到 promoted state，且 approval-bound preflight 同样稳定返回 `missing_approval_id` -> `agent_job_external_lease_preflight_ready`。本轮再新增 `GET /v1/agent-job-external-lease/launcher-bundle` 与 `scripts/verify-agent-job-external-lease-launcher-bundle.ps1`：当前已真实证明 canonical bundle 会稳定返回 exact launcher parameters、environment overrides、`QueueDSN` external input 和 verification steps，并且用 bundle 动态构造的 launcher 参数可以把 temp runtime 拉到 `repo_owned_launcher_external_lease_bundle_live_verified`。当前 blocker 已进一步收敛为 production runtime 仍保持 `provider=local` / `mode=local_state_store`，selected provider 不支持 `agent_job result-ack`，`AKASHIC_AGENT_JOB_STRICT_LEASE_TOKEN=false`、`AKASHIC_QUEUE_EXTERNAL_LEASE_AGENT_JOB_ENABLED=false`，而 queue topology 与 runtime overview 也都仍显示 `agent_job_execution_owner=python_ai_worker_state_store_lease`。 | Python AI worker 继续执行 job；Go 还没有进入生产 result-ack cutover。当前未完成点已从“repo-owned smoke / approval-bound preflight / launcher contract 是否存在”收敛到“何时注入外部 queue / strict token / result-ack flags 并切 owner”。 | 继续保留 `scripts/verify-agent-job-external-lease.ps1`、`scripts/verify-agent-job-external-lease-nats-smoke.ps1`、`scripts/verify-agent-job-external-lease-cutover-preflight.ps1`、`scripts/verify-agent-job-external-lease-launcher-preflight.ps1`、`scripts/verify-agent-job-external-lease-launcher-bundle.ps1`、`GET /v1/agent-job-external-lease/preflight`、`GET /v1/agent-job-external-lease/launcher-bundle` 与 unified goal verifier 作为重复验证入口；当 NATS 凭证和切换意图明确后，先按 bundle/plan 注入 queue backend / cutover / result-ack flags，再做 live runtime smoke，最后再让 owner 从 `python_ai_worker_state_store_lease` 切到 `python_ai_worker_with_nats_result_ack`。 | Open |
| OI-004 | Control Plane | capacity / priority / cutover plan 还没有真实 mutation executor。本轮除 `scripts/verify-worker-control-executor-boundary.ps1` 外，又新增 `scripts/verify-control-audit-boundary.ps1`，当前 live 证据已经固定证明：`/v1/control-mutations/policy` 的 allowlist、`/v1/operator-approvals` / `check`、`/v1/control-mutations` / `preflight`、`.akashic-workspace/agent-runtime/operator-approvals.json`、`.akashic-workspace/agent-runtime/control-mutations.json`，以及 `/v1/runtime-overview` 的 `control_mutation_policy` / `control_audit` summary/card/detail 在当前 turn 上保持一致，且全程只写 runtime state，不修改 env/config、不执行 cutover、不启动 worker、不 ack/nack MQ、不触发 Python AI；与此同时，`/v1/runtime-workers` 里仍没有 autoscaling / concurrency / priority executor worker。 | 目前 approval / audit / preflight / policy 边界已经 live verified，但系统仍只能给只读建议和审计，不能自动调并发、启动 worker 或修改配置。 | 后续必须把 approval、audit、allowlist、回滚和限流策略绑定到真实 executor 上，再继续复跑 `scripts/verify-worker-control-executor-boundary.ps1` 与 `scripts/verify-control-audit-boundary.ps1`，确认从 plan-only 进入 executor-present 后没有越权副作用。 | Open |
| OI-005 | Media Assets | 媒体内容访问已可诊断、代理，已有 recovery plan、approval-bound preflight、dashboard 链接和 Go HTTP/HTTPS 本地 cache recovery executor；当前已新增 `scripts/verify-media-asset-content-recovery-live-smoke.ps1`，在 temp runtime 中真实验证 approval-bound preflight、dry-run、不写 cache、正式 recovery、registry 更新、content 可读和 applied control-mutation audit。与此同时，`scripts/verify-media-recovery-boundary.ps1` 仍表明当前长期运行 runtime 的 sampled assets 主要是 `file://` observe-only QQ group 上传镜像，`content_diagnostics` 为 `forbidden=120/120`，sample recovery plan 为 `media_asset_content_recovery_fix_content_roots`，preflight 为 `missing_approval_id/executor_scope=operator_runtime_config`，且 `has_http_https_assets=false`。本轮又新增 `scripts/verify-dashboard-media-asset-content-recovery-boundary.ps1`，并修复 dashboard fallback 使 `/api/dashboard/runtime-overview` 的 `media_asset_content_recovery` summary/card/detail 在 fallback 模式下仍与 Go `/v1/runtime-overview` 保持一致。 | 对普通 HTTP/HTTPS 源，Go recovery executor 和 dashboard/runtime read-model 都已经有 repo-owned current-turn live evidence；对当前这台机器上的主流 `file://` 平台上传镜像，活跃边界仍是 operator content roots。对真正需要平台 token/cookie/session 的 QQ/Telegram 私有源，仍可能需要 Python/provider-specific worker 或人工重新采集。当前 open gap 已收敛到 private-source fetch、后台重拉与恢复后 AI enrichment，而不是 HTTP/HTTPS executor 或 dashboard 读模型。 | 后续设计 provider-specific media fetch worker 与恢复后 AI enrichment job admission；保持 Go 继续管资产生命周期、approval/audit/cache state，Python 管 OCR/VLM/语义解析和平台私有策略。 | Open |
| OI-006 | Knowledge / RAG | RAG dataset/index state 目前主要来自 Go checkpoint snapshot 推导。本轮又新增 `scripts/verify-dashboard-knowledge-rag-state-boundary.ps1`，已真实证明 Go `/v1/knowledge-pipeline-diagnostics`、Go `/v1/runtime-overview` 与 dashboard `/api/dashboard/runtime-overview` 的 `knowledge_pipelines` card/top-level detail 在当前 turn 上保持一致；同时 unified artifact 已新增 `current_state.knowledge_configured_rag_datasets/knowledge_rag_datasets/knowledge_rag_dataset_index_ready` 与 `residual_classification.knowledge_rag_state_boundary=checkpoint_snapshot_derived_control_plane_live_verified`。 | 当前 gap 不再是 dashboard knowledge/RAG 可见性，而是 checkpoint snapshot 仍不能直接证明外部 RAGFlow/索引服务的真实 parse/index 状态。 | 若需要更强可靠性，接入稳定外部 index metadata，但不要把 provider-specific 原始 payload 耦合进 Go。 | Open |
| OI-007 | Python AI Worker | Python worker 并发控制、autoscaling 和优先级调度还没有真实控制面执行器。本轮 live verifier 已把当前边界固定为 `go_control_plane_live_verified_without_worker_control_executor`：Go 能给 plan、policy 和审计边界，但没有 runtime worker 真正执行这些控制动作。同时，worker-status fencing 的默认 `HTTP 409`、显式 takeover 边界、同一实例 heartbeat renewal 续租语义，以及 stale heartbeat residue 的显式 cleanup mutation 现在都已有 repo-owned live smoke `scripts/verify-agent-worker-status-fencing-live-smoke.ps1`、`scripts/verify-agent-worker-status-heartbeat-live-smoke.ps1` 与 `scripts/verify-agent-worker-status-cleanup-live-smoke.ps1` 证明；当前 live runtime 的 stale worker 残留也已清到 `0`。因此该 issue 剩余 gap 已进一步收敛到 executor 本身，而不是 worker-status lease/cleanup 语义。 | 大量 image / knowledge / rag job 堆积时，Go 只能诊断和建议。 | 基于 AgentJob pressure、worker coverage、capacity/priority plan 设计 operator-approved executor；真正落地前，不要把 plan-only 误报成已具备 executor。 | Open |
| OI-008 | Frontend / Dashboard | runtime overview 已提供大量 card/detail，`rag_eval_failures`、`queue_topology`、`qq_cutover_route_matrix`、`control_audit / control_mutation_policy`，以及 `knowledge_pipelines` dashboard knowledge/RAG boundary 都已补齐 repo-owned current-turn 证据。当前 `scripts/verify-dashboard-knowledge-rag-state-boundary.ps1` 已真实证明 dashboard `/api/dashboard/runtime-overview` 的 `knowledge_pipelines` card/top-level detail 与 Go runtime 和 direct diagnostics 保持一致；`scripts/verify-go-migration-goal.ps1` 现在还会在这条 verifier 首次未返回 `live_verified` 时做一次短暂重试，避免把同一 turn 已通过的 dashboard knowledge/RAG parity 误写成 partial fallback。tracked runtime-overview read-model 已不再依赖 raw JSON-only detail 或纯 panel 字符串探针。 | 剩余问题不再是当前这批 runtime-overview drilldown 的展示缺口，而是未来新增 read-model、非 runtime-overview 页面，或 cutover/config/live smoke 本身。 | 继续优先消费现有 Go overview detail 做只读表格，不在 dashboard 加真实控制逻辑；只有出现新的 raw-JSON-only drilldown 时再补结构化展示，并优先补 repo-owned live verifier 而不是回退到静态 panel 探针。 | Open |
| OI-009 | SDD Governance | 需要持续防止 TODO、BACKLOG、LIVE_CHECKS、OPEN_ISSUES 职责混用，并保证每轮 goal 更新基于当前 live 证据而不是复述旧结论。当前统一入口已收敛为 `scripts/verify-go-migration-goal.ps1`，并在脚本里纳入 outbox scope smoke、observe-only silence、scheduler/proactive、agent-job external lease、worker-status restart/cleanup、`mq_adapter_boundary`、dashboard `control_audit` / `control_mutation_policy`、dashboard proactive tick-log runtime fallback、dashboard knowledge/RAG state boundary，以及 knowledge/RAG boundary 的单次 retry hardening；artifact 现已默认回写 `.codex-goal-verifier.json`，并稳定提供 `current_state`、`migration_residuals`、`migration_bucket_summary` 以及双入口 `dashboard_read_models` / `checks.dashboard_read_models`。 | 文档漂移会导致迭代再次变成半成品堆积，goal 状态也会失真；如果统一 verifier 和子脚本长期无人维护，goal 结论会重新退回人工拼接。 | 每轮结束前同步检查四类文档；新增 open issue 时写入本文件而不是 TODO；默认使用 `scripts/verify-go-migration-goal.ps1` 生成本轮统一 goal 证据，并在修改 Telegram / outbox scope / observe-only silence / scheduler / proactive / MQ boundary / native probe / worker-status restart / worker-status cleanup / dashboard control-audit verifier / dashboard proactive tick-log fallback verifier / dashboard knowledge-RAG verifier 逻辑后立即复跑。若后续仍需要 repo-local artifact，优先读取当前 turn 刚写出的 `.codex-goal-verifier.json`，并直接消费 `current_state` / `migration_residuals` / `checks.dashboard_read_models`，不要继续依赖旧快照或手工拼结论。 | Open |

补充更新（2026-06-04）：

- OI-003 这轮又新增了 `GET /v1/agent-job-external-lease/cutover-diff` 与
  `scripts/verify-agent-job-external-lease-cutover-diff.ps1`。当前剩余 gap
  已进一步收敛为 production runtime 的 flags / QueueDSN / owner 切换，不再是
  launcher contract 或 drift visibility 缺失。

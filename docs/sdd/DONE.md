# Akashic Go 迁移完成记录

最后更新：2026-06-04

本文件只做阶段归档，完整逐切片审查记录见 `docs/sdd/reviews/README.md` 和各 phase review 文档。

## 架构与服务边界

- Go 服务边界已统一为 `services/agent-runtime`，采用 DDD + 六边形架构分层。
- Go 负责 agent runtime/control plane，Python 负责模型调用、prompt、Memory/RAG/chunking/retrieval、OCR/VLM、工具执行、群知识沉淀和快速实验。
- Go runtime 默认使用 `.akashic-workspace/agent-runtime` 文件态保存确定性状态，可用 `AKASHIC_RUNTIME_STATE_DIR` 或单项 DSN/PATH 覆盖。
- 已补充 repo 内本地启动入口 `scripts/start-agent-runtime.ps1`：自动发现 `go.exe`、固化 migrated workspace 的 runtime/shadow 路径与当前双 QQ NapCat 最小环境变量，并把日志写入 repo `logs/`。
- SDD 任务文档已拆分为 `TODO.md`、`DONE.md`、`LIVE_CHECKS.md`、`OPEN_ISSUES.md`、`BACKLOG.md`：TODO 只保存本轮必须完成项，OPEN_ISSUES 记录所有未解决问题，避免长期规划或风险缺口导致 TODO 膨胀。
- `OPEN_ISSUES.md` 已纳入 SDD 入口说明、迭代流程和 review gate：每轮开始必须读取，留下风险、缺口、阻塞或待决策项时必须同步更新。
- unified goal verifier 现已支持显式 `-StdoutMode summary|full|none`：
  默认 stdout 只输出小摘要 JSON，`none` 只刷新 `.codex-goal-verifier.json`，
  `full` 保留完整 artifact stdout。artifact-first 路径已固定，且不改变
  `current_state`、`migration_residuals`、`migration_bucket_summary` 和
  `checks.dashboard_read_models` 的 schema。

## 消息、资产与发送链路

- 已实现 Go shadow audit、inbox、media asset registry、安全 content 访问和 dashboard 附件代理。
- 已实现 Go send ledger、private echo 判断和双账号防循环相关只读诊断。
- 已实现 Go outbox 生命周期、outbox event stream、delivery dispatch plan、readiness、smoke readiness 和 metrics。
- 已实现 Go outbox account pressure 只读诊断：按 `channel_kind:account_id` 汇总 queued/dispatching/active/dead-letter，并接入 runtime overview，为后续账号级限流提供数据基础。
- 已实现 Go local outbox delivery worker 账号级发送节流：支持 min interval / window limit，lease 前跳过被节流账号，blocked delivery 保持 queued，不误标 failed。
- 已实现 Go external lease outbox executor 账号级发送节流：复用同一 account rate limiter，在租约前判断被节流账号并返回 `nack/delivery_rate_limited`，不拿 outbox lease、不调用 delivery adapter，避免未来 NATS cutover 绕过限流。
- 已实现 Telegram DeliveryAdapter、QQ/NapCat OneBot HTTP/WebSocket DeliveryAdapter、adapter diagnostics、live health 和 runtime config 脱敏诊断。
- 已加入可选 Go local outbox delivery worker，默认关闭，避免未完成 cutover 时触发真实平台发送。
- 已实现 outbound cutover readiness：`/v1/outbound-cutover/readiness` 聚合 OneBot 配置、delivery smoke matrix、queue backend execution owner 和 runtime worker 状态，判断 QQ/NapCat 发送链路是否可交给 Go 执行，且不发送平台消息。
- 已实现 outbound cutover plan：`/v1/outbound-cutover/plan` 在 readiness 基础上输出 Go local outbox worker 或 NATS external lease 的启用步骤、验证入口和回滚步骤，保持只读且不改环境变量、不发平台消息。
- 已新增 repo-owned QQ cutover route-matrix verifier：`scripts/verify-qq-cutover-route-matrix.ps1` 会直接读取 `/v1/runtime-config`、`/v1/queue-backend`、`POST /v1/outbound-cutover/readiness`、`/v1/queue-topology` 与 `/v1/runtime-overview`，输出 `go_execution_owner_scope/currently_sendable_routes/policy_blocked_routes/platform_blocker_routes`，把“已由 Go 接管”“被群发总开关拦截”“因 rich-media blocker 保持 gated”三类 route 在当前 turn 明确拆开。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `qq_cutover_route_matrix` verifier 输出并入统一 goal 证据，`qq_outbox_cutover.route_matrix` 不再只靠 `allowed_routes + unresolved_routes` 粗粒度摘要；当前 live 结论为 `qq_partial_go_cutover_route_matrix_live_verified`。
- 已把 runtime overview 中 `qq_cutover_route_matrix` 的 dashboard drilldown 从 verifier-only / raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `go scope/currently sendable/policy blocked/platform blockers/group send enabled/execution scope`，以及 `Go Execution Owner Scope`、`Currently Sendable Routes`、`Policy Blocked Routes`、`Platform Blocker Routes` 四类 route 表；渲染只消费 Go detail，不开启 QQ 群发，不修改 route gate，不执行 native probe，不发送 QQ 消息。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `dashboard_read_models.qq_cutover_route_matrix_table=true` 纳入当前 turn 取证；当前 live `QQ Cutover Route Matrix` card 可直接读出 `go scope=5/currently sendable=5/policy blocked=3/platform blockers=5`，不再需要单独跑 route-matrix verifier 才能看主 panel 结论。
- 已在 migrated workspace 本机启动并验证 Go runtime：`127.0.0.1:8780` 的 `/healthz`、`/v1/runtime-config`、`/v1/runtime-overview`、`/v1/outbound-cutover/readiness`、`/v1/outbound-cutover/plan` 均可访问；当前 OneBot 双账号 preflight healthy，但 real cutover 仍因 `outbox_execution_path_not_ready` 保持 blocked。
- 已新增 repo 内 QQ live smoke 入口 `scripts/run-qq-live-smoke.ps1`：通过 `POST /v1/outbound`、`POST /v1/delivery-dispatch/readiness`、`POST /v1/delivery-dispatch/send` 和显式 `succeeded` 回写，对双 QQ 账号做受控私聊文本 smoke，而不启用 Go local outbox worker。
- 已完成双 QQ 账号真实私聊文本 smoke：`1049511700 -> 2365524513` 与 `2365524513 -> 1049511700` 两个方向都返回 `status=sent` 和 OneBot `provider_message_id`，对应 smoke outbox event 最终状态均为 `succeeded`；Telegram 仍未配置 token。
- 已修复 Go OneBot WebSocket delivery adapter 对非 echo 事件帧的解析容错：即使 NapCat 推送的无关事件携带数组形态的 `message` 字段，adapter 仍会继续等待 echo 响应，不再提前因 JSON 反序列化失败中断 QQ 媒体发送。
- 已新增 repo 内 QQ 群 live smoke 入口 `scripts/run-qq-group-live-smoke.ps1`：自动读取 runtime config、adapter health 和 observe target，针对一个真实 QQ 群执行 text/image/file smoke，并为每个 case 记录 readiness、dispatch/error 和最终 outbox 状态。
- 已完成 QQ 群文本真实 smoke：对 `qq:1049511700:group:27234224` 发送群文本，Go `delivery-dispatch/send` 返回 `provider_message_id=1774231456`，对应 smoke outbox event 最终状态为 `succeeded`。
- 已完成 QQ 群图片/文件真实 smoke 的 blocker 归档：群图片与群文件在当前 NapCat/QQ session 下都返回 `rich media transfer failed` 平台错误；图片 case 进入 `dead_lettered`，文件 case 保持 `queued`，说明当前 rich media 上传问题已从“适配器解析失败”收敛为真实平台侧 blocker。
- 已确认 QQ rich media blocker 更接近当前 NapCat / QQ session 本身，而不是本地文件路径编码问题：改用公网 URL 作为 image/file smoke 输入后，群图片仍失败（下载链路报 `Bad Gateway` 后续落到 `无法获取用户信息`），群文件仍返回 `rich media transfer failed`。
- 已修复 Go local outbox worker 的重复 `dispatching` 状态推进：`LeaseNext` 已负责把 delivery 置为 `dispatching` 并累加 attempts，worker 不再额外调用 `MarkDispatching`，避免单次 smoke 在真实发送成功后被错误打成 `dead_lettered`。
- 已完成 Go local outbox worker 真实 execution-owner smoke：临时以 `AKASHIC_OUTBOX_DELIVERY_WORKER_ENABLED=true` 启动 runtime 后，`/v1/runtime-workers` 显示 `outbox_delivery_worker.enabled=true/running=true`，`/v1/outbound-cutover/readiness` 变为 `execution_ready=true/execution_owner=go_local_outbox_worker`，且 `1049511700 -> 2365524513` 的自动 outbox 私聊文本 smoke 最终为 `succeeded`，`/v1/send-ledger/recent` 返回 `recent=true`。
- 已完成 local outbox worker 回滚验证：恢复默认 `scripts/start-agent-runtime.ps1` 后，`runtime_config.workers.outbox_delivery_worker_enabled=false`，`/v1/outbound-cutover/readiness` 回到 `execution_ready=false` 和 `outbox_execution_path_not_ready`，确认当前仓库默认运行态仍保持保守模式。
- runtime overview 已聚合 outbound cutover plan：summary/card/detail 可直接看到当前/目标/推荐 execution owner、决策和 blocker 数，避免 dashboard/operator 入口漏看 QQ/NapCat cutover 门禁。
- runtime overview 已聚合 delivery smoke readiness：`Delivery Smoke` card 和 `delivery_smoke_*` summary 可直接展示默认 QQ/Telegram smoke matrix 是否具备 adapter 路由条件，仍不发送平台消息、不租约 outbox、不触发 Python AI。
- 已实现 `agent_job` external lease result-ack readiness：`/v1/agent-job-external-lease/readiness` 聚合 queue backend gate、strict lease token、runtime flag、AgentJob pressure 和 Python worker coverage，判断是否可以把 generic job 队列确认权扩展到 NATS external lease；Go 仍不执行 AI job。
- 已实现 `agent_job` external lease result-ack plan：`/v1/agent-job-external-lease/plan` 输出只读启用、验证和回滚步骤，明确 Python AI worker 继续执行模型/RAG/memory/OCR/VLM/图片任务，Go 只规划确定性 AgentJob 生命周期确认权。
- runtime overview 已聚合 `agent_job` external lease result-ack plan：summary/card/detail 可直接看到当前/目标/推荐 execution owner、决策、blocker 数和回滚步骤，仍保持只读且不 ack/nack MQ、不执行 AI job。
- Python dashboard 已规范化 Go-owned runtime control-plane detail：`agent_job_capacity_plan`、`agent_job_external_lease_readiness`、`agent_job_external_lease_plan`、`outbound_cutover_plan` 现在都可通过 `/api/dashboard/runtime-overview` 以稳定 summary/card/detail 读取，仍不接管 worker、MQ ack/nack、outbox 发送或 AI job 执行。

## 观察群与接收链路

- 已实现 Go observe target sync/list、receiver status、receiver lease、observe capture diagnostics。
- 已实现 Go-owned receiver lease cleanup：`POST /v1/receiver-leases/cleanup-expired` 可显式删除过期接收端租约，返回 deleted/remaining summary，并保持 `side_effect=runtime_state_only`；QQ/Telegram receiver 进程、observe-only 群采集和 Python AI pipeline 不受影响。
- runtime overview 和 Python dashboard fallback 已暴露 receiver lease cleanup 可见性：`receiver_lease_cleanup_required`、`receiver_lease_cleanup_endpoint` 和 `Receiver Leases` active/expired card value 可直接提示过期租约是否需要人工清理，但不会自动调用 cleanup 或影响接收端。
- 已实现 Go inbox metrics、receiver stale 降级、recent inbox activity 推断 receiver connected。
- 已实现 Telegram 和 QQ/NapCat inbound dedupe，包含 observe-only 群文本、图片、普通群消息、私聊和群文件上传 notice。
- 已实现 inbound dedupe metrics，并接入 runtime overview/dashboard。
- 已实现 Go-owned media asset content diagnostics：`/v1/media-assets/content-diagnostics` 可按 asset/filter 输出 ready/forbidden/unavailable/disabled/error，帮助前端定位图片/文件无法显示原因；Go 只做安全根目录访问预检，Python 继续负责 OCR/VLM/文件理解。
- 已实现 Go-owned media content recovery executor：`POST /v1/media-assets/content-recovery` 在 existing preflight、operator approval 和 control mutation audit 边界下，将 HTTP/HTTPS media source 下载到本地 cache root，更新 media registry 的 `local_path`、hash/size/mime/recovery metadata，并记录 applied/failed audit；Python 继续负责 OCR/VLM/文件解析、语义 memory 和 RAG。
- runtime overview 已聚合 Go-owned media content recovery audit：`Media Content Recovery` card/detail 从 `media_asset_content/recover_content` control mutation audits 只读汇总 applied/failed/planned/recent records 和 plan/preflight/recovery endpoints，仍不执行下载、恢复、审批、审计写入或 Python AI。
- runtime overview dashboard 的 `Media Content Recovery` detail 已从纯 JSON 提升为只读表格：展示 recovery audit totals、plan/preflight/recovery links 和 recent mutation rows；渲染时不调用 preflight/recovery，不创建 approval/mutation，不下载/缓存内容，不触发 Python AI。
- 已实现 Go-owned media asset retention diagnostics：`/v1/media-assets/retention-diagnostics` 可按 retention policy、age 和 TTL dry-run 参数输出 cleanup due advisory，明确 `side_effect=none`，不删除 registry、不删除文件、不执行 OCR/VLM/文件解析。
- runtime overview 已聚合 `Media Asset Content`：summary/card/detail 直接展示最近附件内容 ready/forbidden/unavailable/disabled/error 状态，方便 dashboard 定位 QQ 图片/文件显示问题，仍不执行 OCR/VLM/文件解析。
- dashboard runtime overview fallback 现已补齐 `Media Asset Content` parity：即使 direct `/v1/runtime-overview` 聚合超时，dashboard 也会从 `/v1/media-assets/content-diagnostics` 保留 `media_asset_content_*` summary、`Media Asset Content` card/detail，以及 sampled asset 的 `content_recovery_plan_endpoint` / `content_recovery_preflight_endpoint`；整个路径保持只读，不调用 preflight/recovery，不下载/恢复内容，不触发 Python AI。
- runtime overview 和 Python dashboard fallback 已聚合 `Media Asset Retention`：summary/card/detail 可直接看到 cleanup_due、permanent/default/ephemeral/unknown 统计和只读 retention diagnostics，仍不删除 registry 或文件，不触发 OCR/VLM/文件解析。
- 已实现 Go-owned media asset retention cleanup plan：`/v1/media-assets/retention-plan` 基于 retention diagnostics 输出只读 dry-run 清理计划、候选资产、operator approval/control mutation audit 绑定步骤、验证/回滚步骤和 `side_effect=none`；当前不删除 media metadata 或本地文件内容。
- runtime overview 和 Python dashboard fallback 已聚合 `Media Asset Retention Plan`：summary/card/detail 可直接看到 retention cleanup plan 是否 ready、候选数、blocker 数和 required steps；Python 只做只读展示，不创建 approval/mutation、不删除文件、不触发 OCR/VLM/AI。
- Go-owned control mutation policy 已支持 `media_asset_retention / cleanup_expired`：retention plan 输出的审批和 planned mutation audit 链路可通过 preflight 识别，unsupported media retention action 仍被阻断；当前仍不实现真实清理执行器、不删除 metadata 或文件。

## AgentJob、队列与 worker 状态

- 已实现 Go generic AgentJob 生命周期、文件态持久化、dedupe_key、event stream、metrics、dashboard 恢复入口。
- 已实现 AgentJob lease token、heartbeat/renew、strict token 模式、recover-expired、queue work id 精确租约入口。
- 已完成 NATS JetStream shadow_publish、dual_read_compare、external_lease 诊断门禁和 outbox external lease smoke。
- external_lease cutover gate 已增加 local outbox worker 冲突检查：`AKASHIC_OUTBOX_DELIVERY_WORKER_ENABLED=true` 时 gate 保持 blocked，防止 state-store worker 与 NATS external lease 同时执行真实发送。
- 已实现 agent_job external lease result-ack 映射和 subject 扩容门禁，当前仍需显式 smoke/cutover flag。
- `/v1/queue-backend` 已增加 MQ provider capability matrix：明确 NATS JetStream 是当前推荐第一外部 MQ，暴露 shadow/dual-read/external-lease/agent_job result-ack、多 goroutine consumer 和 delayed nack 能力，并把 Redis Streams / RabbitMQ 标记为后续可替换 adapter 边界。
- dashboard `/api/dashboard/runtime-overview` 的 `queue_backend` 现在已稳定透传 `supported_providers`、`provider_capabilities` 和 `selected_provider_capability`，不会再在 Python 展示层丢失 MQ adapter capability matrix。
- `/v1/queue-backend` 已增加 execution owner 诊断：outbox delivery 可明确区分 `go_state_store_api`、`go_local_outbox_worker`、`nats_external_lease`；agent_job 可明确区分 Python 通过 state-store lease 执行，或 Python 执行后经 NATS result-ack 回确认。
- 已实现 Go-owned queue topology read model：`/v1/queue-topology` 将 queue backend、provider capability、execution owner 和 external lease gate 派生成 nodes/edges/work_kinds，直接展示 `outbox_delivery` 与 `agent_job` 的 queue source、execution owner、ack owner 和 blockers；全程只读，不 publish/lease/ack/nack/term MQ，不执行 AI job。
- runtime overview 已聚合 queue topology：summary/card/detail 现在可直接展示 queue topology 的 nodes/edges/work_kinds、external lease readiness、outbox execution owner、agent_job execution owner 和 ack owner；Python dashboard 也已规范化 `queue_topology` detail，仍保持只读且不执行 MQ/outbox/AgentJob/AI。
- runtime overview dashboard 的 `Queue Topology` detail 已从纯 JSON 提升为只读表格：展示 provider/mode/phase/external lease、nodes/edges 计数，以及每个 work kind 的 queue source、execution owner、ack owner、allowed 状态和 blockers；渲染时不 publish/lease/ack/nack/term MQ，不创建或执行 AgentJob/outbox，不触发 Python AI。
- external_lease 已增加 Go-owned 执行诊断：`/v1/queue-backend` 可查看 ack/nack/term disposition、reason、work_kind 统计和 bounded recent executions；只读诊断不执行 Python AI job。
- runtime overview 已聚合 external_lease 执行诊断：summary/card 可直接查看执行总数、错误数、ack/nack/term 分布，原始 queue backend detail 仍保留。
- runtime overview summary 已聚合 queue execution owner 字段：`queue_outbox_execution_owner` 和 `queue_agent_job_execution_owner` 能直接说明当前执行边界，避免把 NATS result-ack 误解为 Go 执行 AI job。
- 已实现 Go-owned `AgentJob` pressure 诊断：`/v1/job-metrics` 可按 `job_type` 查看 pending / leased / running / active / oldest_pending_age，并标记 high pressure；runtime overview 也聚合了 `agent_job_pressure_*` summary 与 `Agent Job Pressure` card。
- runtime overview 已新增 `Agent Job Worker Coverage`：把 Go-owned `AgentJob` pressure 与 Python worker heartbeat / stale / failed 状态关联起来，直接解释 backlog 是否由无 active worker、stale worker 或 failed worker 导致，不改变 Python 执行边界。
- 已实现 Go-owned `AgentJob Capacity Plan`：`/v1/agent-job-capacity/plan` 将 job pressure 与 Python worker coverage 转为只读运维建议，可区分需要恢复 worker、检查 failed/stale worker、或在已有 active worker 下调优并发/优先级；Go 不启动 worker、不执行 AI job、不 ack/nack MQ。
- 已实现 Go-owned `AgentJob Priority Plan`：`/v1/agent-job-priority/plan` 将 job pressure 与 Python worker coverage 转为确定性优先级/背压建议，输出 rank、priority class、score、action 和 blockers；Go 仍不做真实调度、不启动 worker、不改 MQ、不执行 AI job。
- runtime overview 已聚合 `Agent Job Capacity`：summary/card/detail 可直接看到 capacity ready/reason/blockers、blocked/warning/high-pressure job types、max pending/active 和完整 plan，仍保持只读且不启动 Python worker、不调度、不执行 AI。
- runtime overview 已聚合 `Agent Job Priority`，Python dashboard 也已规范化该 detail：summary/card/detail 可直接看到 priority ready/reason/blockers、high/blocked/warning job types 和 max priority score，仍保持只读且不改变调度、worker、MQ 或 AI 执行。
- 已实现 Go-owned `Operator Approval Ledger`：`POST /v1/operator-approvals` 和 `GET /v1/operator-approvals` 可记录/查询 operator 对控制面计划的 approved/rejected/revoked 审计，默认持久化到 `.akashic-workspace/agent-runtime/operator-approvals.json`，并明确 `side_effect=runtime_state_only`，不修改配置、不执行 cutover、不触发 Python AI。
- 已实现 Go-owned `Operator Approval Check`：`GET /v1/operator-approvals/check` 可按 `approval_id` 或 `target_kind + target_id` 校验 active approved 记录，返回 `approved/reason/blockers/approval/side_effect=none`，为后续真实控制面 mutation 绑定 approval id 提供只读 preflight。
- 已实现 Go-owned `Control Mutation Audit Ledger`：`POST /v1/control-mutations` 和 `GET /v1/control-mutations` 可记录/查询 planned/applied/failed/rolled_back 控制面变更审计，默认持久化到 `.akashic-workspace/agent-runtime/control-mutations.json`，并明确 `side_effect=runtime_state_only`，不修改配置、不执行 cutover、不启动 worker、不 ack/nack MQ、不触发 Python AI。
- 已实现 Go-owned `Control Mutation Preflight`：`GET /v1/control-mutations/preflight` 可在真实控制面变更前校验 active approved approval，返回 blockers 和建议 planned mutation audit 绑定信息，明确 `side_effect=none`，不创建审计、不修改配置、不执行 cutover、不启动 worker、不 ack/nack MQ、不触发 Python AI。
- 已实现 Go-owned `Control Mutation Policy`：preflight 现在会在 approval 校验前先按 domain policy 校验支持的 target/action，unsupported target/action 返回稳定 blocker，防止后续控制面执行器误把任意字符串变成可执行 mutation。
- 已实现 Go-owned `Control Mutation Policy API`：`GET /v1/control-mutations/policy` 可只读查询当前支持的控制面 mutation target/action，支持按 `target_kind` 过滤，返回 `side_effect=none`，避免 Python/dashboard 复制 allowlist。
- 已新增 repo-owned `control audit boundary` isolated live verifier：`scripts/verify-control-audit-boundary.ps1` 会启动隔离 temp `agent-runtime`，真实写入 `operator approvals` 与 `control mutations` ledger，随后复核 `GET /v1/operator-approvals`、`/v1/operator-approvals/check`、`/v1/control-mutations`、`/v1/control-mutations/preflight`、`/v1/control-mutations/policy`、`.akashic-workspace/agent-runtime/operator-approvals.json`、`.akashic-workspace/agent-runtime/control-mutations.json`，以及 `/v1/runtime-overview` 的 `control_mutation_policy` / `control_audit` summary/card/detail 在当前 turn 上保持一致，并固定证明这组端点当前只写 runtime state，不修改 env/config、不执行 cutover、不启动 worker、不 ack/nack MQ、不触发 Python AI。
- runtime overview 已聚合 Go-owned control mutation policy：summary/card/detail 可直接看到 allowlist target/action 数量与只读 policy 明细，Python/dashboard 不需要维护控制面 mutation allowlist。
- Python dashboard 已规范化 Go-owned control mutation policy：`/api/dashboard/runtime-overview` 稳定透传 `control_mutation_policy` detail、summary 默认值和 card；Python 仍只做展示适配，不维护 allowlist、不执行控制面变更。
- runtime overview dashboard 的 `Control Mutation Policy` detail 已从纯 JSON 提升为只读表格：展示 allowed/reason/target/action totals，以及 target kind 到 allowed actions 的 allowlist；渲染时不编辑 policy、不创建 approval/mutation、不执行 cutover、worker scaling、MQ ack/nack、media cleanup 或 AI。
- runtime overview 已聚合 Go-owned control audit：summary/card/detail 可直接看到 operator approvals、active approvals、control mutations、failed/rolled_back mutation audits 和 bounded recent records，仍保持只读且不记录 approval/mutation、不执行任何控制面变更。
- runtime overview dashboard 的 `Control Audit` detail 已从纯 JSON 提升为只读表格：展示 operator approval totals、control mutation totals、recent approval rows 和 recent mutation rows，并保留 raw JSON fallback；渲染时不创建/检查/撤销 approval，不记录 mutation，不执行 cutover、worker scaling、MQ ack/nack、media cleanup 或 AI。
- 已新增 repo-owned dashboard `control audit` boundary live verifier：`scripts/verify-dashboard-control-audit-boundary.ps1` 会直接比对 live Go `/v1/runtime-overview` 与 dashboard `/api/dashboard/runtime-overview` 的 `control_audit` / `control_mutation_policy` summary、top-level detail 与 card parity；`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.control_audit_table` 与 `dashboard_read_models.control_mutation_policy_table` 切到这条 live verifier 输出，不再只靠 panel 静态字符串探针。
- 已实现 Go-owned media asset retention cleanup preflight：`GET /v1/media-assets/retention-cleanup/preflight` 聚合 retention plan 与 control mutation preflight，校验 active approval 并返回建议 planned audit，明确 `side_effect=none`，不删除媒体 metadata/文件、不创建 approval/mutation、不触发 OCR/VLM/RAG/AI。
- 已实现 Go-owned media asset retention metadata cleanup executor：`POST /v1/media-assets/retention-cleanup` 通过 active approval preflight 后仅删除 Go 媒体登记元数据，并记录 applied/failed control mutation audit；dry-run 和 preflight 失败不产生副作用，且不删除本地文件、不触发 OCR/VLM/RAG/AI。
- runtime overview 已聚合 media retention cleanup：summary/card/detail 可直接看到候选数、recent cleanup audits、applied/failed 计数和 plan/preflight/cleanup endpoint，仍保持只读且不创建 approval/mutation、不执行 cleanup、不删除 metadata/文件、不触发 Python/OCR/VLM/RAG/AI。
- Python dashboard 已规范化 Go-owned media retention cleanup：`/api/dashboard/runtime-overview` 稳定透传 `media_asset_retention_cleanup` summary/card/detail 和 fallback 默认值，仍保持只读且不创建 approval/mutation、不执行 cleanup、不删除 metadata/文件、不触发 Python/OCR/VLM/RAG/AI。
- 已把 runtime overview 中 `media_asset_retention_plan` 与 `media_asset_retention_cleanup` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 retention plan 的 `ready/assets/candidates/required steps/verify steps/rollback steps`、required/verification/rollback steps 表格，以及 cleanup 的 `ready/assets/candidates/planned/applied/failed/audits/rolled back`、endpoints、blockers 与 notes；渲染只消费 Go detail，不执行 cleanup、不创建 approval/mutation、不删除 metadata/文件、不触发 Python/OCR/VLM/RAG/AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `media_asset_retention_plan` 与 `media_asset_retention_cleanup` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 retention plan/cleanup 在内的主要 runtime-overview read-model。
- 已把 runtime overview 中 `media_asset_retention` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `assets/cleanup_due/default/ephemeral/permanent/unknown`、recent assets 的 `asset/name/retention class/cleanup due/age/cleanup after/reason/target`，以及 diagnostics notes；渲染只消费 Go detail，不执行 cleanup、不创建 approval/mutation、不删除 metadata/文件、不触发 Python/OCR/VLM/RAG/AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `media_asset_retention` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 retention diagnostics/plan/cleanup 在内的主要 runtime-overview read-model。
- 已把 runtime overview 中 `dead_letters` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 AgentJob / outbox dead letters 的 totals、recent rows 和 dead-letter totals；渲染只消费 Go detail，不 ack/nack/term MQ、不重试 AgentJob 或 outbox delivery、不修改 queue owner / delivery policy、不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `dead_letters` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 dead letters 在内的主要 runtime-overview read-model。
- 已把 runtime overview 中 `checkpoint_lag` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `checkpoints/max lag/workers/stale after`，以及 lagged checkpoints 的 `session/job type/dataset/latest seq/checkpoint seq/lag messages/freshness/reason`；渲染只消费 Go detail，不推进 checkpoint、不创建/重试 AgentJob、不修改 worker lease / queue owner、不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `checkpoint_lag` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 checkpoint lag 在内的主要 runtime-overview read-model。
- 已实现 Go-owned media asset content access plan：`GET /v1/media-assets/content-access-plan?asset_id=...` 可在前端点击附件前返回 ready/reason/blockers/content endpoint/verify/fallback steps，保持 `side_effect=none`，不流式返回内容、不下载远程媒体、不触发 OCR/VLM/RAG/AI。
- Python dashboard media content proxy 已接入 Go-owned access plan fallback：`/api/dashboard/media-assets/content` 在 Go content route 失败且旧 workspace fallback 不可用时返回结构化 `content_access_plan`，Python 不复制访问策略、不下载远程媒体、不触发 OCR/VLM/RAG/AI。
- Python dashboard 已新增 Go-owned media content access plan 只读代理：`/api/dashboard/media-assets/content-access-plan?asset_id=...` 直接透传 Go plan detail，前端可在打开附件前查看 ready/reason/blockers，Python 不复制访问策略、不触发 OCR/VLM/RAG/AI。
- dashboard 消息列表/详情的 `media_assets` 已新增 `content_access_plan_url`：前端可从每条消息的媒体资产直接跳到 Go-owned access plan 代理，Python 不主动批量拉取 plan、不复制访问策略、不触发 OCR/VLM/RAG/AI。
- 已新增 `MediaAssetContentAccessPlan` 合同 fixture：固化 Go `/v1/media-assets/content-access-plan` 与 dashboard `/api/dashboard/media-assets/content-access-plan` 的字段边界，包括 ready/reason/blockers/content endpoint/dashboard path/side_effect。
- Go contract fixture tests 已覆盖 `MediaAssetContentAccessPlan`：Go 侧现在会加载 `media_asset_content_access_plan.qq.image.json`，并校验 ready/reason/path/content endpoint/side_effect/required steps，防止 Go/Python access plan 字段漂移。
- Go `MediaAssetContentAccessPlan` response 已补齐 `runtime_path`、`dashboard_path`、`content_url`：前端和 dashboard 可直接消费 Go 生成的确定性链接，Python 仍只做代理/展示，不复制媒体访问策略或触发 OCR/VLM/RAG/AI。
- 已实现 Go-owned media content recovery plan：`GET /v1/media-assets/content-recovery-plan?asset_id=...` 基于 access plan 输出 disabled/forbidden/unavailable/error 的只读恢复步骤、future executor scope 和 URL hints，保持 `side_effect=none`，不下载/恢复/流式传输内容、不触发 OCR/VLM/RAG/AI。
- Python dashboard 已新增 media content recovery plan 只读代理：`/api/dashboard/media-assets/content-recovery-plan?asset_id=...` 透传 Go plan detail，浏览器可直接查看恢复建议，Python 不复制访问策略、不下载远程媒体、不触发 OCR/VLM/RAG/AI。
- Go media content diagnostics item 已新增 `content_recovery_plan_endpoint`，dashboard 消息列表/详情的 `media_assets` 已新增 `content_recovery_plan_url`，前端可从每条附件直接进入 Go-owned recovery plan；列表/详情渲染不主动请求 plan、不下载媒体、不触发 OCR/VLM/RAG/AI。
- Go media content diagnostics item 已新增 `content_access_plan_endpoint`，Python runtime overview dashboard 会规范化并在旧 runtime 未返回时按 asset id fallback 生成，使批量诊断可直接跳到单资产 access plan。
- 已实现 Go-owned media content recovery preflight：`GET /v1/media-assets/content-recovery/preflight` 将现有 recovery plan 绑定到 `media_asset_content/recover_content` control mutation policy、operator approval check 和 suggested planned audit；调用后不创建 approval/mutation、不下载/恢复/缓存内容、不流式传输内容、不触发 OCR/VLM/RAG/AI。
- Go-owned control mutation policy 已支持 `media_asset_content / recover_content`，为未来受控 media downloader/cache executor 提供 allowlist 门禁；当前仍不实现真实下载、恢复或缓存执行器。
- Python dashboard 已代理 Go-owned media content recovery preflight：`/api/dashboard/media-assets/content-recovery/preflight` 透传 `asset_id/target_id/operator_id/approval_id` 到 Go，并在 message `media_assets` 中新增 `content_recovery_preflight_url`；列表/详情渲染只暴露链接，不主动请求 preflight，不创建 approval/mutation，不下载/恢复/缓存内容，不触发 OCR/VLM/RAG/AI。
- Go media content diagnostics item 已新增 `content_recovery_preflight_endpoint`，runtime overview dashboard 的 `Media Asset Content` 表格也新增 preflight 链接；打开 detail 只展示 Go-owned endpoint，不主动调用 preflight，不创建 approval/mutation，不下载/恢复/缓存内容，不触发 OCR/VLM/RAG/AI。
- runtime overview dashboard 的 `Media Asset Content` detail 已从纯 JSON 提升为只读表格：展示 assets/ready/forbidden/unavailable/disabled/error totals、asset/name/status/reason，并提供 content/access/recovery 三类链接；渲染时不额外请求 plan、不打开内容、不下载媒体、不触发 OCR/VLM/RAG/AI。
- `services/agent-runtime/README.md` 和 SDD index 已同步当前 Go media asset API：content diagnostics/access plan、retention diagnostics/plan、cleanup preflight/executor 及各自 side-effect 边界。
- runtime overview 已聚合 `agent_job` external lease readiness：summary/card/detail 直接展示 result-ack gate、strict token、execution owner/scope 和 Python worker coverage blockers，仍保持只读且不执行 AI job。
- 已实现 Go-owned `Knowledge Pipeline Diagnostics`：按 observe-only QQ 群聚合 capture、`group_memory_extract` / `rag_ingest`、checkpoint 和 worker coverage，并接入 runtime overview `Knowledge Pipelines` card，开始把群知识编排状态沉淀为稳定 control-plane 视图。
- `Knowledge Pipeline Diagnostics` 已新增 source-seq lag / stalled 诊断：Go 可按群比较 inbox `metadata.seq` 与 checkpoint cursor，直接看出 memory / rag checkpoint 是否落后、是否在高压下停滞，并聚合到 runtime overview summary。
- `Knowledge Pipeline Diagnostics` 已新增 checkpoint age / stagnant 诊断：Go 会按群暴露 memory / rag checkpoint `age_seconds`，区分短暂 lag 与长时间未推进的 stagnant pipeline，并把 stale/stagnant target 汇总到 runtime overview。
- `Knowledge Pipeline Diagnostics` 已新增 per-group job lease freshness：Go 会按群暴露 `group_memory_extract` / `rag_ingest` stage 的 pending/active age、stale active lease、expired active lease，并把 lease 卡死直接提升到 pipeline warn/blocked。
- `Knowledge Pipeline Diagnostics` 已新增 per-dataset RAG state：Go 现在能按群输出每个 `dataset_id` 的 `rag_ingest` stage、checkpoint、lag 和 degraded 状态，避免只看到群级 `rag_checkpoint_lag_max` 却不知道是哪一个 dataset 出问题。
- observe-only QQ 群现已支持配置期 RAG dataset 绑定：Python `QQGroupConfig.ragflow_dataset_ids` 和全局 `ragflow.default_dataset_ids` 会同步到 Go `observe_target.metadata`，`Knowledge Pipeline Diagnostics` 可在 job/checkpoint 尚未生成前先显示“已配置但未启动”的 dataset。
- `rag_ingest` 成功后的稳定快照元数据现已沉到 Go checkpoint metadata，并由 `Knowledge Pipeline Diagnostics` 以结构化 `ingest_snapshot` 暴露：包括 message/document 数、seq 范围、parse 请求标记和 display name，前端不再需要自己解析原始 metadata。
- `Knowledge Pipeline Diagnostics` 已新增 per-dataset RAG index state：Go 基于 checkpoint snapshot metadata 推导 ready/missing snapshot/empty index/source lagging 状态，并聚合到 runtime overview，不调用 RAGFlow、不接管 Python RAG 策略。
- observe-only knowledge job 定期入队已迁到 Go runtime：新增 `knowledge_job_planner` worker，基于 Go observe targets 创建 `group_memory_extract` / `rag_ingest` AgentJob，并复用现有 dedupe/backpressure；Python knowledge worker 检测到 Go planner 启用后只做 lease 执行。
- 已实现 Go-owned knowledge job planner preview：`/v1/knowledge-job-planner/preview` 可在不创建 AgentJob 的情况下展示 eligible observe-only QQ 群、跳过原因、计划创建的 `group_memory_extract` / `rag_ingest` job、dedupe key、route、payload 和 dataset 绑定。
- runtime overview 已聚合 knowledge job planner preview：summary/card/detail 可直接看到计划 admission 的 observe-only QQ targets、groups、`group_memory_extract` jobs 和 `rag_ingest` jobs，仍保持只读且不创建 AgentJob。
- 已实现 Go-owned knowledge job planner readiness：`/v1/knowledge-job-planner/readiness` 聚合 preview、runtime config、runtime worker 和 Python knowledge worker heartbeat，输出 ready/reason/blockers，作为启用真实 admission 前的只读门禁。
- runtime overview 已聚合 knowledge job planner readiness：summary/card/detail 可直接看到 planner 是否 ready、blocker 数、planner enabled/running 状态和 Python knowledge worker 可用性，仍保持只读且不创建 AgentJob。
- 已实现 Go-owned knowledge job planner cutover plan：`/v1/knowledge-job-planner/cutover-plan` 输出 Go planner 与 Python legacy enqueue 的当前/目标 admission owner、required/enable/verify/rollback steps 和 blockers；runtime overview 已聚合 `Knowledge Planner Cutover`，全程只读，不改环境变量、不创建 AgentJob、不执行 group memory/RAG/AI。
- Python dashboard 已规范化 Go-owned `knowledge_job_planner_cutover_plan`：`/api/dashboard/runtime-overview` 会暴露 cutover summary 默认值、`Knowledge Planner Cutover` card 和只读 readiness/step/detail，仍不接管 admission、不创建 AgentJob、不执行 group memory/RAG/AI。
- repo 本地启动入口 `scripts/start-agent-runtime.ps1` 已支持显式启用 Go `knowledge_job_planner`：可通过 `-EnableKnowledgeJobPlanner` 和 planner interval/max-attempts/rag-max-messages 参数启动本地 runtime，而不需要手工改脚本常量。
- Python `AgentGatewayKnowledgeWorker` 已补充 Go-owned admission 的显式 skip 日志：当 runtime config 声明 `knowledge_job_planner_enabled=true` 时，worker 现在会记录 `skip legacy enqueue`，为后续 Python 进程重启后的 live 验证提供直接证据。
- 已完成本机 Go knowledge planner live bring-up：当前 `scripts/start-agent-runtime.ps1 -EnableKnowledgeJobPlanner -KnowledgeJobPlannerIntervalSeconds 300 -KnowledgeJobPlannerMaxAttempts 3 -KnowledgeJobPlannerRagMaxMessages 200` 可把 `/v1/runtime-config.workers.knowledge_job_planner_enabled` 和 `/v1/runtime-workers.knowledge_job_planner.running` 拉到 `true`，且 Go planner 会实际创建 6 个 observe-only `group_memory_extract` jobs。
- 已完成 knowledge planner admission owner 切换验证：当前 `/v1/knowledge-job-planner/cutover-plan` 返回 `decision=ready/current_admission_owner=go_runtime_knowledge_job_planner`，`/v1/runtime-overview.summary` 也同步显示 `knowledge_job_planner_cutover_plan_ready=true/current_owner=go_runtime_knowledge_job_planner`。
- 已完成 Python legacy enqueue 抑制的运行态验证：启用 Go planner 后，`logs/akashic-local-run.err.log` 中最后一次 legacy enqueue 仍停在 `19:50:01`（旧 bucket），后续跨过原 60 秒 enqueue 周期没有新增 `agent_runtime_knowledge_worker] enqueued` 记录；同时 `/v1/jobs?type=group_memory_extract` 出现了由 `scheduler=agent-runtime-knowledge-job-planner` 创建并成功执行的新 bucket 任务。
- 已实现 Go-owned Python AI worker status registry，并接入 runtime overview。
- Python AI worker status registry 已增加 Go-owned lease/fencing：新 reporter 上报 `instance_id` 和 `lease_ttl_seconds`，Go 拒绝同一 `worker_id` 活跃租约期间的异实例状态覆盖。
- Python AI workers 已增加 worker status heartbeat renewal：image / knowledge / rag_eval / outbox 长任务运行期间周期上报 `running/current_job_id`，刷新 Go worker-status lease；AgentJob lease 语义不变。
- Python AI worker status 上报已按角色拆分独立 `worker_id`：`image` / `knowledge` / `rag_eval` / `outbox` 不再共用 `akashic-python-worker`，避免本地 bring-up 时多个 worker 向 Go runtime 抢同一 lease 并触发 `HTTP 409`。
- 已完成 Python 主进程重启后的 worker-status 受控 takeover：Go `/v1/agent-worker-statuses/report` 现在支持在 `replace_existing_instance_id` 与当前记录完全匹配时覆盖 stale lease；Python reporter 只会在 `HTTP 409`、旧 `instance_id` 可解析、旧 PID 已退出时重试一次 takeover。新增 `scripts/verify-agent-worker-status-restart.ps1` 后，本轮已通过 live restart 证明 fresh `logs/bot.log` 不再出现新的 `agent worker status lease conflict` / `agent worker status report failed`，且 `/v1/agent-worker-statuses` 中 `image/knowledge/outbox` 的 active `instance_id` 已切到当前 Python 主进程 PID。
- OneBot WebSocket local media 现在先走 `upload_file_stream`：Go adapter 对本地图片/文件会先把 bytes 流式上传到 NapCat，再用返回的 `file_path` 做最终 `send_group_msg` / `upload_*_file`，从而把 Docker 路径可见性问题和真实 QQ rich-media 平台失败拆开。
- 重新跑 QQ 群 rich-media live smoke 后，当前 blocker 已进一步收敛：文本仍成功；图片/文件都已明确在 stream staging 之后、NapCat 真正向 QQ 发 rich media 的最终步骤上失败，错误仍是 `rich media transfer failed`。

## Knowledge / Group Memory / RAG

- observe-only 群记忆抽取生命周期已迁到 Go generic jobs，Python 作为抽取 worker。
- observe-only 群知识任务 admission 已迁到 Go `knowledge_job_planner`，Python knowledge worker 保留兼容 fallback，但在 Go runtime 声明 planner 启用时不再自行入队。
- group-memory 和 RAGFlow 源消息回放已接入 Go inbox API。
- RAGFlow ingest 游标已接入 Go knowledge checkpoints。
- RAG evaluation jobs 已做成 Go-owned 生命周期记录，Python 作为 opt-in eval worker 执行离线 fixture 并回写指标。
- knowledge worker diagnostics、checkpoint dashboard 可见性，以及 job-type backlog pressure 可见性已完成。

## Proactive Runtime State

- delivery 去重、context-only 节流、drift interval mark 已迁到 Go proactive state。
- AnyAction quota、seen items、rejection cooldown、retention cleanup、background context global mark 已迁到 Go。
- proactive drift 完成态、recent runs 和 per-skill runtime state 已迁到 Go，Python 保留 workspace JSON mirror。
- proactive tick start/finish/step 审计状态已迁到 Go，Python 保留 SQLite `tick_log` / `tick_step_log` mirror 供现有 dashboard 兼容读取。
- dashboard proactive tick log 读路径已增加 Go fallback：SQLite 无匹配 tick 时读取 Go `/v1/proactive/tick-logs`、detail 和 steps；Go 查询支持 dashboard 所需分页、时间区间和排序。

## Scheduler Runtime State

- scheduler durable job snapshot、diagnostics、execution lease、job upsert/delete、completion mutation、startup recovery reconciliation 已迁到 Go。
- Python scheduler 仍负责 tick loop、cron/interval 语义、AI 执行和平台发送。

## Runtime Overview

- 已实现 Go-owned runtime overview aggregate endpoint，聚合 adapter、queue、send ledger、inbox、agent job、outbox、knowledge、observe、receiver、worker、scheduler 等诊断。
- runtime overview summary/card 已接入 selected queue provider capability：可直接展示 NATS-first 推荐阶段、provider 实现状态、多 goroutine consumer、delayed nack、external lease 和 agent_job result-ack 能力。
- dashboard 优先读取 Go aggregate，并保留必要 fallback。
- dashboard 已规范化 Go-owned runtime overview 的 `delivery_smoke_readiness` 和 `media_asset_content_diagnostics`：前端/API 可稳定读取最新 detail 与 summary 默认值，Python 只做只读展示适配，不执行发送、租约、OCR/VLM、文件解析或 AI。
- dashboard 已规范化 Go-owned runtime overview 的 control-plane plan/readiness detail：前端/API 可稳定读取 AgentJob capacity、AgentJob external lease readiness/plan、outbound cutover plan，Python 只做只读展示适配。
- dashboard 已规范化 Go-owned control audit detail：`/api/dashboard/runtime-overview` 稳定透传 `control_audit` card、`operator_approvals` 和 `control_mutations`，并补齐 summary 默认值；Python 仍只做展示适配，不记录 approval/mutation、不执行控制面变更。
- 已新增 repo 内原生 NapCat rich-media 对照脚本 `scripts/run-napcat-native-rich-media-smoke.ps1`：直接复现与 Go OneBot adapter 相同的 `upload_file_stream -> send_group_msg/upload_group_file` 协议，返回原始 OneBot JSON，便于把 QQ rich media 故障归类为平台/session parity 还是 Akashic adapter 偏差。
- 已完成原生 NapCat rich-media parity 验证：对 `ws://127.0.0.1:3001` 和群 `27234224` 直接执行 streamed image/file upload 后，`image_upload` 与 `file_upload` 均返回 `file_complete` 和 `file_path`，但 `send_group_msg` / `upload_group_file` 最终仍与 Akashic 一致返回 `rich media transfer failed`，说明当前 blocker 已确认为 NapCat/QQ session 平台错误，不再归因于 Go adapter。
- 已完成原生 NapCat rich-media cross-group parity 验证：在同一 NapCat WebSocket / 同一 QQ 账号下，额外对 `3219982` 与 `164369633` 两个 observe-only 群重复执行 `upload_file_stream -> send_group_msg/upload_group_file`，结果仍是 image/file staging 成功但最终统一返回 `rich media transfer failed`。结合 `27234224` 的基线结果，当前 rich-media blocker 已进一步收敛为这次 NapCat / QQ 会话的 session-wide 平台能力问题，而不是单群异常。
- 已完成 NapCat rich-media 会话刷新最小尝试：重启 `napcat` 容器后，`/v1/receiver-statuses` 已恢复到 QQ receiver `status=connected`，但重新执行 `.\scripts\run-napcat-native-rich-media-smoke.ps1 -GroupId 3219982` 后，image/file staging 仍成功、最终发送仍返回同样的 `rich media transfer failed`。这说明简单容器重启只能恢复传输连接，不能修复当前 rich-media 会话，下一步需要人工 QQ 重登或替换会话。
- 已完成第二 QQ 账号 rich-media split 验证：通过 `ws://127.0.0.1:3002` 查询到账号 `2365524513` 具有 3 个群；在 `869134328` 和 `284331268` 上运行原生 rich-media smoke 时，image 仍原生失败并返回 `rich media transfer failed`，但 file 已原生成功。随后通过 Akashic/Go adapter 对 `2365524513 -> group:284331268` 手动执行 file/image dispatch，file case 返回 `status=sent` 且最终 outbox 为 `succeeded`，image case 仍返回同样的 `platform_error/rich media transfer failed`。这说明 image 失败不是 Akashic adapter 问题并且已跨两个账号出现；第一账号的 file 失败则是 account-session-specific，而不是所有 rich-media 都坏掉。
- 已完成 dashboard/runtime state 复核：`/api/dashboard/runtime-overview` 当前能透传 `knowledge_job_planner` 已切到 `go_runtime_knowledge_job_planner` 的新 bucket 结果与 Telegram token 缺失状态；本轮 `/v1/outbound-cutover/readiness` 已保持 `execution_ready=true/execution_owner=go_local_outbox_worker`，且 execution scope 仍为 `text_only`。
- 已完成 knowledge planner 持续运行复核：`logs/agent-runtime-local.err.log` 现已连续记录多个 5 分钟 planner bucket（`19:52:25` 到 `20:12:26`），证明 Go planner 持续运行；当前观察不再依赖单次 bring-up snapshot。
- 已实现 Python compatibility outbox worker 的 Go-owner backoff：当 runtime config 报告 `workers.outbox_delivery_worker_enabled=true` 时，Python `AgentGatewayOutboxWorker` 现在直接进入 `go_runtime_outbox_worker_active` idle，不再租约任何 outbox delivery。
- repo 本地启动入口 `scripts/start-agent-runtime.ps1` 已支持透传 `AKASHIC_OUTBOX_DELIVERY_ALLOWED_KINDS`，因此 `AKASHIC_OUTBOX_DELIVERY_WORKER_ENABLED=true` + `AKASHIC_OUTBOX_DELIVERY_ALLOWED_KINDS=text` 的 text-only Go outbox cutover 已成为可复跑运行方式，而不是临时 shell 技巧。
- 已完成 text-only Go outbox owner live 验证：新的私聊文本 outbox event `qq-text-owner-resmoke:6cf419535b404c8884b53f7006bc99fe` 通过 `/v1/outbound` 直接进入状态机，并在 `/v1/outbox-events?limit=20` 中真实记录 `queued -> leased -> succeeded`，`lease_owner=agent-runtime-outbox-worker`，全程没有手工 `delivery-dispatch/send` 或手工 `POST /v1/outbox/{event_id}/succeeded`。
- 已完成 text-only rich-media gate live 验证：新的群图片 outbox event `qq-rich-gate-check:image:07cfb709113240debd3c10e7ca01b80d` 和群文件 outbox event `qq-rich-gate-check:file:45b6657cdff24c2b900db1f447c857ff` 在连续 12 秒轮询中始终保持 `status=queued`、`attempts=0`、`leased_by=null`，证明在 `outbox_allowed_kinds=["text"]` 下 rich media 不再被 Python compatibility worker 抢租或错误执行。
- 当前 `/v1/agent-worker-statuses` 已反映新的部分 cutover 事实：`akashic-python-worker:outbox` 处于 `idle`，`metadata.reason=go_runtime_outbox_worker_active`；同时 `/v1/queue-backend` 和 `/v1/outbound-cutover/readiness` 现在已达到 `outbox_execution_owner=go_local_outbox_worker`、`outbox_execution_scope=text_only`、`execution_ready=true`。
- 已实现 Go local outbox worker 的 per-account / per-kind gate：`AKASHIC_OUTBOX_DELIVERY_ALLOWED_KINDS_BY_ACCOUNT` 现在可按账号覆盖全局 `outbox_allowed_kinds`，并已接入 `/v1/runtime-config`、`/v1/queue-backend`、`/v1/runtime-workers` 诊断。
- 已完成 account-kind gate live 验证：当前 runtime 以 `AKASHIC_OUTBOX_DELIVERY_ALLOWED_KINDS=text` + `AKASHIC_OUTBOX_DELIVERY_ALLOWED_KINDS_BY_ACCOUNT=1049511700=text,2365524513=text|file` 运行，`/v1/queue-backend` 已显示 `outbox_execution_scope=account_kind_gated`。真实 smoke 中，第一账号 file outbox event `qq-account-gate:first-file:5ada75b397494cc6bfd61ed50d707084` 在连续轮询中保持 `queued/attempts=0`，而第二账号 file outbox event `qq-account-gate:second-file:8ffb2b622ed1420f833447943e8720f0` 由 Go local outbox worker 自动处理并最终 `succeeded`。
- 已将原生 NapCat rich-media 对照扩展到 private 场景：`scripts/run-napcat-native-rich-media-smoke.ps1` 现在可切换 `ConversationType=group/private`，并在 private 模式下改用 `send_private_msg` 与 `upload_private_file`。
- 已完成 native + Akashic private rich-media route 验证：`1049511700 -> 2365524513` 与 `2365524513 -> 1049511700` 的原生 private 对照都表明 image 仍返回 `rich media transfer failed`，而 file 两侧都成功；随后通过 Akashic 对第一账号私聊手动 dispatch，`qq-private-manual:file:225967834a484e74ae56d73e809b350d` 成功发送并最终 `succeeded`，`qq-private-manual:image:ee6685d0724749f1a225dec47dcaaac8` 则返回相同的 `platform_error/rich media transfer failed`。这说明 image 失败不是 group-only，而第一账号 file 失败也不是 account-wide，而是当前 group route 特有问题。
- 已实现 Go local outbox worker 的 per-account / per-conversation-type / per-kind gate：`AKASHIC_OUTBOX_DELIVERY_ALLOWED_KINDS_BY_ACCOUNT_CONVERSATION_TYPE` 现在可按 `account/conversation_type` 覆盖 account-only 与 global allowed kinds，并已接入 `/v1/runtime-config`、`/v1/queue-backend`、`/v1/runtime-workers` 诊断。
- 已完成 account-conversation-kind gate live 验证：当前 runtime 以 `AKASHIC_OUTBOX_DELIVERY_ALLOWED_KINDS=text`、`AKASHIC_OUTBOX_DELIVERY_ALLOWED_KINDS_BY_ACCOUNT=2365524513=text|file`、`AKASHIC_OUTBOX_DELIVERY_ALLOWED_KINDS_BY_ACCOUNT_CONVERSATION_TYPE=1049511700/private=text|file` 运行，`/v1/queue-backend` 已显示 `outbox_execution_scope=account_conversation_kind_gated`。真实 outbox smoke 中，第一账号 private file 事件 `qq-account-conv-gate:first-private-file:c65f6cb5bcbe4e589fdcdc396194c66b` 由 Go local outbox worker 自动处理并最终 `succeeded`；第一账号 group file 事件 `qq-account-conv-gate:first-group-file:fac830d6a62847a997b2521e899b259d` 在连续轮询中保持 `queued/attempts=0`；第二账号 group file 事件 `qq-account-conv-gate:second-group-file:e2681d140af248dd91b6191412b201bc` 则继续自动成功。当前 Go default owner 已可安全覆盖“全局 text + 第二账号 file + 第一账号 private file”。
- 已新增 repo 内 `knowledge planner` 持续运行验证脚本 `scripts/verify-knowledge-planner-cutover.ps1`：该脚本会同时读取 `/v1/runtime-config`、`/v1/runtime-workers`、`/v1/knowledge-job-planner/readiness`、`/v1/knowledge-job-planner/cutover-plan`、`/v1/jobs?type=group_memory_extract`，以及 `logs/agent-runtime-local.err.log`、`logs/akashic-local-run.err.log`，以 JSON 汇总 live cutover 证据。
- 已用 `.\scripts\verify-knowledge-planner-cutover.ps1` 完成当前 live 验证：当前 runtime 显示 `knowledge_job_planner_enabled=true`、planner worker running、`current_admission_owner=go_runtime_knowledge_job_planner`；runtime log 已连续记录 bucket `5934695`、`5934696`、`5934697`；最近 `group_memory_extract` jobs 都带 `scheduler=agent-runtime-knowledge-job-planner` 且 `status=succeeded`；Python log 继续出现 `skip legacy enqueue`，并继续处理 Go 创建的 `5934696` / `5934697` jobs。该结论证明当前知识任务 admission 仍由 Go 持有，Python 只 lease/execute。
- 已新增 repo 内 `Telegram backend` 验证脚本 `scripts/verify-telegram-backend.ps1`：该脚本会同时读取 `/v1/runtime-config`、`/v1/receiver-statuses`，并在本地或显式提供 token 时尝试 Telegram `getMe`，以 JSON 区分 `token_missing`、`getme_ok`、`getme_failed` 三类状态。
- 已用 `.\scripts\verify-telegram-backend.ps1` 完成当前 live 验证：当前 runtime 仍显示 `telegram_token_configured=false`，`/v1/receiver-statuses` 仍没有 Telegram receiver，本地环境也不存在 `TELEGRAM_BOT_TOKEN` / `AKASHIC_TELEGRAM_BOT_TOKEN`，因此脚本返回 `backend_state=token_missing`、`getme_attempted=false`。这把 Telegram blocker 明确收敛为 token 缺失，而不是已配置 receiver 或隐藏的 `getMe` 失败。
- 已新增 repo 内统一 goal 验证脚本 `scripts/verify-go-migration-goal.ps1`：该脚本会一次性聚合 `/healthz`、`/v1/runtime-config`、`/v1/runtime-workers`、`/v1/runtime-overview`、`/v1/outbound-cutover/readiness`、`/v1/queue-backend`、`/v1/receiver-statuses`、`/v1/jobs?type=group_memory_extract`、`/v1/agent-job-external-lease/readiness`、`/v1/agent-job-external-lease/plan`、`/v1/agent-job-capacity/plan`、`/v1/agent-job-priority/plan`，并复用 `verify-knowledge-planner-cutover.ps1` 与 `verify-telegram-backend.ps1`，输出统一 JSON 供每轮真实更新 goal 使用。
- 已用 `.\scripts\verify-go-migration-goal.ps1 -IncludeNativeRichMediaProbe -RichMediaProbeGroupId 3219982` 完成本轮 live 复核：当前仍显示 `outbox_execution_scope=account_conversation_kind_gated`、Telegram `backend_state=token_missing`、knowledge planner `conclusion=go_admission_owner_healthy`，并且第一账号 `3219982` group 的原生 NapCat rich-media probe 仍然出现 `image_send.retcode=1200` 与 `file_send.retcode=1200`、错误均为 `rich media transfer failed`。这让本轮 goal 更新不再依赖手工拼接多份证据。
- 已新增 repo 内 Go outbox scope live 验证脚本 `scripts/verify-go-outbox-scope-live.ps1`：该脚本会创建真实 outbox 事件，验证当前已放开的 `first-account private file` 与 `second-account group file` 仍由 Go local outbox worker 自动成功，同时验证 `first-account group file` 与 `first-account private image` 仍保持 `queued/attempts=0`。
- 已用 `.\scripts\verify-go-outbox-scope-live.ps1` 完成本轮 live 复核：`first_account_private_file` 与 `second_account_group_file` 都真实达到 `succeeded`；`first_account_group_file_gated` 与 `first_account_private_image_gated` 都真实保持 `queued/attempts=0`。随后又用 `.\scripts\verify-go-migration-goal.ps1 -IncludeOutboxScopeSmoke -IncludeNativeRichMediaProbe -RichMediaProbeGroupId 3219982` 证明统一 goal 脚本现在可以在显式开关下同时输出 partial outbox cutover 的真实成功/拦截边界与当前 rich-media 平台 blocker。
- 已修复 OneBot WebSocket 失败响应里的结构化 `status` 解析：`onebotResponse.Status` 现在可接受非字符串 JSON，不会再因为 NapCat 返回对象型 `status` 而在 JSON 反序列化阶段直接打断 Go outbox worker 的错误分类。对应 `go test ./infrastructure/onebotdelivery` 已通过。
- 已修复 repo 本地 launcher 无法显式启用 outbox worker 的问题：`scripts/start-agent-runtime.ps1` 新增 `-EnableOutboxDeliveryWorker`，现在可以稳定重启出 `outbox_delivery_worker.enabled=true` 的本地 runtime，而不依赖外部 shell 预先带环境变量。
- 已修复 repo 本地 launcher 缺失 `AKASHIC_DELIVERY_CHANNEL_BY_ACCOUNT` 的问题：`scripts/start-agent-runtime.ps1` 现在默认导出 `1049511700=qq_1049511700,2365524513=qq_2365524513`，避免第二账号 outbox work 误走主账号 `qq` alias。
- 已增强 Telegram verifier 的配置来源证据：`.\scripts\verify-telegram-backend.ps1 -RepoRoot E:\agent\my-akashic_agent` 现在不仅确认 `backend_state=token_missing`，还会明确返回 `config_toml_exists=true`、`config_declares_telegram_channel=true`、`config_token_line=${TELEGRAM_BOT_TOKEN}`、`config_uses_env_placeholder=true`、`repo_env_has_telegram_key=false`、`codex_env_has_telegram_key=false`。这证明当前 Telegram 不是通道未声明，而是 token 注入缺失。
- 已在修复结构化 `status` 解析并用 `-EnableOutboxDeliveryWorker` 重启 runtime 后，再次运行 `.\scripts\verify-go-outbox-scope-live.ps1` 与 `.\scripts\verify-go-migration-goal.ps1 -IncludeOutboxScopeSmoke -IncludeNativeRichMediaProbe -RichMediaProbeGroupId 3219982`：当前不会再因 `json: cannot unmarshal object into Go struct field onebotResponse.status of type string` 误报失败，统一 goal 结论稳定保持 `partial_go_default_owner_live_verified`。
- 已在补上 `AKASHIC_DELIVERY_CHANNEL_BY_ACCOUNT` 后再次重启 runtime，并重新验证 repo-owned live smoke：`.\scripts\verify-go-outbox-scope-live.ps1` 现已稳定恢复到 `first_account_private_file=succeeded`、`second_account_group_file=succeeded`、`first_account_group_file_gated=queued/attempts=0`、`first_account_private_image_gated=queued/attempts=0`；`.\scripts\verify-go-migration-goal.ps1 -IncludeOutboxScopeSmoke -IncludeNativeRichMediaProbe -RichMediaProbeGroupId 3219982` 也已去掉伪造的 `outbox_scope_case_failed_second_account_group_file` blocker。
- 已把 observe-only 群“不回群消息”升级为 Go runtime 硬约束：delivery dispatch planning 现在会读取同步后的 observe targets，并对任何 exact-match 的 `enabled=true` + `reply_allowed=false` QQ group route 直接返回稳定 route error `observe-only target does not allow replies`。live 验证中，`qq:1049511700:group:27234224` 的 `/v1/delivery-dispatch/readiness` 已真实返回 `HTTP 400 + route_error`，而 `qq:1049511700:private:2365524513` 的 readiness 仍返回 `ready=true`。这保证了观察群即使未来误生成 outbox 事件，也不会在任何时刻向群内回复消息。
- 已把 observe-only 静默检查并入统一 goal verifier：`scripts/verify-go-migration-goal.ps1` 现在会默认读取 `/v1/observe-targets`，对一个真实 observe-only QQ group route 做 readiness 阻断验证，并额外确认 QQ private route 仍 `ready=true`。如果任一条件失效，脚本会把 `observe_only_targets_missing`、`observe_only_group_reply_block_not_enforced` 或 `observe_only_private_route_regressed` 直接写进 `checks.open_blockers`，避免每轮 goal 更新再靠人工补充这条安全边界。
- 已运行默认 `.\scripts\verify-go-migration-goal.ps1` 完成 live 复核：当前 `observe_only_silence.checks` 已真实返回 `enabled_reply_disabled_group_targets_present=true`、`observe_only_group_route_blocked=true`、`private_route_still_ready=true`；默认 `checks.open_blockers` 当前只剩 `telegram_token_missing` 和 `qq_image_native_platform_blocker_unresolved`，说明观察群静默约束已被纳入统一 goal 更新路径并保持通过。
- 已把 QQ 群发送从“只阻断 observe-only 群回复”提升为“可手动开关的全局 QQ group 策略”：Go runtime 新增 `AKASHIC_QQ_GROUP_SEND_ENABLED=true|false`，Python QQ channel 新增 `group_send_enabled=true|false`；当前本地默认值已经切到 `false`。live 验证中，`GET /v1/runtime-config` 已真实返回 `delivery.qq_group_send_enabled=false`，`.\scripts\verify-go-outbox-scope-live.ps1` 证明第一账号 `group text` 与第二账号 `group file` 都被 `qq group sends are disabled` 稳定阻断，而第一账号 `private file` 仍可由 Go local outbox worker 自动成功。`.\scripts\verify-go-migration-goal.ps1` 现在也默认把这条策略纳入 `observe_only_silence` 检查，并输出 `qq_group_send_toggle_disabled=true`、`qq_group_route_blocked=true`、`qq_group_route_blocked_by_global_policy=true`、`private_route_still_ready=true`。

## SDD / 迭代治理

- 已补充 Python AI runtime 职责边界：模型/provider、prompt/context、工具执行、Memory/RAG 算法、chunking、retrieval、Embedding/Rerank/OCR/VLM、图片生成执行、群知识沉淀、评估和快速实验继续归 Python；确定性控制面状态归 Go。
- 已沉淀 `docs/sdd/ITERATION_PROMPT.md`，明确每轮迭代必须完成 `TODO.md` 中所有未完成项，不能只提交一个未闭环切片；用户在迭代中追加的本轮要求也必须先写入 TODO 并一起闭环。
- 已新增 SDD spec index guard：`tests/test_sdd_spec_index.py` 会要求 `docs/sdd/specs/agent-gateway/000-index.md` 逐名引用所有 agent-gateway spec 文件，并补齐当前缺失索引，防止 Go 化迁移设计记录漂移。
- 已增强 Go runtime 架构守卫：`services/agent-runtime/architecture_test.go` 现在同时约束源码只能落在 `api/app/cmd/domain/infrastructure/smoke/trigger/types` 顶层根目录，防止新增随意 Go package 破坏 DDD + 六边形结构。
- 已新增 `docs/sdd/OPEN_ISSUES.md` 未解决问题总账，并用 `tests/test_sdd_governance_docs.py` 固化 TODO/DONE/BACKLOG/LIVE_CHECKS/OPEN_ISSUES 的职责边界，防止待解决问题继续堆进本轮 TODO。
- `OPEN_ISSUES.md` 已增加结构化质量守卫：`tests/test_sdd_governance_docs.py` 会校验每条 open issue 都有唯一 `OI-###`、领域、问题、影响、下一步和合法状态，避免未解决问题总账退化成散乱备注。
- 已把“每轮必须基于当前 live 证据更新 goal”继续固化成 repo-owned 入口：`scripts/verify-go-migration-goal.ps1` 现在会在启用 `-IncludeOutboxScopeSmoke` 时把 scope smoke 失败直接反映到 `open_blockers`，避免回到手工拼接结论的状态。
- 已把统一 goal verifier 中的 `scheduler/proactive/dashboard_fallback/agent_job_external_lease_result_ack` 从静态 `not_current_turn` 占位改成当前 turn 的 live 取证：`.\scripts\verify-go-migration-goal.ps1` 现在会真实调用 `/v1/scheduler/jobs`、`/v1/scheduler/leases`、`/v1/scheduler/diagnostics`、`/v1/proactive/tick-logs`、`/v1/proactive/drift/summary`、`/v1/proactive/anyaction/quota`、`/v1/proactive/bg-context/main/last`、`/v1/proactive/context-only/last` 以及 dashboard `/api/dashboard/proactive/tick_logs`，并额外调用 `.\scripts\verify-scheduler-runtime-live-smoke.ps1` 对 scheduler 写路径与 recovery 做隔离 temp-runtime smoke。
- 已完成新的 unified goal live 结论：当前 scheduler 已被证明确认为 `go_control_plane_mutation_and_recovery_live_verified`（隔离 temp runtime 已真实验证 `scheduler-jobs.json` CRUD、lease-fenced complete mutation 和 Python recovery reconciliation），proactive 已被证明确认为 `go_state_live_verified_with_dashboard_read_model`（runtime tick logs、anyaction quota、dashboard tick-log read model 都有当前证据），dashboard fallback 也不再是纯 follow-up 文案。
- 已新增 `scripts/verify_scheduler_runtime_live_smoke.py`、`scripts/verify-scheduler-runtime-live-smoke.ps1` 和 `tests/test_verify_scheduler_runtime_live_smoke.py`：该切片用 isolated temp runtime 替代对当前 `127.0.0.1:8780` 的直接 mutation，验证范围覆盖 scheduler CRUD persistence、completion mutation、startup recovery reconciliation，且整个 smoke 不依赖 QQ/Telegram 发送。
- 已新增 repo-owned `agent_job external lease result-ack` live verifier：`.\scripts\verify-agent-job-external-lease.ps1` 现在会同时聚合 `/v1/agent-job-external-lease/readiness`、`/v1/agent-job-external-lease/plan`、`/v1/runtime-config`、`/v1/queue-backend`、`/v1/queue-topology`、`/v1/runtime-overview`、`/v1/agent-worker-statuses`，并把 blocker 拆成 `configuration`、`execution_owner`、`smoke`、`worker_coverage` 四类。
- 已用 `.\scripts\verify-agent-job-external-lease.ps1` 完成当前 live 验证：当前 `conclusion.status=live_verified`、`current_scope=state_store_lease`、`category=blocked_by_configuration_and_cutover_flags`。这证明当前不是 worker coverage 卡住，而是本机仍保持 `provider=local`、未配置 external queue / NATS cutover flags、`AKASHIC_AGENT_JOB_STRICT_LEASE_TOKEN=false`、`AKASHIC_QUEUE_EXTERNAL_LEASE_AGENT_JOB_ENABLED=false`，同时 duplicate/flow smoke flags 也未注入 runtime。
- `.\scripts\verify-go-migration-goal.ps1` 现已复用新的 `verify-agent-job-external-lease.ps1` 输出，不再只给 `agent_job external lease result-ack` 的简写摘要；统一 goal 结果现在能直接显示当前 owner、queue topology、selected provider capability 和 blocker bucket。
- 已新增 repo-owned `media recovery` live verifier：`.\scripts\verify-media-recovery-boundary.ps1` 会同时聚合 `/v1/runtime-overview`、`/v1/media-assets/content-diagnostics`、`/v1/media-assets`、sample asset 的 access/recovery/preflight，以及 dashboard `/api/dashboard/runtime-overview` 和 `/api/dashboard/media-assets/content-recovery-plan`，把 `OI-005` 从人工口头描述收敛成当前 turn 的稳定 JSON 证据。
- 已用 `.\scripts\verify-media-recovery-boundary.ps1` 完成当前 live 验证：当前 sampled assets 全为 `file://` observe-only QQ group 上传镜像，`content_diagnostics` 为 `forbidden=120/120`，sample recovery plan 为 `media_asset_content_recovery_fix_content_roots`，preflight 为 `missing_approval_id/executor_scope=operator_runtime_config`，且 `has_http_https_assets=false`。同时 dashboard recovery-plan proxy 正常，但 `/api/dashboard/runtime-overview` 仍未透出 `media_asset_content_recovery` card/detail。这说明当前活跃 gap 是 operator content roots / dashboard read-model 收尾，而不是 Go 完全没有 media recovery control plane。
- 已修复 dashboard `/api/dashboard/runtime-overview` 的 `media_asset_content_recovery` read-model gap：`plugins/runtime_overview/dashboard.py` 现在会透传 top-level detail、补齐 summary 默认值，并在 Go runtime `cards` 缺失时合成 `Media Content Recovery` card；同时对 `/v1/runtime-overview` 使用更现实的专用 timeout，避免本机 live runtime 因默认 `0.5s` timeout 退回 fallback 而把 detail 降成 `unknown/muted`。
- 本轮 live 复核已确认 dashboard gap 消失：`GET /api/dashboard/runtime-overview` 当前返回 `media_asset_content_recovery` top-level detail 和 `Media Content Recovery` card，`detail_reason=media_asset_content_recovery_audit_ready`，`partial=false`；`.\scripts\verify-media-recovery-boundary.ps1` 现在返回 `runtime_overview_has_media_content_recovery_card=true`、`runtime_overview_has_media_content_recovery_detail=true`、`dashboard_runtime_overview_missing_media_recovery_detail=false`。
- 已新增 repo-owned `HTTP/HTTPS media recovery executor` isolated live smoke：`.\scripts\verify-media-asset-content-recovery-live-smoke.ps1` 会拉起 temp HTTP source 与 temp `agent-runtime`，真实验证 `missing_approval_id` preflight、active approval 后 ready preflight、dry-run 不写 cache、不下载远端内容、正式 recovery 写入本地 cache 并更新 media registry，以及 `/v1/control-mutations` applied audit 落账。
- 已用 `.\scripts\verify-media-asset-content-recovery-live-smoke.ps1` 完成当前 live 验证：当前真实返回 `conclusion.category=go_http_https_media_recovery_executor_live_verified`，并固定证明 `remote_source_only_downloaded_on_live_recovery=true`、`live_recovery_writes_cache_and_updates_registry=true`、`content_endpoint_reads_recovered_bytes=true`、`control_mutation_audit_recorded=true`。这说明普通 HTTP/HTTPS recovery executor 已有 repo-owned current-turn evidence，剩余 open gap 已收敛到平台私有源抓取、后台重拉和恢复后 AI enrichment。
- 已新增 repo-owned `MQ adapter boundary` live verifier：`.\scripts\verify-mq-adapter-boundary.ps1` 会同时聚合 `/v1/queue-backend`、`/v1/queue-topology`、`/v1/runtime-overview` 和 dashboard `/api/dashboard/runtime-overview`，把当前 selected provider、推荐外部 MQ、planned-only adapter 边界和 dashboard read-model 一次性收敛成稳定 JSON 证据。
- 已用 `.\scripts\verify-mq-adapter-boundary.ps1` 完成当前 live 验证：当前 `conclusion.status=live_verified`，并且已真实证明 `selected_provider_visible=true`、`nats_is_recommended_first_backend=true`、`redis_and_rabbit_are_planned_only=true`、`dashboard_preserves_provider_capabilities=true`。这说明 MQ adapter 的剩余问题已经收敛为“未来如有部署需求再实现 Redis/Rabbit adapter”，而不是 dashboard 读模型缺口。
- 已把 runtime overview 中 `agent_job_external_lease_readiness` 与 `agent_job_external_lease_plan` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `Current Owner / Desired Owner / Recommended Owner`、worker coverage、blockers、notes，以及 `Required Checks / Enable Steps / Verification Steps / Rollback Steps`。该渲染只消费现有 Go detail，不修改 env、不执行 cutover、不 ack/nack MQ、不创建/执行 AgentJob，也不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把上述 dashboard panel 证据并入 `dashboard_fallback` 当前 turn 分类，不再只依赖 proactive tick-log read-model；当前结论已扩大为 runtime-overview 的 `agent_job external lease` structured read-model 也在 live runtime 中可见。
- 已把 runtime overview 中 `agent_job_capacity_plan`、`agent_job_priority_plan`、`knowledge_job_planner_cutover_plan` 与 `outbound_cutover_plan` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 capacity/priority items、knowledge planner owner/preview bucket/outbound owner/expected OneBot channels，以及对应 verification / required / enable / rollback steps。该渲染继续只消费 Go detail，不修改 env、不启动 worker、不发送 QQ/Telegram、不 ack/nack MQ、不创建/执行 AgentJob，也不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 capacity/priority/knowledge-cutover/outbound-cutover 的 dashboard panel 证据并入 `dashboard_read_models`，并把 `dashboard_fallback.category` 升级为 `live_verified_runtime_read_models`。这说明当前 dashboard 这条线上，plan/readiness 类 control-plane drilldown 已不再是主要 gap。
- 已把 runtime overview 中 `delivery_smoke_readiness` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 overall `ready/reason`、`cases/ready/not_ready` totals，以及每个 smoke case 的 `name/channel/chat_id/conversation_type/kind/step_count`；渲染只消费 Go detail，不发送 QQ/Telegram、不创建 outbox、不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `delivery_smoke_readiness` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `queue_backend` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `provider/mode/migration_phase`、`outbox_execution_owner/outbox_execution_scope`、`agent_job_execution_owner`、`recommended_first_backend` 和 provider capability matrix；渲染只消费 Go detail，不连接 MQ、不 publish/lease/ack/nack/term、不发送 QQ/Telegram、不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `queue_backend` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、queue backend、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `receiver_statuses` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `receivers/connected/qq/telegram/suspended/failed` totals，以及每个 receiver 的 `receiver_id/kind/channel/account/status/reason/endpoint/updated_at`；渲染只消费 Go detail，不启动/停止/重连 receiver，不发送 QQ/Telegram，不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `receiver_statuses` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、receiver statuses、queue backend、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `receiver_leases` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `leases/active/expired/qq/telegram` totals，以及每个 lease 的 `receiver_id/kind/account_id/owner_instance_id/status/expires_at/updated_at`；渲染只消费 Go detail，不执行 cleanup、不修改 lease、不启动/停止 receiver、不发送 QQ/Telegram、不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `receiver_leases` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、receiver statuses、receiver leases、queue backend、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `knowledge_pipelines` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `targets/enabled/ready/warning/blocked/lagging/stale_checkpoints/receiver_connected` totals，以及每个 pipeline 的 `target_id/account/capture_status/group_memory/rag_ingest/coverage/latest_source_seq/reasons`；渲染只消费 Go detail，不创建 checkpoint、不创建/租约/执行 AgentJob、不发送 QQ/Telegram、不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `knowledge_pipelines` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、receiver statuses、receiver leases、knowledge pipelines、queue backend、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `external_lease_diagnostics` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `provider/mode/external_queue_configured/external_queue_active/consumer_concurrency/max_in_flight/outbox_execution_owner/agent_job_execution_owner`，以及 selected provider 的 `consumer_model/adapter_boundary/recommended_first_backend/supports_external_lease/supports_agent_job_result_ack/blockers`；渲染只消费 Go detail，不修改 MQ、不 publish/lease/ack/nack/term，不创建/执行 AgentJob 或 outbox delivery，不触发 QQ/Telegram 或 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `external_lease_diagnostics` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、receiver statuses、receiver leases、knowledge pipelines、queue backend、external lease diagnostics、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `scheduler_jobs` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `sampled/enabled/disabled/overdue/due_soon/instant/soft/due_soon_seconds`、trigger/tier/status 分布，以及 recent sample jobs 的 `job_id/trigger/tier/status/channel/next_run/reason`；渲染只消费 Go detail，不创建/修改/删除 scheduler job，不执行 complete/recovery，不发送 QQ/Telegram，不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `scheduler_jobs` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、receiver statuses、receiver leases、knowledge pipelines、scheduler jobs、queue backend、external lease diagnostics、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已新增 repo-owned `agent_job external lease result-ack` temp NATS smoke verifier：
  `.\scripts\verify-agent-job-external-lease-nats-smoke.ps1` 现在会自动拉起
  临时 `nats:2-alpine -js`、执行
  `TestExternalLeaseNATSSmokeAgentJobDuplicateTerminalAck` 与
  `TestExternalLeaseNATSSmokeAgentJobPendingRunningSucceededFlow`，并输出
  Docker / Go test / selected test / conclusion 的结构化 JSON。
- `services/agent-runtime/smoke/external_lease_nats_smoke_test.go` 已移除固定
  `2026-05-31` smoke 时间，改为当前 UTC 时间，避免
  `pending/running/succeeded` flow smoke 因日历推进把自身 lease 判成过期。
- `.\scripts\verify-go-migration-goal.ps1` 现已把新的 temp NATS smoke 证据并入
  `agent_job_external_lease_nats_smoke`，并新增
  `residual_classification.agent_job_external_lease_result_ack_smoke` 复用同一
  结论；这条证据证明 repo-owned Go result-ack smoke 路径可重复运行，但不改变
  当前长期运行 runtime 的 owner、flags 或 cutover 状态。
- 已新增 repo-owned isolated `agent_job external lease result-ack` cutover
  preflight verifier：`.\scripts\verify-agent-job-external-lease-cutover-preflight.ps1`
  现在会同时启动 `config_blocked` temp runtime 和 `preflight_ready` temp
  NATS/runtime 场景，固定证明两件事：
  1) 无 external-lease flags 时当前 blocked 语义仍是 local provider +
  `python_ai_worker_state_store_lease`；
  2) 满足 NATS、explicit result-ack flags、active `knowledge` worker coverage
  后，`agent_job_execution_owner`、queue topology `ack_owner`、readiness 和
  plan 会一起翻到 ready。
- `.\scripts\verify-go-migration-goal.ps1` 现已把上述 isolated preflight 证据并入
  `agent_job_external_lease_cutover_preflight` 与
  `residual_classification.agent_job_external_lease_cutover_preflight`；这条证据
  证明 repo-owned preflight 语义已 live verified，但不等于生产 runtime 已切到
  external lease。
- 已把 runtime overview 中 `agent_workers` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `workers/idle/running/stopped/stale/failed/knowledge/outbox_delivery`，以及每个 worker 的 `worker_id/worker_type/status/lease_active/stale/processed_total/failed_total/reason`；渲染只消费 Go detail，不启动/停止/替换 worker，不修改 lease，不发送 QQ/Telegram，不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `agent_workers` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、receiver statuses、receiver leases、knowledge pipelines、scheduler jobs、agent workers、queue backend、external lease diagnostics、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `runtime_workers` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `workers/enabled/disabled/running`，以及每个 Go worker 的 `name/kind/enabled/running/worker_id/interval_or_concurrency/batch_or_max_in_flight/notes`；渲染只消费 Go detail，不启动/停止/重启 worker，不修改 queue flags，不发送 QQ/Telegram，不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `runtime_workers` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、receiver statuses、receiver leases、knowledge pipelines、scheduler jobs、agent workers、runtime workers、queue backend、external lease diagnostics、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `agent_job_worker_coverage` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `job_type/expected_worker_types/coverage_status/worker_count/active/running/failed/stale/high_pressure/reason`；渲染只消费 Go detail，不启动/停止/替换 worker，不创建/租约/执行 AgentJob，不发送 QQ/Telegram，不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `agent_job_worker_coverage` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、receiver statuses、receiver leases、knowledge pipelines、scheduler jobs、agent workers、runtime workers、agent-job worker coverage、queue backend、external lease diagnostics、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `agent_job_pressure` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `job_types/high_pressure/max_pending/max_active/oldest_pending_age_seconds`、throughput 的 `created/succeeded`、dead letters current total，以及 `pressure.by_type` 的 `job_type/pending/leased/running/active/oldest_pending_age_seconds/high_pressure`；渲染只消费 Go detail，不创建/租约/执行 AgentJob，不修改 queue 参数，不发送 QQ/Telegram，不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `agent_job_pressure` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、receiver statuses、receiver leases、knowledge pipelines、scheduler jobs、agent workers、runtime workers、agent-job worker coverage、agent-job pressure、queue backend、external lease diagnostics、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `outbox_pressure` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `accounts/high_pressure_accounts/max_queued/max_active`、throughput 的 `queued/leased/succeeded`、dead letters current total，以及 `pressure.by_account` 的 `account_key/channel_kind/account_id/queued/dispatching/active/dead_lettered/high_pressure/pressure_reason`；渲染只消费 Go detail，不创建/租约/执行 outbox delivery，不修改 queue/backpressure 参数，不发送 QQ/Telegram，不触发 Python AI。
- runtime overview dashboard reader 已补齐 `outbox_pressure` 顶层透传，`.\scripts\verify-go-migration-goal.ps1` 现已把 `outbox_pressure` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、receiver statuses、receiver leases、knowledge pipelines、scheduler jobs、agent workers、runtime workers、agent-job worker coverage、agent-job pressure、outbox pressure、queue backend、external lease diagnostics、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `outbox_metrics` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 throughput 的 `queued/leased/dispatching/succeeded/failed/dead_lettered/terminal_events`、dead letters current total、`deliveries_by_channel_kind`，以及 recent dead-letter rows 的 `delivery_id/channel_kind/event_type/status/error_kind/error_message/attempt/occurred_at`；渲染只消费 Go detail，不创建/租约/执行 outbox delivery，不修改 queue/backpressure 参数，不发送 QQ/Telegram，不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `outbox_metrics` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、receiver statuses、receiver leases、knowledge pipelines、scheduler jobs、agent workers、runtime workers、agent-job worker coverage、agent-job pressure、outbox metrics、outbox pressure、queue backend、external lease diagnostics、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `send_ledger_metrics` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `sampled_records/unique_bots/unique_conversations/unique_content_hashes/repeated_content_hashes`，以及 `repeated_hashes` 与 `recent` 的表格；渲染只消费 Go detail，不创建/租约/执行 outbox delivery，不修改 send ledger / loop guard 状态，不发送 QQ/Telegram，不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `send_ledger_metrics` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、receiver statuses、receiver leases、knowledge pipelines、scheduler jobs、agent workers、runtime workers、agent-job worker coverage、agent-job pressure、send ledger metrics、outbox metrics、outbox pressure、queue backend、external lease diagnostics、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `inbox_metrics` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `sampled_events/observe_only_total/reply_eligible_total/with_attachments/attachment_count/unique_senders`、`events_by_channel_kind`、conversation 分布，以及 recent inbox event 表格；渲染只消费 Go detail，不修改 observe/inbox 状态，不发送 QQ/Telegram，不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `inbox_metrics` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、receiver statuses、receiver leases、knowledge pipelines、scheduler jobs、agent workers、runtime workers、agent-job worker coverage、agent-job pressure、send ledger metrics、inbox metrics、outbox metrics、outbox pressure、queue backend、external lease diagnostics、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `inbound_dedupe_metrics` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `sampled_records/active_records/expired_records/duplicate_records/seen_total/duplicate_seen_total`、scope 表格和 notes；渲染只消费 Go detail，不修改 dedupe 记录，不发送 QQ/Telegram，不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `inbound_dedupe_metrics` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、receiver statuses、receiver leases、knowledge pipelines、scheduler jobs、agent workers、runtime workers、agent-job worker coverage、agent-job pressure、send ledger metrics、inbox metrics、inbound dedupe metrics、outbox metrics、outbox pressure、queue backend、external lease diagnostics、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `observe_targets` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `targets/enabled/observe_only/reply_allowed/groups/qq`、target 表格和 notes；渲染只消费 Go detail，不修改 observe target 配置，不发送 QQ/Telegram，不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `observe_targets` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、receiver statuses、receiver leases、knowledge pipelines、observe targets、scheduler jobs、agent workers、runtime workers、agent-job worker coverage、agent-job pressure、send ledger metrics、inbox metrics、inbound dedupe metrics、outbox metrics、outbox pressure、queue backend、external lease diagnostics、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `observe_capture` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `targets/ready/warning/blocked/image_covered/file_covered/content_ready_assets/receiver_activity_recent`、target 表格和 notes；渲染只消费 Go detail，不修改 observe capture / media 状态，不发送 QQ/Telegram，不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `observe_capture` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、receiver statuses、receiver leases、knowledge pipelines、observe targets、observe capture、scheduler jobs、agent workers、runtime workers、agent-job worker coverage、agent-job pressure、send ledger metrics、inbox metrics、inbound dedupe metrics、outbox metrics、outbox pressure、queue backend、external lease diagnostics、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `delivery_adapters` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `provider/channel/transport/enabled/endpoint_configured/access_token_configured/endpoint`，并保留 `Adapter Health` 与 `Delivery Smoke` 的只读入口；渲染只消费 Go detail，不修改 adapter 配置，不发送 QQ/Telegram，不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `delivery_adapters` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、delivery adapters、receiver statuses、receiver leases、knowledge pipelines、observe targets、observe capture、scheduler jobs、agent workers、runtime workers、agent-job worker coverage、agent-job pressure、send ledger metrics、inbox metrics、inbound dedupe metrics、outbox metrics、outbox pressure、queue backend、external lease diagnostics、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `agent_job_metrics` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `sampled_jobs/sampled_events/job_types/created/leased/running/succeeded/dead_letters_current`，以及 `jobs_by_type`、`pressure.by_type`、`dead_letters.recent` 三个结构化表格；渲染只消费 Go detail，不创建/租约/执行 AgentJob，不修改 queue owner/worker，不发送 QQ/Telegram，不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `agent_job_metrics` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 proactive tick logs、delivery adapters、receiver statuses、receiver leases、knowledge pipelines、observe targets、observe capture、scheduler jobs、agent workers、runtime workers、agent-job worker coverage、agent-job metrics、agent-job pressure、send ledger metrics、inbox metrics、inbound dedupe metrics、outbox metrics、outbox pressure、queue backend、external lease diagnostics、delivery smoke、external lease、capacity/priority、knowledge cutover 与 outbound cutover 等主要 runtime-overview read-model。
- 已把 runtime overview 中 `worker_leases` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `jobs/checkpoints/leaseable jobs/stale leases/workers/stale after`，以及每个 lease diagnostics 的 `job_type/checkpoint_prefix/status_counts/stale_lease_count/leaseable_count/recent_jobs/latest_job/latest_updated`；渲染只消费 Go detail，不修改 lease、不执行 cleanup/takeover、不发送 QQ/Telegram、不触发 Python AI。
- dashboard reader 现已在旧 Go overview payload 缺失 `worker_leases` card 时追加 fallback card，`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.worker_leases_table=true` 纳入当前 turn 取证。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 worker leases 在内的主要 runtime-overview read-model。
- 已把 runtime overview 中 `runtime_config` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `QQ Group Send`、`Telegram Token`、`OneBot Endpoints`、`Expected Channels`、`Strict Lease Token`、`AgentJob External Lease`，以及 runtime / worker flags / environment keys 表格；渲染只消费 Go detail，不修改 runtime 配置、不补 token/endpoint、不发送 QQ/Telegram、不触发 Python AI。
- dashboard reader 现已在旧 Go overview payload 缺失 `runtime_config` card 时追加 fallback card，并把 fallback 地址规范成 `host:port`；`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.runtime_config_table=true` 纳入当前 turn 取证。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 runtime config 在内的主要 runtime-overview read-model。
- 已把 runtime overview 中 `knowledge_job_planner_preview` 与 `knowledge_job_planner_readiness` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 preview 的 `targets/skipped/total_jobs/group_memory_jobs/rag_ingest_jobs/interval`、plan 级 `observe_only/scheduler` 信息，以及 readiness 的 `planner enabled/running`、knowledge worker `ready/active/stale/failed/stopped` 与 preview bucket/timestamp；渲染只消费 Go detail，不创建 AgentJob、不切换 owner、不启动/停止 planner 或 Python knowledge worker。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `knowledge_job_planner_preview` 与 `knowledge_job_planner_readiness` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 knowledge planner preview/readiness 在内的主要 runtime-overview read-model。
- 已把 runtime overview 中 `runtime_health` 与 `stale_jobs` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 runtime health snapshot / health errors，以及 stale-job worker diagnostics / sampled stale jobs；渲染只消费 Go detail，不执行 runtime recovery、不执行 stale lease cleanup/takeover/retry、不修改 worker 或 job 状态、不发送 QQ/Telegram、不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `runtime_health` 与 `stale_jobs` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 runtime health / stale jobs 在内的主要 runtime-overview read-model，剩余 raw-JSON-only gap 已缩到 `job_events`、`outbox_events`、`rag_eval_failures`。
- 已把 runtime overview 中 `job_events` 与 `outbox_events` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 event-type totals、recent job/outbox event rows，并兼容旧 `recent_events` 形状与当前 live metrics-nested detail；渲染只消费 Go detail，不创建/租约/重试 AgentJob，不 dispatch/cleanup/retry outbox，不修改 worker、lease、dead-letter 或 event store 状态，不发送 QQ/Telegram，不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `job_events` 与 `outbox_events` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖 job events / outbox events 在内的主要 runtime-overview read-model，剩余 raw-JSON-only gap 已缩到 `rag_eval_failures`。
- 已把 runtime overview 中 `rag_eval_failures` 的 dashboard drilldown 从 raw JSON 提升为结构化只读表格：当前 panel 资产已稳定展示 `sampled jobs/rag eval dead letters/total dead letters/failed events/terminal events`，以及 recent rag-eval failure rows 与 notes；渲染兼容 dashboard fixture `detail.items` 和当前 live `detail.agent_job_metrics`，只消费 Go detail，不创建/租约/重试 `rag_eval` AgentJob，不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `rag_eval_failures` 的 dashboard panel 证据并入 `dashboard_read_models`。当前 `dashboard_fallback.category=live_verified_runtime_read_models` 已覆盖当前跟踪的主要 runtime-overview structured read-model，不再剩余已知 raw-JSON-only runtime-overview drilldown gap。
- 已新增 repo-owned `queue_topology` live verifier：`scripts/verify-queue-topology-boundary.ps1` 会同时读取 `/v1/queue-topology`、`/v1/queue-backend` 与 `/v1/runtime-overview`，校验 provider recommendation、`outbox_delivery` / `agent_job` execution owner、`agent_job` ack owner 和 external-lease-ready 的当前 turn 一致性；全程只读，不 publish/lease/ack/nack/term MQ，不创建 AgentJob/outbox delivery，不触发 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `queue_topology` verifier 输出并入统一 goal 证据，新增 `queue_topology` 段与 `residual_classification.queue_topology_boundary`；同时 runtime overview panel 的 `Queue Topology` 空态文案已固定为 `No queue topology work kinds sampled`，使 `dashboard_read_models.queue_topology_table=true` 与 live verifier 证据在当前 turn 同步成立。
- 已新增 repo-owned worker-control-executor boundary live verifier：`scripts/verify-worker-control-executor-boundary.ps1` 会同时读取 `/v1/agent-job-capacity/plan`、`/v1/agent-job-priority/plan`、`/v1/control-mutations/policy`、`/v1/runtime-workers` 与 `/v1/runtime-overview`，校验 capacity/priority plan summary、只读属性、policy allowlist 以及 runtime 里“尚无 autoscaling/concurrency/priority executor worker”的当前 turn 边界；全程只读，不 apply control mutation，不启动/停止 worker，不执行 Python AI。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 worker-control verifier 输出并入统一 goal 证据，新增 `worker_control_executors` 段与 `residual_classification.worker_control_executors`，并用同一 live 结论替换原先写死的 `autoscaling_executor` 静态分类。当前 live 结论是 `go_control_plane_live_verified_without_worker_control_executor`。
- 已新增 repo-owned temp-runtime worker-status fencing live smoke：`scripts/verify-agent-worker-status-fencing-live-smoke.ps1` 会对同一 synthetic `worker_id` 先写入 `instance_a`，再验证第二实例 `instance_b` 默认得到 `HTTP 409`，最后仅在 `replace_existing_instance_id=instance_a` 时允许 takeover，并同时核对 `/v1/agent-worker-statuses` 与 `agent-worker-statuses.json`。这条 smoke 证明了 Go worker-status lease fencing 的默认拒绝和受控 takeover 边界，而不影响长期运行 runtime。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `agent_worker_status_fencing` 并入 `runtime_invariants` 与 `residual_classification`。当前 live 结论是 `controlled_worker_status_fencing_live_verified`。
- 已新增 repo-owned temp-runtime worker-status heartbeat renewal live smoke：`scripts/verify-agent-worker-status-heartbeat-live-smoke.ps1` 会对同一 synthetic `worker_id + instance_id` 连续上报三次 `running` heartbeat，并同时核对 `/v1/agent-worker-statuses` 与 `agent-worker-statuses.json` 的最终 `updated_at/lease_until`。这条 smoke 证明了 Go worker-status renewal 语义会持续刷新 lease，而不会把同一实例误判成 stale。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `agent_worker_status_heartbeat` 并入 `runtime_invariants` 与 `residual_classification`。当前 live 结论是 `worker_status_heartbeat_renewal_live_verified`。
- 已新增 Go-owned stale worker cleanup mutation：`POST /v1/agent-worker-statuses/cleanup-stale` 只删除超过 stale threshold 的 heartbeat 残留，可选按 `worker_id` / `instance_id` 过滤，返回 `deleted/remaining/totals/notes/side_effect=runtime_state_only`，不会启动/停止 Python worker、不会修改 AgentJob 或任何 AI 执行状态。
- 已新增 repo-owned stale worker cleanup live smoke：`scripts/verify-agent-worker-status-cleanup-live-smoke.ps1` 会在隔离 temp runtime 中证明 stale worker 可被删除且活跃 worker 不会被误删；本轮还在当前 live runtime 上真实清掉了历史 stale `akashic-python-worker` 残留，`/v1/agent-worker-statuses.totals.stale` 与 `/v1/runtime-overview.summary.agent_workers_stale` 现都为 `0`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `agent_worker_status_cleanup` 并入 `runtime_invariants` 与 `residual_classification`。
- 已新增 Go-owned worker-status startup prune：`NewAgentWorkerStatusServiceWithRepository()` 现在会在 repository load 时直接删除 stale `agent-worker-statuses`，避免 runtime restart 后把历史 heartbeat 残留重新载回内存。
- 已新增 repo-owned startup prune live smoke：`scripts/verify-agent-worker-status-startup-prune-live-smoke.ps1` 会预写 stale + active `agent-worker-statuses.json`，启动隔离 temp `agent-runtime` 后验证 stale record 已从 API/state file 消失而 active record 保留；本轮还已重启当前 8780 live runtime 并复核 `agent_workers_total=0/agent_workers_stale=0`，`knowledge_job_planner_readiness_worker_stale=0`。`.\scripts\verify-go-migration-goal.ps1` 现已把这条证据纳入 `runtime_invariants.agent_worker_status_startup_prune`，并把 `current_state.agent_workers_total/agent_workers_stale/agent_workers_stopped` 直接对齐 live `/v1/agent-worker-statuses`。
- `.\scripts\verify-go-migration-goal.ps1` 现在会把当前 turn 的完整 JSON 默认写回 repo 根目录 `.codex-goal-verifier.json`，不再只停留在 stdout；同时新增顶层 `current_state` 摘要，把 `qq_group_send_enabled`、`telegram_token_configured`、`outbox_execution_owner/scope`、`agent_job_execution_owner/ack_owner`、`agent_job_external_lease_ready/decision`、`receiver_statuses_telegram`、`agent_workers_total/stale`、`runtime_overview_agent_workers_stale` 和 `dashboard_fallback_category` 规范成稳定字段，便于每轮 goal 收口与残留分类直接消费当前 live 证据，而不需要手工深挖嵌套 JSON。
- `.\scripts\verify-go-migration-goal.ps1` 现在还会输出顶层 `migration_residuals`，把 QQ/NapCat cutover、Telegram backend、`agent_job external lease result-ack`、scheduler runtime、worker control executors、media recovery private-source executor、dashboard read-model cleanup 和 MQ adapter boundary 这八类残留固定成 machine-readable 分类结果，并附带当前 live facts 与 `next_minimal_step`。这让每轮收口不再依赖人工从大 JSON 里重新拼结论。
- `.\scripts\verify-go-migration-goal.ps1` 现在还会给每个 residual entry 补 `migration_bucket`，并新增顶层 `migration_bucket_summary`，直接把残留归入四个精确桶：`already_in_go_only_missing_live_verification`、`go_control_plane_present_but_real_executor_missing`、`still_not_fully_cut_over`、`explicitly_python_owned`。同时还新增了 `python_owned_surfaces` entry，明确把 LLM/provider、prompt/context/reasoning、tool execution、OCR/VLM、image generation 和 memory/RAG 策略实验标记为 Python 保留面，而不是 Go 未完成项。
- 已新增 repo-owned isolated proactive runtime flow live smoke：`scripts/verify-proactive-runtime-flow-live-smoke.ps1` 现在会启动隔离 temp `agent-runtime`，真实验证 proactive deliveries duplicate/count、seen-items normalized hit、rejection cooldown、cleanup、anyaction quota、drift mark/finish/summary、bg-context global mark，以及 tick-log start/step/finish/list/detail/steps，并确认这些 Go-owned deterministic state 会落盘到 `proactive-state.json`，且全过程不触发 QQ/Telegram 发送或 Python AI。`.\scripts\verify-go-migration-goal.ps1` 也已把这条 current-turn 证据并入 `proactive_runtime_flow_smoke` 与 `residual_classification.proactive_flow_smoke`。
- 已新增 repo-owned isolated dashboard proactive tick-log runtime-fallback live verifier：`scripts/verify-dashboard-proactive-tick-logs-boundary.ps1` 现在会启动隔离 temp `agent-runtime`，在空 SQLite dashboard workspace 下证明 `/api/dashboard/proactive/tick_logs` 的 list/detail/steps 会回退到 Go runtime tick state，且不会把 runtime tick 数据写回 SQLite `tick_log` mirror。`.\scripts\verify-go-migration-goal.ps1` 也已把这条 current-turn 证据并入 `dashboard_proactive_tick_logs_boundary`，并把 `dashboard_read_models.proactive_tick_logs_readable` 提升为“live dashboard 可读 + repo-owned fallback verifier 已通过”的组合判据。
- `.\scripts\verify-go-migration-goal.ps1` 现已把同一个 `$dashboardReadModelChecks` 对象同时暴露在顶层 `dashboard_read_models` 和 `checks.dashboard_read_models`。这次改动不改变任何 live verifier 结论，只是把 canonical artifact 的机器可读路径固定下来，避免 `checks.dashboard_read_models.proactive_tick_logs_readable` 被错误读成 `null`。
- 已新增 repo-owned dashboard knowledge/RAG boundary live verifier：`scripts/verify-dashboard-knowledge-rag-state-boundary.ps1` 会直接比对 Go `/v1/knowledge-pipeline-diagnostics`、Go `/v1/runtime-overview` 与 dashboard `/api/dashboard/runtime-overview` 的 `knowledge_pipelines` card/top-level detail，并固定当前 checkpoint-derived RAG dataset/index boundary的 live parity。
- `plugins/runtime_overview/dashboard.py` 现已在 fallback 路径补齐 `knowledge_pipelines` top-level payload、summary fields 和 card，并把 `/v1/knowledge-pipeline-diagnostics` 的 fallback timeout 提高到不低于 5 秒，避免 live dashboard 因默认超时太短而把 knowledge/RAG detail 退化为空态。
- `.\scripts\verify-go-migration-goal.ps1` 现已把 `dashboard_knowledge_rag_state_boundary` 并入统一 artifact，并把 `dashboard_read_models.knowledge_pipelines_table` 从 panel 字符串探针升级为“dashboard live parity verifier 已通过”的当前 turn 证据；同时新增 `current_state.knowledge_configured_rag_datasets`、`knowledge_rag_datasets`、`knowledge_rag_dataset_index_ready` 与 `residual_classification.knowledge_rag_state_boundary=checkpoint_snapshot_derived_control_plane_live_verified`。
- `.\scripts\verify-go-migration-goal.ps1` 现已对 `dashboard_knowledge_rag_state_boundary` 增加单次短暂重试：如果首次 repo-owned dashboard knowledge/RAG live verifier 没拿到 `live_verified`，统一 verifier 会在当前 turn 再取样一次，并在第二次通过时直接采用该结果。当前这条 hardening 已把 `.codex-goal-verifier.json.current_state.dashboard_fallback_category` 稳定恢复到 `live_verified_runtime_read_models`，避免 knowledge/RAG dashboard parity 在同一 turn 上被偶发误降级成 `partially_live_verified_read_model`。
- 已新增 repo-owned dashboard media recovery live verifier：`scripts/verify-dashboard-media-asset-content-recovery-boundary.ps1` 现在会直接比对 Go `/v1/runtime-overview` 与 dashboard `/api/dashboard/runtime-overview` 的 `media_asset_content_recovery_*` summary、`Media Content Recovery` card 和 top-level detail，并固定当前 turn 的 read-only parity，不调用 preflight/recovery、不创建 approval/mutation、不下载/缓存内容、不触发 OCR/VLM/RAG/AI。
- `plugins/runtime_overview/dashboard.py` 的 fallback 现在会从 `control-mutations` audit ledger 合成 `media_asset_content_recovery` summary/card/detail；因此即使 dashboard 因 `/v1/runtime-overview` 聚合超时走 fallback，`media_asset_content_recovery` 也不再退回 `unknown/muted`。`.\scripts\verify-go-migration-goal.ps1` 已把这条 current-turn 证据并入 `dashboard_read_models.media_asset_content_recovery_table`、`checks.dashboard_read_models.media_asset_content_recovery_table` 与 `residual_classification.dashboard_media_asset_content_recovery_boundary`。
- 已新增 Go-owned stale receiver-status cleanup mutation：
  `POST /v1/receiver-statuses/cleanup-stale` 只删除超过 stale threshold 的
  receiver-status heartbeat 残留，可选按 `receiver_id` 过滤，返回
  `deleted/remaining/totals/notes/side_effect=runtime_state_only`，不会启动、
  停止、重连 QQ/Telegram receiver，不修改 receiver lease，不触发 Python AI。
- 已新增并运行 `.\scripts\verify-receiver-status-cleanup-live-smoke.ps1`：脚本会先在
  隔离 temp `agent-runtime` 中制造一条 stale receiver 和一条 active receiver，
  再调用 `POST /v1/receiver-statuses/cleanup-stale`，当前真实证明 stale receiver
  会进入 `deleted`，active receiver 保留在 `remaining`，并且 temp
  `receiver-statuses.json` 与 `GET /v1/receiver-statuses` 一致。当前 live runtime
  inspection 还确认仍有 2 条 `heartbeat_stale` receiver-status 记录，但默认未对
  live runtime 执行 cleanup；`.\scripts\verify-go-migration-goal.ps1` 已把
  `receiver_status_cleanup` 纳入 `runtime_invariants` 与
  `residual_classification`。
- 已修复 dashboard runtime-overview normalize 路径遗漏 top-level
  `knowledge_pipelines` 的问题；当前 `/api/dashboard/runtime-overview` 的
  `knowledge_pipelines` top-level、card detail 与 Go runtime state 已恢复一致，
  `verify-dashboard-knowledge-rag-state-boundary.ps1` 不再因 top-level 空壳把
  dashboard fallback 降级成 partial。
- 已新增 Go-owned `agent_job external lease` approval-bound preflight endpoint：
  `GET /v1/agent-job-external-lease/preflight` 先读取
  `/v1/agent-job-external-lease/plan`，只有在 `plan.ready=true` 后才继续做
  control-mutation approval preflight；当前返回会稳定区分
  `agent_job_external_lease_plan_not_ready`、`missing_approval_id` 和
  `agent_job_external_lease_preflight_ready`，全程 `side_effect=none`。
- 已新增 Go-owned canonical launcher bundle 只读接口：
  `GET /v1/agent-job-external-lease/launcher-bundle` 会把
  `scripts/start-agent-runtime.ps1` 所需的 exact launcher parameters、
  environment overrides、required external inputs、verification steps 和当前
  `agent_job external lease` plan 一起固定成 machine-readable contract。
- 已新增并运行 repo-owned launcher bundle live smoke
  `scripts/verify-agent-job-external-lease-launcher-bundle.ps1`：当前已在隔离
  temp runtime + temp NATS 上真实证明 blocked bundle 会稳定返回 canonical
  NATS/result-ack flags、`QueueDSN` external input 和 verification steps；同时
  也已真实证明用 bundle 动态构建出的 launcher 参数可以把 temp runtime 启到
  promoted state，并让 queue backend / queue topology / runtime-overview /
  approval-bound preflight 一起进入
  `repo_owned_launcher_external_lease_bundle_live_verified`。
- repo-local launcher `scripts/start-agent-runtime.ps1` 现已支持显式注入
  `agent_job external lease result-ack` preflight 所需的
  runtime addr/state/shadow、queue backend/dsn/mode、external-lease cutover、
  dual-read/state-lease-worker disable、agent-job duplicate/flow smoke 和
  strict lease token flags；`/v1/runtime-config` 也已同步暴露这些布尔 cutover
  env 的 presence/redacted value。
- 已新增并运行 repo-owned launcher live smoke
  `scripts/verify-agent-job-external-lease-launcher-preflight.ps1`：
  当前通过 `start-agent-runtime.ps1 -Foreground` 在隔离 temp runtime 上真实证明：
  runtime-config 里这些 flags 已 present，queue backend 已提升到
  `nats_jetstream`，queue topology 的 `agent_job.execution_owner/ack_owner`
  已提升，并且 approval-bound preflight 能从 `missing_approval_id` 走到
  `agent_job_external_lease_preflight_ready`。统一 goal verifier 也已把这条
  evidence 纳入 `runtime_invariants.agent_job_external_lease_launcher_preflight`
  与 `residual_classification.agent_job_external_lease_launcher_preflight`。
- 已增强并重新运行
  `.\scripts\verify-agent-job-external-lease-cutover-preflight.ps1`：当前 repo-owned
  isolated verifier 已固定证明三段语义同时成立：
  1) `config_blocked` temp runtime 下 approval-bound preflight 会先停在
  `agent_job_external_lease_plan_not_ready`；
  2) temp NATS/runtime 满足 worker coverage 后，不带 `approval_id` 的 preflight
  会稳定返回 `missing_approval_id`；
  3) active approval 注入后，同一路径会返回
  `agent_job_external_lease_preflight_ready`。这说明当前剩余 gap 已收敛为生产
  flags/owner cutover，而不是 approval-bound gate 缺失。
- 已新增 Go-owned `agent_job external lease` cutover diff 只读接口：
  `GET /v1/agent-job-external-lease/cutover-diff` 会直接比较 live runtime 与
  canonical launcher bundle，输出 `matching/drift/current_*/expected_*`，
  并把 external-lease/result-ack 的缺口固定成 machine-readable payload。
- 已新增并运行 repo-owned live verifier
  `scripts/verify-agent-job-external-lease-cutover-diff.ps1`：当前已真实证明
  8780 live runtime 仍处于 blocked drift state，同时也已在 temp NATS/runtime
  上证明 `zero config drift -> agent_job_worker_coverage_blocked -> ready`
  这条链路。当前 unified goal artifact 已纳入
  `repo_owned_agent_job_external_lease_cutover_diff_live_verified`。

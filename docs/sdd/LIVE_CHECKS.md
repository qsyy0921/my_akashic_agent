# Akashic 运行态观察清单

最后更新：2026-06-04

这些项目不是新的开发任务，而是已完成切片上线后的 live smoke / 观察验证。不要再塞回 `TODO.md`。

## QQ / Telegram / Observe

- [ ] 继续验证 QQ 群实时采集质量：Go `/v1/observe-capture-diagnostics` 应显示观察群文本、图片、文件覆盖和 media content ready 状态；新文件样本出现后确认 file coverage 从 0 变为 covered，且 observe-only 不回复。
- [x] 已把 observe-only 群“绝不回复群消息”提升为 Go runtime 硬约束：当前 `GET /v1/observe-targets` 已显示 6 个 enabled QQ group observe targets 且全部 `reply_allowed=false`；对真实观察群 `qq:1049511700:group:27234224` 创建 outbox 后调用 `POST /v1/delivery-dispatch/readiness`，live 返回 `HTTP 400`、`message="observe-only target does not allow replies"`、`data.error_kind="route_error"`。同时，`qq:1049511700:private:2365524513` 的 readiness 仍返回 `ready=true`。这证明当前观察群在 runtime 层也被强制静默，而不只是依赖 config 约定。
- [x] 已把 QQ 群发送改成“可手动开启/关闭”的全局策略，并完成 live 验证：当前 `config.toml` 中两个 QQ 配置都为 `group_send_enabled=false`，Go runtime 以 `AKASHIC_QQ_GROUP_SEND_ENABLED=false` 启动，`GET /v1/runtime-config` 已真实返回 `delivery.qq_group_send_enabled=false`。`.\scripts\verify-go-outbox-scope-live.ps1` 证明第一账号 `group text` 与第二账号 `group file` 的 readiness 都返回 `HTTP 400/message="qq group sends are disabled"/error_kind="route_error"`，而第一账号 `private file` 仍可自动 `succeeded`。`.\scripts\verify-go-migration-goal.ps1` 也默认输出 `qq_group_send_toggle_disabled=true` 与 `qq_group_route_blocked_by_global_policy=true`。
- [x] 已新增并运行 `.\scripts\verify-qq-cutover-route-matrix.ps1`：当前 live 结果已真实把 QQ route 切成三类：`go_execution_owner_scope` 包含 global `text`、second-account `file` 和 first-account `private file`；`policy_blocked_routes` 包含当前被 `qq_group_send_enabled=false` 拦截的 `qq/group` text 与 second-account `group file`；`platform_blocker_routes` 包含两个账号的 `private/group image` 与 first-account `group file`。`.\scripts\verify-go-migration-goal.ps1` 也已把该结构并入 `qq_cutover_route_matrix` 与 `qq_outbox_cutover.route_matrix`。
- [x] 已查看 `/v1/runtime-overview` 与 runtime overview panel 资产：`qq_cutover_route_matrix` 当前已是结构化只读 drilldown，不再只停留在 verifier 输出或 raw JSON；panel 已展示 `go scope/currently sendable/policy blocked/platform blockers/group send enabled/execution scope`，以及 `Go Execution Owner Scope`、`Currently Sendable Routes`、`Policy Blocked Routes`、`Platform Blocker Routes` 四类 route 表。当前 live 结果为 `go scope=5/currently sendable=5/policy blocked=3/platform blockers=5`，`group send enabled=false`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.qq_cutover_route_matrix_table=true` 纳入当前 turn 取证；调用后不开启 QQ 群发、不修改 route gate、不执行 native probe、不发送 QQ 消息。
- [ ] 查看 `/v1/media-assets/content-diagnostics?asset_id=...`：真实 QQ 图片/文件应显示 `content_status=ready`；若为 `forbidden`/`unavailable`/`disabled`，应能对应到安全根目录、文件缺失或 content reader 配置问题，调用后不执行 OCR/VLM/文件解析。
- [x] 已新增并运行 `.\scripts\verify-media-asset-content-recovery-live-smoke.ps1`：脚本会拉起 temp HTTP source 与 temp `agent-runtime`，真实验证 `GET /v1/media-assets/content-recovery/preflight` 在无 approval 时返回 `missing_approval_id`、在 active approval 后返回 `media_asset_content_recovery_preflight_ready`，并且整个 preflight / dry-run 阶段都不会下载远端内容。随后脚本再执行 `POST /v1/media-assets/content-recovery` 的 dry-run 和正式 recovery，当前已真实证明 `dry_run_does_not_write_cache=true`、`remote_source_only_downloaded_on_live_recovery=true`、`live_recovery_writes_cache_and_updates_registry=true`、`content_endpoint_reads_recovered_bytes=true`、`control_mutation_audit_recorded=true`，且全过程不触发 OCR/VLM/RAG/AI。
- [x] 已查看 `/v1/runtime-overview` 的 `Media Content Recovery` card、`media_asset_content_recovery_*` summary 和 detail：当前 live 结果已真实返回 `media_asset_content_recovery_ready=true`、`media_asset_content_recovery_reason=media_asset_content_recovery_audit_ready`、`applied=0/failed=0/recent_audits=0`，card 为 `value=0/0/status=muted`，detail 仍只暴露 `plan/preflight/recovery` 链接与只读 notes。调用过程仅 GET `/v1/runtime-overview`，未创建 approval/mutation、未调用 recovery executor、未下载/缓存内容、未触发 OCR/VLM/RAG/AI。
- [x] 已新增并运行 `.\scripts\verify-dashboard-media-asset-content-recovery-boundary.ps1`：当前 live 结果已真实证明 dashboard `/api/dashboard/runtime-overview` 的 `media_asset_content_recovery` summary/card/detail 与 Go `/v1/runtime-overview` 当前 turn 一致；同时 fallback 现在会从 `control-mutations` audit ledger 合成 `media_asset_content_recovery_audit_ready`，不再退回 `unknown/muted`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.media_asset_content_recovery_table=true` 与 `checks.dashboard_read_models.media_asset_content_recovery_table=true` 纳入当前 turn 取证；调用后不调用 preflight/recovery endpoint、不开新 approval/mutation、不下载/缓存内容、不触发 OCR/VLM/RAG/AI。
- [x] 已新增并运行 `.\scripts\verify-dashboard-media-asset-content-boundary.ps1`：当前 live 结果已真实证明 dashboard `/api/dashboard/runtime-overview` 的 `media_asset_content_*` summary、`Media Asset Content` card/detail、sampled asset 的 `content_recovery_plan_endpoint` / `content_recovery_preflight_endpoint`，以及 dashboard `/api/dashboard/media-assets/content-recovery/preflight` proxy 都与 Go 当前 turn 保持一致；dashboard fallback 现在也会从 `/v1/media-assets/content-diagnostics` 保留该只读视图，不再在 direct overview 聚合超时时丢失 `media_asset_content`。调用后不主动请求 recovery、不创建 approval/mutation、不下载/恢复/缓存内容、不触发 OCR/VLM/RAG/AI。
- [ ] 查看 `/v1/media-assets/retention-diagnostics?limit=50`：真实 QQ 图片/文件应按 retention policy 显示 permanent/default/ephemeral/unknown 和 cleanup_due；调用后不得删除 media asset registry、本地文件或触发 OCR/VLM/文件解析。
- [ ] 查看 `/v1/runtime-overview` 和 Python dashboard `/api/dashboard/runtime-overview` 的 `Media Asset Retention` card：应与 `/v1/media-assets/retention-diagnostics` 的 cleanup_due / assets / retention class 统计一致，调用 overview 不得删除 registry、本地文件或触发 OCR/VLM/文件解析。
- [ ] 查看 `/v1/media-assets/retention-plan?limit=50`：当存在过期非 permanent 资产时应返回 `ready=true`、`candidate_count>0`、`required_steps` 包含 operator approval 与 planned control mutation；无候选时应返回 `ready=false` 和 blocker。调用后不得删除 media metadata、本地文件、创建 approval/mutation、触发 OCR/VLM/文件解析或调用 AI。
- [ ] 查看 `/v1/runtime-overview` 和 Python dashboard `/api/dashboard/runtime-overview` 的 `Media Asset Retention Plan` card：应与 `/v1/media-assets/retention-plan` 的 ready/candidate_count/blockers/required_steps 一致；调用后不得删除 media metadata、本地文件、创建 approval/mutation、触发 OCR/VLM/文件解析或调用 AI。
- [ ] 验证 media retention control mutation policy：`/v1/control-mutations/policy?target_kind=media_asset_retention` 应只列出 `cleanup_expired`；`/v1/control-mutations/preflight` 使用 active approval + `cleanup_expired` 应返回 ready，使用其它 action 应返回 `unsupported_control_mutation_action`。调用后不得删除 media metadata、本地文件、创建 mutation、触发 OCR/VLM/AI。
- [ ] 若 Telegram `getUpdates` conflict 再次出现，检查 Go `/v1/receiver-statuses` 的 `status=suspended`、`reason=getupdates_conflict`，并确认 `/v1/receiver-leases` 没有重复 Akashic receiver；若仍冲突，排查外部 polling 进程或改 webhook。
- [ ] Telegram 后台 smoke 继续阻塞在 `TELEGRAM_BOT_TOKEN` 缺失：本轮复核后 `/v1/runtime-config` 仍显示 `telegram_token_configured=false`，`/v1/receiver-statuses` 也只有 2 个 QQ receiver、没有 Telegram receiver。补齐 token 后再做 `getMe`、receiver status 和收发 smoke，不要把 Desktop 登录误记为 bot backend 已就绪。
- [x] 已新增并运行 `.\scripts\verify-telegram-backend.ps1`：脚本会同时读取 `/v1/runtime-config` 和 `/v1/receiver-statuses`，并在存在 token 时尝试 Telegram `getMe`。当前 live 结果明确是 `runtime_reports_token_configured=false`、`local_env_token_present=false`、`telegram_receiver_present=false`、`getme_attempted=false`、`backend_state=token_missing`。
- [x] 已用 `.\scripts\verify-telegram-backend.ps1 -RepoRoot E:\agent\my-akashic_agent` 增强 Telegram blocker 证据：当前不仅确认 `backend_state=token_missing`，还明确证明 `config.toml` 已声明 `[channels.telegram]` 且 `token = "${TELEGRAM_BOT_TOKEN}"`，同时 repo `.env`、`~/.codex/.env` 和当前进程环境都没有 Telegram token。这说明当前 blocker 是 token 注入缺失，而不是通道未声明或隐藏的 `getMe` 失败。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`receiver_statuses` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `receivers/connected/qq/telegram/suspended/failed` totals，以及每个 receiver 的 `receiver_id/kind/channel/account/status/reason/endpoint/updated_at`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.receiver_statuses_table=true` 纳入当前 turn 取证；调用后不启动/停止/重连 receiver，不发送 QQ/Telegram，不触发 Python AI。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`receiver_leases` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `leases/active/expired/qq/telegram` totals，以及每个 lease 的 `receiver_id/kind/account_id/owner_instance_id/status/expires_at/updated_at`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.receiver_leases_table=true` 纳入当前 turn 取证；调用后不执行 cleanup、不创建/删除 lease、不启动/停止 receiver、不发送 QQ/Telegram、不触发 Python AI。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`knowledge_pipelines` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `targets/enabled/ready/warning/blocked/lagging/stale_checkpoints/receiver_connected` totals，以及每个 pipeline 的 `target_id/account/capture_status/group_memory/rag_ingest/coverage/latest_source_seq/reasons`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.knowledge_pipelines_table=true` 纳入当前 turn 取证；调用后不创建 checkpoint、不创建/租约/执行 AgentJob、不发送 QQ/Telegram、不触发 Python AI。
- [x] 已新增并运行 `.\scripts\verify-dashboard-knowledge-rag-state-boundary.ps1`：当前 live 结果已真实证明 Go `/v1/knowledge-pipeline-diagnostics`、Go `/v1/runtime-overview` 与 dashboard `/api/dashboard/runtime-overview` 的 `knowledge_pipelines` card/top-level detail 在当前 turn 上保持一致；dashboard fallback 现在也会补齐 `knowledge_pipelines` top-level payload、summary 和 card。`.\scripts\verify-go-migration-goal.ps1` 已把 `dashboard_read_models.knowledge_pipelines_table=true` 升级为这条 live verifier 通过后的证据，并新增 `current_state.knowledge_configured_rag_datasets=0`、`knowledge_rag_datasets=0`、`knowledge_rag_dataset_index_ready=0`；调用后不创建 checkpoint、不创建/租约/执行 AgentJob、不触发 Python AI。
- [x] 已重新运行 `.\scripts\verify-go-migration-goal.ps1` 并确认 unified artifact 的 dashboard knowledge/RAG 证据已稳定：当 `scripts/verify-dashboard-knowledge-rag-state-boundary.ps1` 首次采样未返回 `live_verified` 时，统一 verifier 现在会做一次短暂重试；当前 `.codex-goal-verifier.json.current_state.dashboard_fallback_category` 已恢复为 `live_verified_runtime_read_models`，不会再把同一 turn 已通过的 knowledge/RAG dashboard parity 误写成 `partially_live_verified_read_model`。
- [x] 已修复并复核 dashboard runtime-overview normalize 路径的 top-level `knowledge_pipelines` parity：当前 `verify-dashboard-knowledge-rag-state-boundary.ps1` 不再出现 “card/detail 正确但 dashboard top-level 空壳” 的失败形态；live dashboard `/api/dashboard/runtime-overview` 已恢复与 Go runtime 一致的 `knowledge_pipelines` top-level payload。
- [x] 已重新运行 `.\scripts\verify-go-migration-goal.ps1` 并确认 stdout mode hardening 生效：默认调用现在只输出小摘要 JSON，不再把整份 artifact 直接刷到 stdout；`.\scripts\verify-go-migration-goal.ps1 -StdoutMode none` 也已真实刷新 repo 根的 `.codex-goal-verifier.json`。本轮实测 `none` 模式完成后 artifact 时间戳已更新到 `2026-06-03T21:17:29.2305899+08:00`，默认 `summary` 模式则输出 `artifact_path/generated_at/open_blockers/current_state/migration_bucket_summary`，不改变 artifact schema。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`external_lease_diagnostics` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `provider/mode/external_queue_configured/external_queue_active/consumer_concurrency/max_in_flight/outbox_execution_owner/agent_job_execution_owner`，以及 selected provider 的 `consumer_model/adapter_boundary/recommended_first_backend/supports_external_lease/supports_agent_job_result_ack/blockers`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.external_lease_diagnostics_table=true` 纳入当前 turn 取证；调用后不修改 MQ、不 publish/lease/ack/nack/term、不创建/执行 AgentJob/outbox、不触发 QQ/Telegram 或 Python AI。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`scheduler_jobs` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `sampled/enabled/disabled/overdue/due_soon/instant/soft/due_soon_seconds`、trigger/tier/status 分布，以及 recent sample jobs 的 `job_id/trigger/tier/status/channel/next_run/reason`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.scheduler_jobs_table=true` 纳入当前 turn 取证；调用后不创建/修改/删除 scheduler job，不执行 complete/recovery，不发送 QQ/Telegram，不触发 Python AI。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`agent_workers` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `workers/idle/running/stopped/stale/failed/knowledge/outbox_delivery`，以及每个 worker 的 `worker_id/worker_type/status/lease_active/stale/processed_total/failed_total/reason`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.agent_workers_table=true` 纳入当前 turn 取证；调用后不启动/停止/替换 worker，不修改 lease，不发送 QQ/Telegram，不触发 Python AI。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`runtime_workers` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `workers/enabled/disabled/running`，以及每个 Go worker 的 `name/kind/enabled/running/worker_id/interval_or_concurrency/batch_or_max_in_flight/notes`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.runtime_workers_table=true` 纳入当前 turn 取证；调用后不启动/停止/重启 worker，不修改 queue flags，不发送 QQ/Telegram，不触发 Python AI。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`agent_job_worker_coverage` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `job_type/expected_worker_types/coverage_status/worker_count/active/running/failed/stale/high_pressure/reason`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.agent_job_worker_coverage_table=true` 纳入当前 turn 取证；调用后不启动/停止/替换 worker，不创建/租约/执行 AgentJob，不发送 QQ/Telegram，不触发 Python AI。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`agent_job_pressure` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `job_types/high_pressure/max_pending/max_active/oldest_pending_age_seconds`、throughput 的 `created/succeeded`、dead letters current total，以及 `pressure.by_type` 的 `job_type/pending/leased/running/active/oldest_pending_age_seconds/high_pressure`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.agent_job_pressure_table=true` 纳入当前 turn 取证；调用后不创建/租约/执行 AgentJob，不修改 queue 参数，不发送 QQ/Telegram，不触发 Python AI。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`outbox_pressure` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `accounts/high_pressure_accounts/max_queued/max_active`、throughput 的 `queued/leased/succeeded`、dead letters current total，以及 `pressure.by_account` 的 `account_key/channel_kind/account_id/queued/dispatching/active/dead_lettered/high_pressure/pressure_reason`。当前 live `GET /v1/runtime-overview` 中 `Outbox Pressure` card 为 `value=1/2/status=warn`，detail 显示 `accounts=2/high_pressure_accounts=1/max_active=17/max_queued=17`，且高压账号是 `qq:1049511700`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.outbox_pressure_table=true` 纳入当前 turn 取证；调用后不创建/租约/执行 outbox delivery，不修改 queue/backpressure 参数，不发送 QQ/Telegram，不触发 Python AI。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`outbox_metrics` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 throughput 的 `queued/leased/dispatching/succeeded/failed/dead_lettered/terminal_events`、dead letters current total、`deliveries_by_channel_kind`，以及 recent dead-letter rows 的 `delivery_id/channel_kind/event_type/status/error_kind/error_message/attempt/occurred_at`。当前 live `GET /v1/runtime-overview` 中 `Outbox Metrics` card 为 `value=50/status=danger`，detail 显示 `sampled_deliveries=200/sampled_events=50/current_total=97`，且 recent dead letters 仍主要是 `qq group sends are disabled` 的 observe-only goal-check route_error。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.outbox_metrics_table=true` 纳入当前 turn 取证；调用后不创建/租约/执行 outbox delivery，不修改 queue/backpressure 参数，不发送 QQ/Telegram，不触发 Python AI。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`send_ledger_metrics` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `sampled_records/unique_bots/unique_conversations/unique_content_hashes/repeated_content_hashes`，以及 `repeated_hashes` 与 `recent` 的表格。当前 live `GET /v1/runtime-overview` 中 `Send Ledger Metrics` card 为 `value=200/status=warn`，detail 显示 `sampled_records=200/unique_bots=2/unique_conversations=6/repeated_content_hashes=7`，且高频重复 hash 仍集中在 `1049511700/2365524513` 与 `1049511700/164369633`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.send_ledger_metrics_table=true` 纳入当前 turn 取证；调用后不创建/租约/执行 outbox delivery，不修改 send ledger / loop guard 状态，不发送 QQ/Telegram，不触发 Python AI。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`inbox_metrics` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `sampled_events/observe_only_total/reply_eligible_total/with_attachments/attachment_count/unique_senders`、`events_by_channel_kind`、conversation 分布，以及 recent inbox event 表格。当前 live `GET /v1/runtime-overview` 中 `Inbox Metrics` card 为 `value=200/status=ok`，detail 显示 `sampled_events=200/observe_only_total=200/reply_eligible_total=0/with_attachments=23`，并且主会话集中在 `qq/1049511700/group/3219982`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.inbox_metrics_table=true` 纳入当前 turn 取证；调用后不修改 observe target / receiver / checkpoint 状态，不发送 QQ/Telegram，不触发 Python AI。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`inbound_dedupe_metrics` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `sampled_records/active_records/expired_records/duplicate_records/seen_total/duplicate_seen_total`、scope 表格和 notes。当前 live `GET /v1/runtime-overview` 中 `Inbound Dedupe` card 为 `value=0/status=ok`，detail 显示 `sampled_records=200/active_records=200/duplicate_records=0/duplicate_seen_total=0`，并且当前 scope 集中在 `qq:qq:1049511700`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.inbound_dedupe_metrics_table=true` 纳入当前 turn 取证；调用后不修改 dedupe 记录，不发送 QQ/Telegram，不触发 Python AI。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`observe_targets` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `targets/enabled/observe_only/reply_allowed/groups/qq`、target 表格和 notes。当前 live `GET /v1/runtime-overview` 中 `Observe Targets` card 为 `value=6/status=ok`，detail 显示 `targets=6/enabled=6/observe_only=6/reply_allowed=0`，且 6 个目标全部是 `1049511700` 的 QQ group。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.observe_targets_table=true` 纳入当前 turn 取证；调用后不修改 observe target 配置，不发送 QQ/Telegram，不触发 Python AI。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`observe_capture` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `targets/ready/warning/blocked/image_covered/file_covered/content_ready_assets/receiver_activity_recent`、target 表格和 notes。当前 live `GET /v1/runtime-overview` 中 `Observe Capture` card 为 `value=0/6/status=warn`，detail 显示 `targets=6/warning=6/image_covered=6/file_covered=0/content_ready_assets=0`，并且所有 target 当前 blocker 都包含 `file_not_seen` 与 `media_content_not_fully_ready`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.observe_capture_table=true` 纳入当前 turn 取证；调用后不修改 observe capture / media 状态，不发送 QQ/Telegram，不触发 Python AI。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`delivery_adapters` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `provider/channel/transport/enabled/endpoint_configured/access_token_configured/endpoint`，并保留 `Adapter Health` 与 `Delivery Smoke` 的只读入口。当前 live `GET /v1/runtime-overview` 中 `Delivery Adapters` card 为 `value=3/status=ok`，detail 显示当前 3 个 OneBot websocket adapter 都已启用，endpoint/token 都已配置，而 Telegram adapter 仍未出现在列表里。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.delivery_adapters_table=true` 纳入当前 turn 取证；调用后不修改 adapter 配置，不发送 QQ/Telegram，不触发 Python AI。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`agent_job_metrics` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `sampled_jobs/sampled_events/job_types/created/leased/running/succeeded/dead_letters_current`，以及 `jobs_by_type`、`pressure.by_type`、`dead_letters.recent` 三个表格。当前 live `GET /v1/runtime-overview` 中 `Agent Job Metrics` card 为 `value=50/status=ok`，detail 显示 `sampled_jobs=200/sampled_events=50`、throughput `created=12/leased=12/running=13/succeeded=13`，并且 `dead_letters.current_total=0`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.agent_job_metrics_table=true` 纳入当前 turn 取证；调用后不创建/租约/执行 AgentJob，不修改 queue owner/worker，不发送 QQ/Telegram，不触发 Python AI。
- [x] 已新增并运行 `.\scripts\verify-go-migration-goal.ps1 -IncludeNativeRichMediaProbe -RichMediaProbeGroupId 3219982`：脚本会把当前 QQ outbox scope、可选原生 rich-media probe、Telegram backend、knowledge planner 和剩余分类汇总成单个 JSON。当前 live 结果明确显示 `execution_scope=account_conversation_kind_gated`、Telegram `backend_state=token_missing`、knowledge planner `conclusion=go_admission_owner_healthy`，且第一账号 `3219982` group 的原生 probe 仍返回 `image_send.retcode=1200` 与 `file_send.retcode=1200`。
- [x] 已把 observe-only 静默检查并入 `.\scripts\verify-go-migration-goal.ps1` 默认路径：当前 unified verifier 会直接输出 `observe_only_silence`，并要求至少一个 enabled QQ group target `reply_allowed=false`、真实 group readiness 被 `observe-only target does not allow replies` 阻断、QQ private readiness 仍为 `ready=true`；若任一条件失效，会把对应 blocker 写进 `checks.open_blockers`。
- [x] 已运行默认 `.\scripts\verify-go-migration-goal.ps1`：当前 `observe_only_silence.checks` 全部为 `true`，默认 `checks.open_blockers` 只剩 `telegram_token_missing` 和 `qq_image_native_platform_blocker_unresolved`。这说明默认 goal 更新路径现在会自动复核观察群静默，而不是依赖额外手工说明。
- [x] 已把 scheduler / proactive / dashboard fallback 并入默认 `.\scripts\verify-go-migration-goal.ps1` 的当前 turn 取证：scheduler 现在不只读取 `/v1/scheduler/jobs`、`/v1/scheduler/leases`、`/v1/scheduler/diagnostics`，还会调用 `.\scripts\verify-scheduler-runtime-live-smoke.ps1` 启动隔离 temp runtime，验证 `scheduler-jobs.json` CRUD、lease-fenced `/v1/scheduler/jobs/{job_id}/complete` 和 Python recovery reconciliation；本轮 `residual_classification.scheduler.status=live_verified/category=go_control_plane_mutation_and_recovery_live_verified`。`residual_classification.proactive.status=live_verified/category=go_state_live_verified_with_dashboard_read_model` 仍保持不变，因为 `/v1/proactive/tick-logs` 存在近期 tick、`/v1/proactive/anyaction/quota` 可见 quota，且 dashboard `/api/dashboard/proactive/tick_logs` 可直接读到当前 tick list；`dashboard_fallback.status` 也已转成 `live_verified`。
- [x] 已新增并运行 `.\scripts\verify-agent-job-external-lease.ps1`：当前 live 结果明确表明 `agent_job external lease result-ack` 不是 worker coverage blocker，而是 `blocked_by_configuration_and_cutover_flags`。具体来说，当前 provider 仍是 `local`，selected provider 不支持 `agent_job result-ack`，`AKASHIC_AGENT_JOB_STRICT_LEASE_TOKEN` 与 `AKASHIC_QUEUE_EXTERNAL_LEASE_AGENT_JOB_ENABLED` 都未启用，且 duplicate/flow smoke flags 不存在。
- [x] 已新增并运行 `.\scripts\verify-agent-job-external-lease-launcher-preflight.ps1`：当前 repo-local launcher 已能在隔离 temp runtime 上稳定注入 result-ack preflight 所需 flags，并真实证明 runtime-config/queue-backend/queue-topology/approval-bound preflight 会一起进入 `repo_owned_launcher_external_lease_preflight_live_verified`。这条 evidence 说明剩余 gap 已不是 launcher 参数面，而是 production runtime 何时真正注入 flags 并切 owner。
- [x] 已新增并运行 `.\scripts\verify-agent-job-external-lease-cutover-preflight.ps1`：脚本会先启动一个 `config_blocked` temp runtime，固定证明当前 blocked 语义仍是 `provider=local`、`agent_job_execution_owner=python_ai_worker_state_store_lease`，且 readiness 含 `external_lease_not_configured` 与 `strict_lease_token_disabled`；随后再启动 temp NATS + full result-ack flags 的 `preflight_ready` 场景，创建一个 old pending `group_memory_extract` job，先复现 `agent_job_worker_coverage_blocked`，再通过 active `knowledge` worker heartbeat 让 readiness 与 plan 一起翻到 `ready`。这证明 repo-owned isolated preflight 已经 live verified，但不改变长期运行的生产 runtime owner/flags。
- [x] 已把 approval-bound endpoint 纳入同一条 isolated verifier：当前 `GET /v1/agent-job-external-lease/preflight` 在 `config_blocked` temp runtime 下会先返回 `reason=agent_job_external_lease_plan_not_ready`；在 temp NATS/runtime 满足 worker coverage 后，不带 approval 的同一路径会稳定返回 `reason=missing_approval_id`，而注入 active approval 后则返回 `reason=agent_job_external_lease_preflight_ready`。这条 current-turn 证据说明 cutover 前的 approval gate 已从文档约束落成可重复调用的只读 API，而不是人工拼 `plan + control-mutations/preflight`。
- [x] 已新增并运行 `.\scripts\verify-media-recovery-boundary.ps1`：当前 live 结果明确表明，runtime 的 media recovery control plane 是可达的，本机当前 sampled assets 全为 `file://` observe-only QQ group 上传镜像，`content_diagnostics` 为 `forbidden=120/120`，sample recovery plan 为 `media_asset_content_recovery_fix_content_roots`，preflight 为 `missing_approval_id/executor_scope=operator_runtime_config`，同时 `has_http_https_assets=false`。本轮还已修复 dashboard `/api/dashboard/runtime-overview` 的 `media_asset_content_recovery` read-model gap，脚本现已真实返回 `runtime_overview_has_media_content_recovery_card=true`、`runtime_overview_has_media_content_recovery_detail=true` 和 `dashboard_runtime_overview_missing_media_recovery_detail=false`。
- [x] 已再次运行 `.\scripts\verify-go-migration-goal.ps1` 并确认 unified verifier 现在还会输出 `media_recovery_executor_smoke`：当前 `.codex-goal-verifier.json` 已真实包含 `conclusion.category=go_http_https_media_recovery_executor_live_verified`，且 `migration_residuals.media_recovery_private_source_executor.category=go_http_https_executor_live_verified_private_source_executor_incomplete`。这说明当前 HTTP/HTTPS recovery executor 已有 repo-owned current-turn evidence，剩余 gap 已准确收敛到 private-source fetch / background repull / post-recovery AI enrichment。
- [x] 已新增并运行 `.\scripts\verify-go-outbox-scope-live.ps1`：脚本会对当前 partial Go owner 真实创建 4 个 outbox 事件。当前 live 结果是：`first_account_private_file` 与 `second_account_group_file` 都自动 `succeeded`；`first_account_group_file_gated` 与 `first_account_private_image_gated` 都保持 `queued/attempts=0`。这证明当前 Go owner 的成功边界和 gate 边界都仍然成立。
- [x] 已新增并运行 `.\scripts\verify-go-migration-goal.ps1 -IncludeOutboxScopeSmoke -IncludeNativeRichMediaProbe -RichMediaProbeGroupId 3219982`：统一 goal 验证脚本现在可以在显式开关下同时输出 outbox partial cutover 的真实成功/拦截边界、Telegram `token_missing`、knowledge planner 健康状态，以及第一账号 `3219982` group rich-media 仍为 `retcode=1200/rich media transfer failed`。
- [x] 已在修复 Go OneBot WebSocket 对结构化 `status` 的解析后再次运行 `.\scripts\verify-go-outbox-scope-live.ps1` 与 `.\scripts\verify-go-migration-goal.ps1 -IncludeOutboxScopeSmoke -IncludeNativeRichMediaProbe -RichMediaProbeGroupId 3219982`：当前再复跑不会因 `json: cannot unmarshal object into Go struct field onebotResponse.status of type string` 误报失败，统一 goal 结果稳定回到 `partial_go_default_owner_live_verified`。
- [x] 已在修复 repo-local launcher 缺失的 `AKASHIC_DELIVERY_CHANNEL_BY_ACCOUNT` 后再次重启 runtime，并重新运行 `.\scripts\verify-go-outbox-scope-live.ps1` 与 `.\scripts\verify-go-migration-goal.ps1 -IncludeOutboxScopeSmoke -IncludeNativeRichMediaProbe -RichMediaProbeGroupId 3219982`：当前 `second_account_group_file` 已恢复为真实 `succeeded`，统一 goal 的 `open_blockers` 也不再包含伪造的 `outbox_scope_case_failed_second_account_group_file`。
- [ ] 若 `/v1/receiver-leases` 出现 expired 租约残留，先调用 `POST /v1/receiver-leases/cleanup-expired`，确认返回 `deleted` 增加、`remaining_active` 保留活跃租约、`side_effect=runtime_state_only`，且调用后 QQ/Telegram 接收端不被启动/停止、observe-only 群不回复。
- [ ] 查看 `/v1/runtime-overview` 或 Python dashboard `/api/dashboard/runtime-overview`：当 `receiver_leases_expired>0` 时应看到 `receiver_lease_cleanup_required=true`、`receiver_lease_cleanup_endpoint=/v1/receiver-leases/cleanup-expired`，`Receiver Leases` card value 应显示 active/expired，调用 overview 不得自动执行 cleanup。
- [x] 已完成双 QQ 账号私聊文本真实 smoke：运行 `.\scripts\run-qq-live-smoke.ps1 -SkipLogCheck`，`1049511700 -> 2365524513` 与 `2365524513 -> 1049511700` 均返回 `dispatch.results[0].status="sent"`、OneBot `provider_message_id` 非空，且对应 smoke outbox event 最终为 `succeeded`。该验证不等于群文本/图片/文件已通过，也不等于 Go local outbox worker 已启用。
- [x] 已完成 QQ 群文本真实 smoke：运行 `.\scripts\run-qq-group-live-smoke.ps1 -GroupId 27234224`，`1049511700 -> group:27234224` 的 text case 返回 `dispatch.results[0].status="sent"`、OneBot `provider_message_id=1774231456`，对应 smoke outbox event 最终为 `succeeded`。
- [ ] QQ 群图片/文件 smoke 仍被当前 NapCat/QQ rich media 上传阻塞，但阻塞已被拆分：第一账号 `1049511700` 上，`.\scripts\run-napcat-native-rich-media-smoke.ps1` 在 `27234224`、`3219982`、`164369633` 三个群都复现了 image/file 最终 `rich media transfer failed`，且 `docker restart napcat` 后仍不恢复；本轮再次通过 `.\scripts\verify-go-migration-goal.ps1 -IncludeNativeRichMediaProbe -RichMediaProbeGroupId 3219982` 复核，`image_send.retcode=1200` 与 `file_send.retcode=1200` 仍然一致。第二账号 `2365524513` 上，native 与 Akashic 对照都表明 group file 已可成功，而 image 仍失败。新增 private 对照后，又确认两账号 private image 也失败、两账号 private file 都成功，且第一账号 Akashic private file 也成功。当前结论是：image 失败不是 Go adapter staging blocker，并且已跨账号、跨 group/private 出现；第一账号 file 失败则收窄为当前 group route 特有问题，而不是 account-wide。在 rich media blocker 解除前，不要把 image/file 全量能力当作已通过。
- [ ] 做 QQ/NapCat Go adapter live send smoke：当前已完成双账号私聊文本、单群文本，以及 account-kind / account-conversation-kind gated file smoke。第二账号 `2365524513` 的 `group file` 已在 Go local worker 自动执行下真实成功；第一账号 `1049511700` 的 `private file` 现在也已在 route-scoped gate 下自动成功，而第一账号 `group file` 在同一运行态下保持 `queued/attempts=0`。当前剩余 blocker 已收敛为：第一账号 `group file` / `group image` 需要人工重新登录或替换 group rich-media 会话后重跑；`image` 则仍跨两个账号、跨 group/private 原生失败。在 image 和第一账号 group-file blocker 解除前，不要把 QQ 全量 rich-media cutover 记成完成。
- [x] 已验证 Go local outbox worker execution-owner 路径：临时以 `AKASHIC_OUTBOX_DELIVERY_WORKER_ENABLED=true` + `AKASHIC_DELIVERY_CHANNEL_BY_ACCOUNT=1049511700=qq_1049511700,2365524513=qq_2365524513` 启动 runtime 后，`/v1/runtime-workers` 显示 worker running，`/v1/outbound-cutover/readiness` 返回 `execution_ready=true/execution_owner=go_local_outbox_worker`，且真实私聊文本 outbox item 无需手工 `delivery-dispatch/send` 即自动变成 `succeeded`。该验证完成后已回滚到默认 worker-disabled 运行态。
- [x] 已验证 text-only Go outbox cutover gate：当前 `/v1/runtime-config` 显示 `outbox_delivery_worker_enabled=true`、`outbox_delivery_allowed_kinds=["text"]`，`/v1/queue-backend` 显示 `outbox_execution_owner=go_local_outbox_worker/outbox_execution_scope=text_only`，`/v1/outbound-cutover/readiness` 也已达到 `execution_ready=true`。新的私聊文本 outbox event `qq-text-owner-resmoke:6cf419535b404c8884b53f7006bc99fe` 在事件流中真实经过 `queued -> leased -> succeeded`，`lease_owner=agent-runtime-outbox-worker`；新的群图片/文件 outbox event `qq-rich-gate-check:image:07cfb709113240debd3c10e7ca01b80d` 与 `qq-rich-gate-check:file:45b6657cdff24c2b900db1f447c857ff` 在连续轮询中都保持 `queued/attempts=0`。同时 `/v1/agent-worker-statuses` 中 Python outbox worker 已显示 `metadata.reason=go_runtime_outbox_worker_active`，不再抢 rich-media lease。
- [x] 已验证 account-kind Go outbox gate：当前 runtime 以 `AKASHIC_OUTBOX_DELIVERY_ALLOWED_KINDS=text` + `AKASHIC_OUTBOX_DELIVERY_ALLOWED_KINDS_BY_ACCOUNT=1049511700=text,2365524513=text|file` 运行，`/v1/runtime-config`、`/v1/queue-backend`、`/v1/runtime-workers` 都已显示 account-scoped override，且 `outbox_execution_scope=account_kind_gated`。真实 smoke 中，第一账号 file outbox event `qq-account-gate:first-file:5ada75b397494cc6bfd61ed50d707084` 连续轮询保持 `queued/attempts=0`，第二账号 file outbox event `qq-account-gate:second-file:8ffb2b622ed1420f833447943e8720f0` 则由 Go local outbox worker 自动处理并最终 `succeeded`。
- [x] 已验证 account-conversation-kind Go outbox gate：当前 runtime 以 `AKASHIC_OUTBOX_DELIVERY_ALLOWED_KINDS=text`、`AKASHIC_OUTBOX_DELIVERY_ALLOWED_KINDS_BY_ACCOUNT=2365524513=text|file`、`AKASHIC_OUTBOX_DELIVERY_ALLOWED_KINDS_BY_ACCOUNT_CONVERSATION_TYPE=1049511700/private=text|file` 运行，`/v1/runtime-config`、`/v1/queue-backend`、`/v1/runtime-workers` 都已显示 route-scoped override，且 `outbox_execution_scope=account_conversation_kind_gated`。真实 smoke 中，第一账号 private file 事件 `qq-account-conv-gate:first-private-file:c65f6cb5bcbe4e589fdcdc396194c66b` 由 Go local outbox worker 自动处理并最终 `succeeded`；第一账号 group file 事件 `qq-account-conv-gate:first-group-file:fac830d6a62847a997b2521e899b259d` 连续轮询保持 `queued/attempts=0`；第二账号 group file 事件 `qq-account-conv-gate:second-group-file:e2681d140af248dd91b6191412b201bc` 则继续自动 `succeeded`。
- [ ] 做 QQ/NapCat cutover 前先查看 `/v1/outbound-cutover/readiness`：应同时满足 OneBot aliases 完整、delivery smoke ready、`execution_ready=true`；若 local worker 和 NATS external lease 都没执行 outbox，应出现 `outbox_execution_path_not_ready`。当前还要额外确认 rich media 原生对照已通过，否则即便文本 smoke 已通，也不要把默认 Go execution owner 当作全量 cutover 完成。
- [ ] 做 QQ/NapCat cutover 前查看 `/v1/outbound-cutover/plan`：确认 `desired_execution_owner`、enable steps、verification endpoints 和 rollback steps 与本次计划一致，并确认 `side_effect=none`。
- [x] 已查看 `/v1/runtime-overview` 的 `outbound_cutover_plan_*` summary 和 `Outbound Cutover` card：当前与 `/v1/outbound-cutover/plan` 一致，runtime overview panel 已结构化展示 `Current/Desired/Recommended Owner`、expected OneBot channels、required/enable/verification/rollback steps 和 blocker 数；调用后未执行 cutover、未发送 QQ/Telegram、未 ack/nack MQ、未触发 Python AI。
- [ ] 继续观察 QQ/NapCat `message_id` 和 group upload file 字段在真实重连/重投场景的稳定性；如果 NapCat 暴露新的文件 notice 字段名，补进 `_file_meta_from_notice` 和 file-event dedupe key metadata。

## AgentJob / Worker / Queue

- [ ] 观察 Go `AgentJob` admission dedupe live 效果：重启 Python knowledge worker 后确认 `group_memory_extract` / `rag_ingest` pending 数不再按分钟无界增长。
- [x] 已查看 `/v1/knowledge-job-planner/preview`：当前 6 个 observe-only QQ 群均可见，`group_memory_extract` 计划 job 数为 6、`rag_ingest` 为 0，返回 `side_effect=none`，调用本身不新增 `/v1/jobs` 记录。
- [x] 已查看 `/v1/knowledge-job-planner/readiness`：在 Python `knowledge` worker active=1 的情况下返回 `ready=true`；planner 启用前为 `planner_enabled=false/planner_running=false` 的 preflight 状态，启用后变为 `planner_enabled=true/planner_running=true`。
- [x] 已查看 `/v1/knowledge-job-planner/cutover-plan`：启用前返回 `decision=ready_to_enable_go_planner/current_admission_owner=python_legacy_knowledge_enqueue`；启用后返回 `decision=ready/current_admission_owner=go_runtime_knowledge_job_planner`，且保持 `side_effect=none`。
- [x] 已查看 `/v1/runtime-overview` 的 `knowledge_job_planner_preview_*` summary 和 `Knowledge Planner` card：与 `/v1/knowledge-job-planner/preview` 的 targets/groups/total jobs 一致。
- [x] 已查看 `/v1/runtime-overview` 的 `knowledge_job_planner_readiness_*` summary 和 `Knowledge Planner Readiness` card：与 `/v1/knowledge-job-planner/readiness` 的 ready/planner/worker 状态一致。
- [x] 已查看 `/v1/runtime-overview` 的 `knowledge_job_planner_cutover_plan_*` summary 和 `Knowledge Planner Cutover` card：当前已显示 `ready=true/current_owner=go_runtime_knowledge_job_planner`，与 `/v1/knowledge-job-planner/cutover-plan` 一致。
- [x] 已查看 Python dashboard `/api/dashboard/runtime-overview`：当前已透传并规范化 Go-owned `knowledge_job_planner_cutover_plan` detail，最新 bucket 的 `scheduler=agent-runtime-knowledge-job-planner` 结果也已出现在 dashboard runtime overview 明细中；调用后未创建 AgentJob、未修改 env、未启动额外 worker、未执行额外 group memory/RAG/AI。
- [x] 已查看 runtime overview panel 资产：`knowledge_job_planner_cutover_plan` 当前已展示 `Current/Desired/Recommended Owner`、`Preview Bucket`、`Required Checks / Enable Steps / Verification Steps / Rollback Steps` 等结构化 drilldown，不再只依赖 raw JSON；打开 detail 不创建 AgentJob、不改 env、不启动 worker、不执行 group memory/RAG/AI。
- [x] 已启用 `AKASHIC_KNOWLEDGE_JOB_PLANNER_ENABLED=true` 并观察 `/v1/runtime-config` 和 `/v1/runtime-workers`：当前显示 `knowledge_job_planner_enabled=true` 且 `knowledge_job_planner.running=true`；启用后 `logs/akashic-local-run.err.log` 没有再出现新的 legacy enqueue 行，同时 `/v1/jobs?type=group_memory_extract` 已出现 `scheduler=agent-runtime-knowledge-job-planner` 的新 bucket 成功记录，`logs/agent-runtime-local.err.log` 也已连续跨多个 5 分钟 bucket 记录 planner run，且本轮 `/v1/knowledge-job-planner/cutover-plan` 仍保持 `decision=ready/current_admission_owner=go_runtime_knowledge_job_planner`，证明 Python 仍在 lease/execute 但 admission owner 已切走。
- [x] 已新增并运行 `.\scripts\verify-knowledge-planner-cutover.ps1`：脚本会从当前 runtime endpoint 和当前 repo 日志同时取证，输出 `planner_running_and_ready`、`recent_planner_buckets_seen`、`recent_go_scheduler_jobs_seen`、`recent_skip_legacy_enqueue_seen` 等显式检查位。当前 live 结果已经覆盖 bucket `5934695`、`5934696`、`5934697`，且最近 `group_memory_extract` jobs 仍由 `agent-runtime-knowledge-job-planner` 创建并由 Python knowledge worker 成功执行。
- [ ] 查看 `/v1/job-metrics` 与 `/v1/runtime-overview.summary`：当 `group_memory_extract` / `rag_ingest` 堆积时，`pressure.by_type` 应反映 pending / active / oldest_pending_age_seconds，`agent_job_pressure_high_job_types` 应随 high pressure job type 增长。
- [ ] 查看 `/v1/runtime-overview` 的 `agent_job_worker_coverage_*` 和 `Agent Job Worker Coverage` card：当 knowledge / rag_eval / image job 堆积时，应能区分 no active worker、stale worker、failed worker 和 active worker available。
- [ ] 查看 `/v1/agent-job-capacity/plan`：当 `group_memory_extract` / `rag_ingest` / `image_generation` 堆积时，应能给出 `start_or_recover_python_worker`、`inspect_failed_worker`、`renew_or_restart_stale_worker` 或 `increase_worker_concurrency_or_prioritize_queue` 等只读建议；调用后不得新增/租约/重试 AgentJob。
- [ ] 查看 `/v1/agent-job-priority/plan`：当 `group_memory_extract` / `rag_ingest` / `rag_eval` / `image_generation` 堆积时，应按 critical/high/medium/low 输出 rank、score、action 和 blockers；调用后不得新增/租约/重试 AgentJob，不得 ack/nack MQ，不得启动 Python worker 或执行 AI。
- [x] 已查看 `/v1/runtime-overview` 与 runtime overview panel 资产：`Agent Job Priority` 当前已是结构化只读 drilldown，可直接看到 rank/job_type/priority_class/score/recommendation/verification steps；调用后未改变 job/MQ/worker/AI 执行状态。
- [x] 已新增并运行 `.\scripts\verify-control-audit-boundary.ps1`：脚本会启动隔离 temp `agent-runtime`，真实写入两条 `outbound_cutover` operator approvals（1 条 `approved`、1 条 `rejected`），再用 `GET /v1/operator-approvals?target_kind=outbound_cutover&limit=10` 复核 totals 和 `side_effect=runtime_state_only`，并确认 `.akashic-workspace/agent-runtime/operator-approvals.json` 等价落盘。当前 live 结果已真实证明 ledger 只写 runtime state，不修改 env/config、不执行 cutover、不创建/租约 AgentJob、不 ack/nack MQ、不触发 Python AI。
- [x] 同一脚本已复核 `GET /v1/operator-approvals/check`：对 active approved 记录当前真实返回 `approved=true/reason=approval_active/side_effect=none`；对 rejected 记录返回 `approval_not_approved`；对缺失 target 返回 `approval_not_found`。这些检查只读，不修改 env/config、不执行 cutover、不创建/租约 AgentJob、不 ack/nack MQ、不触发 Python AI。
- [x] 同一脚本已真实写入两条 `control_mutations` 审计（1 条 `planned`、1 条 `failed`），再用 `GET /v1/control-mutations?approval_id=...&target_kind=outbound_cutover&limit=10` 复核 totals 和 `side_effect=runtime_state_only`，并确认 `.akashic-workspace/agent-runtime/control-mutations.json` 等价落盘。当前 live 结果已证明 mutation audit 仅写 runtime state，不修改 env/config、不执行 cutover、不创建/租约 AgentJob、不 ack/nack MQ、不触发 Python AI。
- [x] 同一脚本已复核 `GET /v1/control-mutations/preflight?target_kind=outbound_cutover&target_id=cutover-a&action=enable&operator_id=qsyy&approval_id=...`：active approved 当前真实返回 `ready=true/reason=approval_active/suggested_audit.status=planned/side_effect=none`；缺失 approval 返回 `approval_not_ready/approval_not_found` blocker。调用后未新增 approval/mutation 记录、未修改 env/config、未执行 cutover、未创建/租约 AgentJob、未 ack/nack MQ、未触发 Python AI。
- [x] 同一脚本已验证 control mutation policy blocker：对 unsupported `target_kind=model_provider_config` 查询 `/v1/control-mutations/policy` 当前真实返回 `allowed=false/reason=unsupported_control_mutation_target`；对 unsupported `action=enable` 查询 `target_kind=agent_job_capacity` 的 preflight 当前真实返回 `ready=false/reason=unsupported_control_mutation_action/supported_actions=["apply","rollback"]`。当前 approval 结果不会把任意 target/action 放行为可执行 mutation。
- [x] 同一脚本已查看 `/v1/control-mutations/policy`：当前真实返回 Go-owned allowlist、`side_effect=none`，其中 `outbound_cutover=enable|rollback`、`agent_job_capacity=apply|rollback`，并保留 `agent_job_external_lease`、`agent_job_priority`、`knowledge_job_planner_cutover`、`media_asset_retention`、`media_asset_content` 等 intent。调用后不创建 approval/mutation、不修改 env/config、不执行 cutover、不启动 worker、不 ack/nack MQ、不触发 AI。
- [x] 同一脚本已查看 `/v1/runtime-overview` 的 `control_mutation_policy` card、`control_mutation_policy_*` summary 和 detail：当前与 `/v1/control-mutations/policy` 一致，`control_mutation_policy_allowed=true`、`reason=control_mutation_policy_listed`，card 为 `status=ok`，且 value 与 intent/action totals 一致。调用后未创建 approval/mutation、未修改 env/config、未执行 cutover、未启动 worker、未 ack/nack MQ、未触发 AI。
- [x] 已新增并运行 `.\scripts\verify-dashboard-control-audit-boundary.ps1`：当前 live 结果已真实证明 dashboard `/api/dashboard/runtime-overview` 的 `control_mutation_policy` summary、top-level detail 与 `Control Mutation Policy` card 和 Go `/v1/runtime-overview` 当前 turn 完全一致；`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.control_mutation_policy_table=true` 切到这条 live verifier 输出，不再只靠 panel 静态字符串探针。调用过程不创建 approval/mutation、不修改 env/config、不执行 cutover、不启动 worker、不 ack/nack MQ、不触发 AI。
- [ ] 查看 runtime overview dashboard 的 `Control Mutation Policy` detail：应显示 allowed/reason/targets/actions totals 和 target/action allowlist 表格，同时保留 raw JSON；打开 detail 不得编辑 policy、创建 approval/mutation、执行 cutover、worker scaling、MQ ack/nack、media cleanup 或 AI。
- [x] 已查看 `/v1/runtime-overview` 的 `control_audit` card、`operator_approvals_*` 和 `control_mutations_*` summary：通过 `.\scripts\verify-control-audit-boundary.ps1`，当前真实证明 summary 与 `/v1/operator-approvals`、`/v1/control-mutations` 保持一致，`operator_approvals_total=2/active=1/approved=1/rejected=1`，`control_mutations_total=2/planned=1/failed=1`，card 当前为 `value=1/2/status=warn`。调用后未新增 approval/mutation 记录、未修改 env/config、未执行 cutover、未创建/租约 AgentJob、未 ack/nack MQ、未触发 Python AI。
- [x] 同一 verifier 已真实证明 dashboard `/api/dashboard/runtime-overview` 的 `control_audit` card、top-level `operator_approvals` / `control_mutations` 和 summary 默认值与 Go `/v1/runtime-overview` 当前 turn 一致；当前 `conclusion.category=dashboard_control_audit_boundary_live_verified`。调用过程不新增 approval/mutation 记录，不修改 env/config，不执行 cutover，不创建/租约 AgentJob，不 ack/nack MQ，不触发 Python AI。
- [x] 已重新运行 `.\scripts\verify-go-migration-goal.ps1` 并确认 unified verifier 现在会默认回写 repo 根目录 `.codex-goal-verifier.json`。当前 artifact 已真实包含顶层 `current_state`，直接暴露 `qq_group_send_enabled=false`、`telegram_token_configured=false`、`outbox_execution_owner=go_local_outbox_worker`、`outbox_execution_scope=account_conversation_kind_gated`、`agent_job_execution_owner=python_ai_worker_state_store_lease`、`agent_job_external_lease_ready=false`、`receiver_statuses_telegram=0`、`agent_workers_total=3`、`agent_workers_stale=0`、`runtime_overview_agent_workers_stale=0` 与 `dashboard_fallback_category=live_verified_runtime_read_models`。写回动作只持久化当前 turn verifier 结果，不修改 runtime 配置、不执行 cutover、不发送 QQ/Telegram、不触发 Python AI。
- [x] 已再次运行 `.\scripts\verify-go-migration-goal.ps1` 并确认 unified verifier 现在还会输出顶层 `migration_residuals`：当前 artifact 已真实把 `qq_napcat_cutover`、`telegram_backend`、`agent_job_external_lease_result_ack`、`scheduler_runtime`、`worker_control_executors`、`media_recovery_private_source_executor`、`dashboard_read_models`、`mq_adapter_boundary` 八类残留固定为 machine-readable 分类，并附带当前 turn live facts 与 `next_minimal_step`。该分类输出只消费已有 verifier / runtime evidence，不执行新的 cutover、worker mutation、QQ/Telegram 发送或 Python AI。
- [x] 已再次运行 `.\scripts\verify-go-migration-goal.ps1` 并确认 unified verifier 现在还会输出顶层 `migration_bucket_summary`：当前 `.codex-goal-verifier.json` 已真实把 residual entries 直接汇总进四个精确桶，分别是 `already_in_go_only_missing_live_verification`、`go_control_plane_present_but_real_executor_missing`、`still_not_fully_cut_over` 和 `explicitly_python_owned`。当前 turn 的 live 结果中，`scheduler_runtime`、`dashboard_read_models`、`mq_adapter_boundary` 归入第一桶，`worker_control_executors` 与 `media_recovery_private_source_executor` 归入第二桶，`qq_napcat_cutover`、`telegram_backend` 与 `agent_job_external_lease_result_ack` 归入第三桶，`python_owned_surfaces` 归入第四桶。该 summary 只复用现有 live verifier 结果，不触发新的 cutover、worker mutation、QQ/Telegram 发送或 Python AI。
- [ ] 查看 runtime overview dashboard 的 `Control Audit` detail：应显示 Operator Approvals 和 Control Mutations totals/table，并保留 raw JSON；打开 detail 不得创建/检查/撤销 approval，不得记录 mutation，不得执行 cutover、worker scaling、MQ ack/nack、media cleanup 或 AI。
- [x] 已查看 `/v1/runtime-overview` 的 `agent_job_capacity_*` summary、`Agent Job Capacity` card 和 runtime overview panel 资产：当前与 `/v1/agent-job-capacity/plan` 的 ready/reason/summary 一致，panel 已结构化展示 capacity items、notes 与 verification steps；调用后未启动 worker、调度、租约、重试或 ack/nack MQ。
- [x] 已查看 `/v1/runtime-overview` 的 `agent_job_priority_*` summary、`Agent Job Priority` card 和 runtime overview panel 资产：当前与 `/v1/agent-job-priority/plan` 的 ready/reason/blockers/summary 一致，panel 已结构化展示 priority items、notes 与 verification steps；调用后未真实调整 job 顺序、worker 并发或 ack/nack MQ。
- [x] 已查看 Python dashboard `/api/dashboard/runtime-overview` 与 runtime overview panel 资产：`agent_job_capacity_plan`、`agent_job_priority_plan`、`agent_job_external_lease_readiness`、`agent_job_external_lease_plan`、`knowledge_job_planner_cutover_plan`、`outbound_cutover_plan` 当前都已是结构化只读 drilldown，不再只停留在 raw JSON；调用后不启动 worker、不 ack/nack MQ、不创建/租约 AgentJob、不创建 outbox delivery，也不发送 QQ/Telegram。
- [ ] 查看 `/v1/runtime-overview` 的 `media_asset_content_*` summary 和 `Media Asset Content` card：应与 `/v1/media-assets/content-diagnostics?limit=50` 的 ready/forbidden/unavailable/disabled/error 计数一致；调用后不得改变 `/content` 访问策略，不触发 OCR/VLM/文件解析。
- [ ] 查看 Python dashboard `/api/dashboard/runtime-overview`：应透传并规范化 Go-owned `delivery_smoke_readiness` 与 `media_asset_content_diagnostics` detail，summary 中应包含 `delivery_smoke_*` 和 `media_asset_content_*` 默认值；调用后不得发送 QQ/Telegram 消息、租约 outbox/AgentJob、触发 OCR/VLM/文件解析或调用 AI。
- [ ] 点击前查看 `GET /v1/media-assets/content-access-plan?asset_id=...`：ready 资产应返回 content endpoint 和 `media_asset_content_ready`；forbidden/unavailable/disabled/missing id/missing asset 应返回稳定 blockers，调用后不得流式传输内容、不得下载远程媒体、不得触发 OCR/VLM/RAG/AI。
- [ ] 在浏览器打开 `/api/dashboard/media-assets/content?asset_id=...`：若 Go content route 失败且 workspace fallback 找不到本地文件，应返回 JSON detail，其中包含 Go `content_access_plan.reason/blockers/side_effect`；调用后不得改变 Go content policy、不得下载远程媒体、不得触发 OCR/VLM/RAG/AI。
- [ ] 在浏览器直接请求 `/api/dashboard/media-assets/content-access-plan?asset_id=...`：应返回 Go plan 的 ready/reason/blockers/content endpoint/side_effect，且不打开内容流、不下载远程媒体、不触发 OCR/VLM/RAG/AI。
- [ ] 查看 Go `/v1/media-assets/content-access-plan?asset_id=...`：响应应包含 `runtime_path`、`dashboard_path`、`content_url`，且这些字段与 dashboard proxy URL 一致；调用后不得打开内容流、下载远程媒体或触发 OCR/VLM/RAG/AI。
- [ ] 查看 Go `/v1/media-assets/content-recovery-plan?asset_id=...` 和 dashboard `/api/dashboard/media-assets/content-recovery-plan?asset_id=...`：ready 资产应返回 `media_asset_content_recovery_not_required`；forbidden/unavailable/disabled/error 应返回对应恢复步骤和 `future_executor_scope`，调用后不得下载/恢复/流式传输内容、不得触发 OCR/VLM/RAG/AI。
- [ ] 查看 dashboard 消息列表/详情的 `media_assets`：每个带 `asset_id` 的媒体资产应同时包含 `content_url`、`content_access_plan_url` 和 `content_recovery_plan_url`，列表加载时不得批量请求 access/recovery plan、不得触发 OCR/VLM/RAG/AI。
- [ ] 修改 Go media content access plan 或 dashboard proxy 字段前，先更新 `media_asset_content_access_plan.qq.image.json` 合同 fixture，并确认 `tests/test_sdd_contract_fixtures.py` 通过，避免 Go/Python 字段漂移。
- [ ] 修改 `MediaAssetContentAccessPlan` 字段前，同时运行 `go test ./...`（`services/agent-runtime`）和 `uv run pytest tests/test_sdd_contract_fixtures.py -q`，确认 Go/Python 双侧合同 fixture 都能捕获 ready/reason/path/content endpoint/side_effect/steps 漂移。
- [ ] 查看 `/v1/media-assets/content-diagnostics?limit=50` 和 Python dashboard `/api/dashboard/runtime-overview` 的 `media_asset_content_diagnostics.items`：每个带 asset id 的 item 应有 `content_access_plan_endpoint` 和 `content_recovery_plan_endpoint`，调用后不得批量请求 access/recovery plan、不得打开内容流、不得触发 OCR/VLM/RAG/AI。
- [ ] 查看 runtime overview dashboard 的 `Media Asset Content` detail：应显示媒体内容 totals 表格和每个 asset 的 content/access/recovery 链接，同时保留 raw JSON；打开 detail 不得额外请求 access/recovery plan、不得打开内容流、不得触发 OCR/VLM/RAG/AI。
- [ ] 修改 media asset runtime API 时，同步检查 `services/agent-runtime/README.md` 与 `docs/sdd/specs/agent-gateway/000-index.md`，确保 endpoint、side-effect 和 Go/Python 边界说明没有落后于代码。
- [ ] 通过 `GET /v1/media-assets/retention-cleanup/preflight?target_id=...&operator_id=...&approval_id=...` 校验媒体保留清理前置条件：有 cleanup candidates 且 approval active 时应返回 `ready=true/reason=media_asset_retention_cleanup_preflight_ready/suggested_audit.status=planned/side_effect=none`；无候选、approval 缺失/过期/不匹配时应返回稳定 blockers，且调用后不得新增 approval/mutation 记录、不得删除媒体 metadata/文件、不得触发 OCR/VLM/RAG/AI。
- [ ] 通过 `POST /v1/media-assets/retention-cleanup` 做一次 dry-run：应返回候选和 `side_effect=none`，不得删除 Go media asset metadata、不得记录 control mutation audit、不得删除本地文件、不得触发 OCR/VLM/RAG/AI；正式 apply 前必须先记录 operator approval 并确认 preflight ready。
- [ ] 通过 `POST /v1/media-assets/retention-cleanup` 做一次小范围正式 apply：应只删除过期非 permanent 的 Go media asset metadata，返回 `side_effect=runtime_state_cleanup` 和 applied audit；随后用 `/v1/media-assets/retention-plan` 验证 candidate_count 下降，并抽查本地文件仍存在。
- [ ] 查看 `/v1/runtime-overview` 的 `media_asset_retention_cleanup_*` summary 和 `Media Asset Retention Cleanup` card：应与 retention plan candidates 和最近 `media_asset_retention/cleanup_expired` control mutation audits 一致，调用后不得创建 approval/mutation、不得执行 cleanup、不得删除 metadata/文件、不得触发 OCR/VLM/RAG/AI。
- [ ] 查看 Python dashboard `/api/dashboard/runtime-overview` 的 `media_asset_retention_cleanup` detail、summary 和 `Media Asset Retention Cleanup` card：应与 Go `/v1/runtime-overview` 一致，并保持 `side_effect=none`；调用后不得创建 approval/mutation、不得执行 cleanup、不得删除 metadata/文件、不得触发 OCR/VLM/RAG/AI。
- [x] 已查看 `/v1/runtime-overview`、`/v1/media-assets/retention-plan?limit=50`、`/v1/media-assets/retention-diagnostics?limit=50` 与 runtime overview panel 资产：`media_asset_retention_plan` 与 `media_asset_retention_cleanup` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `Required Steps / Verification Steps / Rollback Steps`、retention candidate/totals、cleanup totals、endpoints、blockers 与 notes。当前 live 结果为 `reason=media_asset_retention_no_cleanup_candidates`、`candidate_count=0`、`asset_count=50`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.media_asset_retention_plan_table=true` 与 `media_asset_retention_cleanup_table=true` 纳入当前 turn 取证；调用后不执行 cleanup、不创建 approval/mutation、不删除 metadata/文件、不触发 OCR/VLM/RAG/AI。
- [x] 已查看 `/v1/runtime-overview` 与 runtime overview panel 资产：`media_asset_retention` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `assets/cleanup_due/default/ephemeral/permanent/unknown`、recent assets 的 `Retention Class / Cleanup Due / Cleanup After / Reason / Target`，以及 diagnostics notes。当前 live 结果为 `value=0/200`、`status=ok`、`assets=200`、`cleanup_due=0`、`default=200`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.media_asset_retention_table=true` 纳入当前 turn 取证；调用后不执行 cleanup、不创建 approval/mutation、不删除 metadata/文件、不触发 OCR/VLM/RAG/AI。
- [x] 已查看 `/v1/runtime-overview` 与 runtime overview panel 资产：`dead_letters` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 AgentJob / outbox dead letters 的 totals、recent rows 和 dead-letter totals。当前 live 结果为 `value=102`、`status=danger`、`agent_job_metrics.dead_letters.current_total=0`、`outbox_metrics.dead_letters.current_total=102`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.dead_letters_table=true` 纳入当前 turn 取证；调用后不 ack/nack/term MQ、不重试 AgentJob 或 outbox delivery、不修改 queue owner / delivery policy、不触发 Python AI。
- [x] 已查看 `/v1/runtime-overview` 与 runtime overview panel 资产：`checkpoint_lag` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `checkpoints/max lag/workers/stale after`，以及 lagged checkpoint 的 `Lag Messages / Checkpoint Seq / Freshness / Reason`。当前 live 结果为 `value=0`、`status=ok`，并且当前 diagnostics 没有 lagged checkpoints。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.checkpoint_lag_table=true` 纳入当前 turn 取证；调用后不推进 checkpoint、不创建/重试 AgentJob、不修改 worker lease / queue owner、不触发 Python AI。
- [ ] 查看 `/v1/knowledge-pipeline-diagnostics` 与 runtime overview `Knowledge Pipelines` card：对 observe-only QQ 群应能直接看出 ready / warn / blocked，且 blocked 场景能区分 capture 问题还是 knowledge worker coverage 问题。
- [ ] 观察 `/v1/knowledge-pipeline-diagnostics` 的 `latest_source_seq` / `memory_checkpoint_lag` / `rag_checkpoint_lag_max`：真实群消息增长后，应能看到 lag 变化，并在高压积压时把 stalled target 计入 runtime overview `knowledge_pipeline_stalled_targets`。
- [ ] 观察 `/v1/knowledge-pipeline-diagnostics` 的 checkpoint `age_seconds`：当群持续有新消息但 memory / rag checkpoint 不推进时，应先看到 `*_checkpoint_stagnant`，并把 stale/stagnant target 计入 runtime overview `knowledge_pipeline_stale_checkpoint_targets` / `knowledge_pipeline_stagnant_targets`。
- [ ] 观察 `/v1/knowledge-pipeline-diagnostics` 的 stage `freshness_status` / `freshness_reason`：当 `group_memory_extract` 或 `rag_ingest` 长时间停在旧 lease 上时，应看到 `*_lease_stale` 或 `*_lease_expired`，并把目标计入 runtime overview `knowledge_pipeline_stale_active_lease_targets` / `knowledge_pipeline_expired_active_lease_targets`。
- [ ] 观察 `/v1/knowledge-pipeline-diagnostics` 的 `rag_datasets`：同一群绑定多个 RAG dataset 时，应能直接看到具体 `dataset_id` 的 lag / freshness / blocked 状态，并在 runtime overview `knowledge_pipeline_rag_dataset_*` summary 中聚合。
- [ ] 观察 `/v1/knowledge-pipeline-diagnostics` 的配置期 dataset 可见性：当某个 observe-only QQ 群已配置 `ragflow_dataset_ids` 但还没跑出 `rag_ingest` job/checkpoint 时，应能看到 `configured_dataset_not_started`，并在 runtime overview `knowledge_pipeline_configured_rag_*` summary 中聚合。
- [ ] 观察 `/v1/knowledge-pipeline-diagnostics` 的 `ingest_snapshot`：当某个 dataset 成功完成一次 `rag_ingest` 后，应能看到最近一次 ingest 的 `message_count`、`document_count`、`start_seq`、`end_seq`、`parse_requested` 和 `display_name`，并在 runtime overview `knowledge_pipeline_rag_dataset_ingest_snapshots` 中聚合。
- [ ] 观察 `/v1/knowledge-pipeline-diagnostics` 的 `rag_index_state`：成功 ingest 且 `document_count>0` 时应为 `index_ready`；`document_count=0` 时应为 `no_index_documents`；未产生 snapshot 的配置期 dataset 应为 `no_ingest_snapshot`；source seq 增长后应能看到 `index_lagging_source_seq` 和 runtime overview `knowledge_pipeline_rag_dataset_index_*` summary。
- [ ] 观察 Go `Agent Workers` live 状态：重启 Python 主服务后确认 image/knowledge/rag_eval/outbox worker 上报 `starting` / `idle` / `running` / `stopped` 或 stale。
- [x] 已完成 Python 主进程重启后的 worker-status takeover live 验证：使用 `uv run python main.py` 重启本地主进程后，运行 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-agent-worker-status-restart.ps1`，当前已真实返回 `conclusion.status=live_verified`、`active_worker_instance_ids_match_current_port_owner=true`、`no_worker_status_conflict_lines=true`、`no_worker_status_report_failed_lines=true`。这证明 stale lease 会在旧 PID 已退出时被新 reporter 安全接管，而不是持续卡在 `agent worker status lease conflict`。
- [x] 已新增并运行 `.\scripts\verify-agent-worker-status-fencing-live-smoke.ps1`：脚本会启动隔离 temp `agent-runtime`，对同一 synthetic `worker_id` 先写入 `instance_a`，再以 `instance_b` 默认上报，当前真实返回 `HTTP 409` 且冲突文本包含 `existing_instance_id=instance_a`；随后仅在显式 `replace_existing_instance_id=instance_a` 时 takeover 成功，并且 `/v1/agent-worker-statuses` 与 temp `agent-worker-statuses.json` 最终都切到 `instance_b`。这证明 Go worker-status lease fencing 的默认拒绝和受控 takeover 边界已经 live verified，而不影响长期运行的本地 runtime。
- [x] 已新增并运行 `.\scripts\verify-agent-worker-status-heartbeat-live-smoke.ps1`：脚本会启动隔离 temp `agent-runtime`，对同一 synthetic `worker_id + instance_id` 连续上报三次 `running` heartbeat。当前真实返回三次 `202`，并且 `GET /v1/agent-worker-statuses` 与 temp `agent-worker-statuses.json` 都显示 `updated_at` 和 `lease_until` 随 heartbeat 持续推进，最终 `lease_active=true`、`stale=false`。这证明 Go worker-status renewal 语义已经有 repo-owned live smoke 固定下来，而不需要依赖长期运行 runtime 上的人肉盯梢。
- [x] 已新增并运行 `.\scripts\verify-agent-worker-status-cleanup-live-smoke.ps1`：脚本会先在隔离 temp `agent-runtime` 中制造一条 stale worker 和一条 active worker，再调用 `POST /v1/agent-worker-statuses/cleanup-stale`，当前真实证明 stale worker 会进入 `deleted` 而 active worker 保留在 `remaining`，并且 temp `agent-worker-statuses.json` 与 `GET /v1/agent-worker-statuses` 一致。随后在当前 live runtime 上执行受控 cleanup 后，历史 stale `akashic-python-worker` 残留已删除，当前 `/v1/agent-worker-statuses.totals.stale=0`、`/v1/runtime-overview.summary.agent_workers_stale=0`、`Agent Workers` card 已回到无 stale 残留状态。
- [x] 已新增并运行 `.\scripts\verify-agent-worker-status-startup-prune-live-smoke.ps1`：脚本会预写一份同时包含 stale + active worker 的 `agent-worker-statuses.json`，再启动隔离 temp `agent-runtime`。当前真实证明 stale worker 会在 repository load 时直接被 prune，不会重新出现在 `GET /v1/agent-worker-statuses` 或 state file 中，而 active worker 仍保留，且 `/v1/runtime-overview.summary.agent_workers_stale=0`。本轮还已重启当前 8780 live runtime 并确认 `GET /v1/agent-worker-statuses.totals.workers=0`、`stale=0`，`knowledge_job_planner_readiness_worker_stale=0`；当前剩下的是 `knowledge_worker_unavailable` 的真实执行面缺口，不再是 stale heartbeat 假阻塞。
- [x] 已新增并运行 `.\scripts\verify-receiver-status-cleanup-live-smoke.ps1`：脚本会先在隔离 temp `agent-runtime` 中制造一条 stale receiver 和一条 active receiver，再调用 `POST /v1/receiver-statuses/cleanup-stale`，当前真实证明 stale receiver 会进入 `deleted`，active receiver 保留在 `remaining`，并且 temp `receiver-statuses.json` 与 `GET /v1/receiver-statuses` 一致。当前 live runtime inspection 还确认仍有 2 条 `heartbeat_stale` receiver-status 记录，但本轮默认未对 live runtime 执行 cleanup。
- [x] 当前 live runtime 已真实执行 `POST /v1/receiver-statuses/cleanup-stale`：新 `agent-runtime` 进程加载本轮 handler 后，cleanup endpoint 返回 `deleted=2`；随后 `receiver-statuses.json` 变为空，`/v1/runtime-overview.summary.receiver_statuses=0`，并且当前再次读取 `GET /v1/receiver-statuses` 也已回到 `receivers=0`。
- [ ] 启用 NATS `external_lease` smoke 时观察 `/v1/queue-backend`：`external_lease.diagnostics.executed_total` 应随消费增长，`dispositions` 应反映 ack/nack/term，recent executions 不触发 Python AI job 或真实平台发送。
- [ ] 查看 `/v1/queue-backend` 的 `provider_capabilities`：当前 provider 应有 `status=selected`；NATS JetStream 应显示 `recommended=true`、`supports_concurrent_consumers=true`、`supports_delayed_nack=true`；Redis Streams / RabbitMQ 不应显示 external lease 已实现。
- [x] 已运行 `.\scripts\verify-mq-adapter-boundary.ps1`：当前 live 结果已真实证明 `/v1/queue-backend` 的 selected provider 可见，`nats_jetstream` 仍是 `recommended_first_backend` 且 `implemented=true`，`redis_streams` / `rabbitmq` 仍是 `status=planned/implemented=false`。同时 dashboard `/api/dashboard/runtime-overview` 的 `queue_backend` 已保留 `supported_providers`、`provider_capabilities` 和 `selected_provider_capability`，不再丢失 MQ capability matrix。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`queue_backend` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `provider/mode/migration_phase`、`outbox_execution_owner/outbox_execution_scope`、`agent_job_execution_owner`、`recommended_first_backend` 和 provider capability matrix。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.queue_backend_table=true` 纳入当前 turn 取证；调用后不连接 MQ、不 publish/lease/ack/nack/term、不发送 QQ/Telegram、不触发 Python AI。
- [x] 已查看 `/v1/runtime-config`、`/v1/runtime-overview` 与 runtime overview panel 资产：`runtime_config` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `QQ Group Send`、`Telegram Token`、`OneBot Endpoints`、`Expected Channels`、`Strict Lease Token`、`AgentJob External Lease`、runtime / worker flags 和 `Environment Keys`。当前 live 结果为 `runtime.address=127.0.0.1:8780`、`qq_group_send_enabled=false`、`telegram_token_configured=false`、`agent_job_strict_lease_token=false`、`queue_external_lease_agent_job_enabled=false`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.runtime_config_table=true` 纳入当前 turn 取证；调用后不修改 runtime 配置、不补 token/endpoint、不发送 QQ/Telegram、不触发 Python AI。
- [x] 已查看 `/v1/runtime-overview`、`/v1/knowledge-job-planner/readiness` 与 `/v1/knowledge-job-planner/cutover-plan`：`knowledge_job_planner_preview` 与 `knowledge_job_planner_readiness` 当前都已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 preview 的 `Group Memory Jobs / Observe Only` 与 readiness 的 `Planner Running / Worker Active`。当前 live 结果为 `preview.value=6/6`、`readiness.value=ready`、`ready=true`、`planner_running=true`、`knowledge_worker_ready=true`、`current_admission_owner=go_runtime_knowledge_job_planner`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.knowledge_job_planner_preview_table=true` 与 `knowledge_job_planner_readiness_table=true` 纳入当前 turn 取证；调用后不创建 AgentJob、不切 owner、不启动/停止 worker、不发送 QQ/Telegram、不触发 Python AI。
- [x] 已运行 `.\scripts\verify-worker-control-executor-boundary.ps1`：当前 live 结果已真实证明 `/v1/agent-job-capacity/plan`、`/v1/agent-job-priority/plan`、`/v1/control-mutations/policy`、`/v1/runtime-workers` 与 `/v1/runtime-overview.summary` 在当前 turn 上保持一致，且 capacity/priority 仍是只读 plan、policy 已 allowlist `agent_job_capacity/apply|rollback` 与 `agent_job_priority/apply|rollback`，runtime workers 里没有 autoscaling / concurrency / priority executor candidate；`conclusion.category=go_control_plane_live_verified_without_worker_control_executor`。调用后未 apply control mutation、未启动/停止 worker、未触发 Python AI。
- [x] 已运行 `.\scripts\verify-queue-topology-boundary.ps1`：当前 live 结果已真实证明 `/v1/queue-topology`、`/v1/queue-backend` 与 `/v1/runtime-overview.summary` 在 `provider=local`、`selected_provider=local`、`recommended_provider=nats_jetstream`、`outbox_execution_owner=go_local_outbox_worker`、`agent_job_execution_owner=python_ai_worker_state_store_lease`、`agent_job_ack_owner=go_state_store_api` 与 `external_lease_ready=false` 上完全一致，`conclusion.category=queue_topology_boundary_live_verified_with_runtime_overview`。调用后未 publish/lease/ack/nack/term MQ、未创建 AgentJob/outbox delivery、未触发 Python AI worker 或平台发送。
- [x] 已查看 `/v1/runtime-overview` 与 Python dashboard `/api/dashboard/runtime-overview` 的 `queue_topology_*` summary、`Queue Topology` card 和 `queue_topology` detail：当前 summary 已稳定返回 `queue_topology_nodes=4`、`queue_topology_edges=2`、`queue_topology_work_kinds=2`、`queue_topology_external_lease_ready=false`、`queue_topology_outbox_execution_owner=go_local_outbox_worker`、`queue_topology_agent_job_execution_owner=python_ai_worker_state_store_lease`、`queue_topology_agent_job_ack_owner=go_state_store_api`，并与 `/v1/queue-topology` 的 work_kinds / ack owner 一致；调用后未 publish/lease/ack/nack/term MQ、未创建 AgentJob/outbox delivery、未触发 Python AI worker 或平台发送。
- [x] 已查看 runtime overview dashboard 的 `Queue Topology` detail：当前 panel 已稳定展示 provider/mode/phase、nodes/edges 和 work kind 表格，并直接显示 `Execution Owner / Ack Owner / Allowed / Blockers`；`.\scripts\verify-go-migration-goal.ps1` 当前也已返回 `dashboard_read_models.queue_topology_table=true`。打开 detail 不会 publish/lease/ack/nack/term MQ，不会创建 AgentJob/outbox delivery，不会触发 Python AI。
- [ ] 查看 `/v1/runtime-overview.summary`：应包含 `queue_provider_status`、`queue_provider_recommended_phase`、`queue_provider_supports_concurrent_consumers`、`queue_provider_supports_delayed_nack`、`queue_provider_supports_external_lease`，且 queue backend card value 应带 NATS 推荐阶段。
- [ ] 查看 `/v1/queue-backend` 和 `/v1/runtime-overview.summary`：`outbox_execution_owner` / `queue_outbox_execution_owner` 应随 local worker、external lease gate 改变；`agent_job_execution_owner` / `queue_agent_job_execution_owner` 应始终明确 Python AI worker 负责执行，NATS 只在 result-ack ready 时参与确认。
- [ ] 查看 `/v1/runtime-overview` 的 `delivery_smoke_*` summary 和 `Delivery Smoke` card：应与 `/v1/delivery-smoke/readiness` 的 ready/reason/cases/blockers 一致，且调用后不得发送 QQ/Telegram 消息、租约 outbox 或触发 Python AI。
- [x] 已查看 runtime overview panel 资产与 unified goal verifier：`delivery_smoke_readiness` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `ready/reason`、`cases/ready/not_ready` totals，以及每个 smoke case 的 `name/channel/chat_id/conversation_type/kind/step_count`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.delivery_smoke_readiness_table=true` 纳入当前 turn 取证；调用后不发送 QQ/Telegram、不创建 outbox、不触发 Python AI。
- [x] 已查看 `/v1/runtime-overview` 与 Python dashboard `/api/dashboard/runtime-overview` 的 `worker_leases` card 和 detail：当前 panel 已稳定展示 `Worker Leases / Checkpoint Prefix / Stale Leases` 结构化字段；对于旧 Go overview payload 缺失 `worker_leases` card 的情况，dashboard reader 也会按 `summary.worker_leases` 与 `summary.stale_jobs` 追加 fallback card。调用后未修改 lease、未执行 cleanup/takeover、未发送 QQ/Telegram、未触发 Python AI。
- [x] 已查看 `/v1/runtime-overview` 与 runtime overview panel 资产：`runtime_health` 与 `stale_jobs` 当前都已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `Runtime Health / Health Errors` 和 `Stale Jobs / Latest Updated`。当前 live 结果为 `runtime_health.value=ok/status=ok`、`stale_jobs.value=0/status=ok`、`stale_jobs.jobs=200/stale_leases=0/workers=3`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.runtime_health_table=true` 与 `stale_jobs_table=true` 纳入当前 turn 取证；调用后不执行 runtime recovery、不执行 stale lease cleanup/takeover/retry、不修改 worker 或 job 状态、不发送 QQ/Telegram、不触发 Python AI。
- [x] 已查看 `/v1/runtime-overview` 与 runtime overview panel 资产：`job_events` 与 `outbox_events` 当前都已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 event totals 和 recent event rows，并兼容旧 `recent_events` 形状与当前 live metrics-nested detail。当前 live 结果为 `job_events.value=50/status=ok`、`outbox_events.value=50/status=ok`，且当前 live detail 形状分别是 `job_events.detail.agent_job_metrics` 与 `outbox_events.detail.outbox_metrics`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.job_events_table=true` 与 `outbox_events_table=true` 纳入当前 turn 取证；调用后不创建/租约/重试 AgentJob，不 dispatch/cleanup/retry outbox，不修改 worker、lease、dead-letter 或 event store 状态，不发送 QQ/Telegram，不触发 Python AI。
- [x] 已查看 `/v1/runtime-overview` 与 runtime overview panel 资产：`rag_eval_failures` 当前已是结构化只读 drilldown，不再只停留在 raw JSON；panel 已展示 `sampled jobs / rag eval dead letters / total dead letters / failed events / terminal events`、recent rag-eval failure rows 和 notes，并兼容 dashboard fixture `detail.items` 与当前 live `detail.agent_job_metrics`。当前 live 结果为 `rag_eval_failures.value=0/status=ok`，且当前 live detail 形状为 `rag_eval_failures.detail.agent_job_metrics`。`.\scripts\verify-go-migration-goal.ps1` 也已把 `dashboard_read_models.rag_eval_failures_table=true` 纳入当前 turn 取证；调用后不创建/租约/重试 `rag_eval` AgentJob，不修改 dead-letter/lease/worker 状态，不触发 Python AI。
- [ ] 启用 `agent_job` NATS result-ack 前查看 `/v1/agent-job-external-lease/readiness`：应同时满足 external lease allowed、`agent_job` allowed work kind、strict lease token、`AKASHIC_QUEUE_EXTERNAL_LEASE_AGENT_JOB_ENABLED=true` 和 Python worker coverage 非 danger。
- [ ] 启用 `agent_job` NATS result-ack 前查看 `/v1/agent-job-external-lease/plan`：确认 required checks、enable env、verification endpoints 和 rollback steps 与本次计划一致，且 `side_effect=none`；尤其确认 rollback 只移除 agent_job result-ack scope，不影响 outbox external lease。
- [ ] 启用 `agent_job` NATS result-ack 前查看 `/v1/runtime-overview`：`agent_job_external_lease_*` readiness summary/card 应与 `/v1/agent-job-external-lease/readiness` 一致；`agent_job_external_lease_plan_*` summary 和 `Agent Job External Lease Plan` card 应与 `/v1/agent-job-external-lease/plan` 的 decision/owners/blockers 一致，且调用后不新增或执行 AgentJob。
- [x] 已新增并运行 `.\scripts\verify-agent-job-external-lease-nats-smoke.ps1`：脚本会自动拉起临时 `nats:2-alpine -js`，执行
  `TestExternalLeaseNATSSmokeAgentJobDuplicateTerminalAck` 与
  `TestExternalLeaseNATSSmokeAgentJobPendingRunningSucceededFlow`，并输出结构化
  JSON。当前 live 结果已真实返回 `temp_nats_ready=true`、
  `duplicate_terminal_ack_passed=true`、
  `pending_running_succeeded_flow_passed=true`、
  `conclusion.category=repo_owned_temp_nats_smoke_live_verified`。这证明
  repo-owned Go result-ack smoke 路径已稳定可复跑，但不等于当前长期运行
  runtime 已完成 production cutover。
- [x] 已新增并运行 `.\scripts\verify-agent-job-external-lease-launcher-bundle.ps1`：脚本会先在 blocked temp runtime 上验证 `GET /v1/agent-job-external-lease/launcher-bundle` 返回 canonical NATS/result-ack flags、`QueueDSN` external input 和 verification steps，再在 temp NATS/runtime 上直接用 bundle 动态拼出 `start-agent-runtime.ps1` 参数并拉起 promoted runtime。当前 live 结果已真实证明 queue backend / queue topology / runtime-overview / approval-bound preflight 会一起进入 `repo_owned_launcher_external_lease_bundle_live_verified`，说明剩余 gap 已不再是 launcher contract 缺失，而是 production runtime 还没真正注入这些 flags 并切 owner。
- [x] 已新增并运行 `.\scripts\verify-agent-job-external-lease-cutover-diff.ps1`：脚本现在会先对当前 8780 live runtime 读取 `GET /v1/agent-job-external-lease/cutover-diff`，真实验证 external-lease/result-ack 的 missing flags、`QueueDSN`、queue provider/mode 和 `agent_job execution/ack owner` drift；随后再用同一 canonical launcher bundle 拉起 temp NATS/runtime，先造 aged `group_memory_extract` pressure，验证 `zero config drift + agent_job_worker_coverage_blocked`，再上报 knowledge worker heartbeat，最终进入 `repo_owned_agent_job_external_lease_cutover_diff_live_verified`。本轮还重启了当前 8780 `agent-runtime` 到最新代码，并按既有 gate 参数恢复了 `outbox_execution_owner=go_local_outbox_worker` 与 `outbox_execution_scope=account_conversation_kind_gated`。
- [x] 已查看 dashboard runtime overview panel 资产与 `/api/dashboard/runtime-overview`：`agent_job_external_lease_readiness` 与 `agent_job_external_lease_plan` 已不再只停留在 raw JSON；当前 panel 已真实展示 `Current Owner / Desired Owner / Recommended Owner`、worker coverage、blockers、notes 以及 `Required Checks / Enable Steps / Verification Steps / Rollback Steps`。打开 detail 只读，不修改 env、不执行 cutover、不 ack/nack MQ、不创建/执行 AgentJob、不触发 Python AI。
- [ ] 启用 NATS `external_lease` smoke 后观察 `/v1/runtime-overview`：summary 中 `queue_external_lease_*` 应与 `/v1/queue-backend.external_lease.diagnostics` 一致，`external_lease_diagnostics` card 状态应按 error/nack/term 变化。
- [ ] 若未来开始落地 worker control executor，先复跑 `.\scripts\verify-worker-control-executor-boundary.ps1`：当前它应仍返回 `go_control_plane_live_verified_without_worker_control_executor`；只有当 approval / audit / rollback 闭环、runtime worker 和实际执行路径一起落地后，才允许把该分类改成 executor-present。
- [ ] 压测或人为堆积 outbox 后观察 `/v1/outbox-metrics` 与 `/v1/runtime-overview`：`outbox_pressure_high_accounts`、`max_active`、`max_queued` 应随账号积压增长；该诊断只读，不应阻断真实发送。
- [ ] 启用 Go local outbox worker 账号节流后做 dry/live smoke：短时间连续发送同一账号 delivery 时，第二条应保持 queued 且不增加 attempts；其它账号 delivery 仍可被租约执行。
- [ ] 启用 NATS `external_lease` outbox smoke 并配置账号节流后，连续触发同一 `channel_kind:account_id` 的 outbox work：第一条可执行，第二条应在租约前返回 `nack/delivery_rate_limited`，Go outbox delivery 不应增加 attempts 或进入 dispatching。
- [ ] 做 external lease cutover preflight 时确认：若 `AKASHIC_OUTBOX_DELIVERY_WORKER_ENABLED=true`，`/v1/queue-backend.external_lease.blockers` 应包含 `local_outbox_worker_disabled`，且 `allow_execution=false`；关闭 local worker 后再看其它 smoke gate。

## Proactive

- [x] 已新增并运行 `.\scripts\verify-proactive-runtime-flow-live-smoke.ps1`：脚本会启动隔离 temp `agent-runtime`，真实串起 Go-owned proactive deterministic state 的 deliveries duplicate/count、seen-items normalized hit、rejection cooldown、cleanup、context-only、anyaction quota、drift mark/finish/summary/skill state、bg-context global mark，以及 tick-log start/step/finish/list/detail/steps，并核对 temp `proactive-state.json` 中 `deliveries/seen_items/rejection_cooldowns/context_only/session_marks/global_marks/anyaction_quotas/drift_skills/drift_recent_runs/tick_logs/tick_steps` 的持久化。当前 live 结果为 `conclusion.status=live_verified`、`category=go_proactive_state_flow_live_verified`，且全过程不触发 QQ/Telegram 发送、不启动 Python proactive loop、不触发 LLM/provider/tool/OCR/VLM/image generation。
- [ ] 观察 AnyAction quota：主动推送实际触发后确认 `.akashic-workspace/agent-runtime/proactive-state.json` 中 `anyaction_quotas.used` 增长，fallback 不放宽 quota。
- [ ] 观察 seen/rejection/cleanup：主动候选流运行后确认 `seen_items` 或 `rejection_cooldowns` 出现在 Go proactive state，并在 cleanup 后过期记录减少。
- [ ] 观察 background context：主 topic 触发后确认 `global_marks` / `bg_context_last_main_at` 出现在 Go proactive state，SQLite fallback 不让节流回退。
- [ ] 观察 drift state：启用 drift 后完成一次 `finish_drift`，确认 Go proactive state 中出现 `drift_skills`、`drift_recent_runs` 和可选 `drift_note`，workspace JSON mirror 仍写入。
- [ ] 观察 tick log：完成一次 proactive tick 后确认 Go proactive state 中出现 `tick_logs` 和可选 `tick_steps`，SQLite `tick_log` / `tick_step_log` mirror 仍写入。
- [x] 已新增并运行 `.\scripts\verify-dashboard-proactive-tick-logs-boundary.ps1`：脚本会启动隔离 temp `agent-runtime`，先在 Go runtime 中写入 synthetic tick，再用空 SQLite dashboard workspace 调用 `/api/dashboard/proactive/tick_logs`、detail 和 steps。当前 live 结果已真实证明 dashboard 会从 Go runtime 返回 tick list/detail/steps，且 SQLite `tick_log` 在读取前后都保持 `0`，不会把 runtime tick 数据写回 mirror。剩余未覆盖的仅是“runtime 不可用时仍回到 SQLite/404”的人工场景。
- [x] 已重跑 `.\scripts\verify-go-migration-goal.ps1` 并检查 `.codex-goal-verifier.json`：当前 `dashboard_read_models.proactive_tick_logs_readable` 与 `checks.dashboard_read_models.proactive_tick_logs_readable` 都已可直接读取，不再出现 top-level 已验证但 `checks.*` 为 `null` 的 artifact 形状偏差。该修正不改变 live runtime 行为，只强化 current-turn evidence 的机器消费路径。

## Scheduler

- [x] 已新增并运行 `.\scripts\verify-scheduler-runtime-live-smoke.ps1`：脚本会启动隔离 temp `agent-runtime`，向其 `POST /v1/scheduler/jobs/upsert` 写入 synthetic one-shot job，确认临时 `scheduler-jobs.json` 落盘，再通过 `DELETE /v1/scheduler/jobs/{job_id}` 确认 HTTP state 和文件 state 都移除对应 job；整个路径不触发 QQ/Telegram 发送。
- [x] 同一脚本已验证 completion mutation：对 synthetic one-shot job 先 acquire lease 再执行 `action=delete`，对 synthetic recurring job 先 acquire lease 再执行 `action=reschedule`，确认 recurring 只更新自身、one-shot 只删除自身、lease 被释放；验证结束后 recurring smoke job 会主动 cleanup，避免污染后续 recovery evidence。
- [x] 同一脚本已验证 startup recovery reconciliation：向隔离 temp runtime 注入 overdue recurring、expired one-shot 和 future one-shot，再调用 Python `SchedulerService.load_and_recover()` against 该 temp runtime，确认 recurring 被推进到未来、expired one-shot 被删除、future job 被保留、diagnostics 不再报告 overdue job，且整个过程不触发 QQ/Telegram 发送、不全量覆盖其它 job。

## SDD Governance

- [ ] 新增或重命名 `docs/sdd/specs/agent-gateway/[0-9][0-9][0-9]-*.md` 后，必须同步更新 `docs/sdd/specs/agent-gateway/000-index.md`，并运行 `uv run pytest tests/test_sdd_spec_index.py -q`。
- [ ] 新增 `services/agent-runtime` Go 源码目录前，先确认它属于 `api/app/cmd/domain/infrastructure/smoke/trigger/types` 之一；否则需要先更新 SDD package 设计并让 `go test .` 的架构守卫通过。
- [ ] 每轮结束前检查 `docs/sdd/OPEN_ISSUES.md`：新增未解决风险、待决策项或长期缺口应进 OPEN_ISSUES；只有本轮承诺解决的拆解任务才进入 `TODO.md`。
- [ ] 修改 `docs/sdd/OPEN_ISSUES.md` 后运行 `uv run pytest tests/test_sdd_governance_docs.py -q`，确认 open issue ID、字段和状态仍然可解析。
